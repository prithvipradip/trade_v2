# Blind-Spot + Composition Hunts — 2026-08-25/26 (R23 register)

Round 2 (blind-spot): 8 classes the relationship hunt could not see — concurrency,
multi-process DB, external contracts, log contracts, policy-vs-implementation,
fail-open/fail-closed direction, model-vs-reality, dead surface.
51 candidates -> 50 standing (37 CONFIRMED, 13 ENFORCEMENT_GAP).

Round 3 (composition): 4 vertical whole-flow audits — trade-life, bot-day,
research-to-live, money-flow. Each auditor reconstructed the full end-to-end flow
map first (collective context), then hunted defects existing only in composition.
27 candidates -> 27 standing (22 CONFIRMED, 5 ENFORCEMENT_GAP).

Combined with the R22 relationship register (46): 123 standing findings total.
Status: REGISTERED, NOT YET FIXED.

# Round 2 — blind-spot classes

## concurrency-1  [CONFIRMED | live_money]

**Proof:** Re-read all cited sites and re-derived the claim: executor.py:2328-2332 discards the transition() CAS result and unconditionally executes update_trade_entry_price (state.py:666-670, blind UPDATE), insert_open_position (state.py:440-452, INSERT OR IGNORE — lands as a fresh row because close_trade deleted the open_positions row in its own transaction at state.py:407-411), and update_trade_quantity (state.py:672-682, blind UPDATE on both tables). Executed an independent probe on a real StateManager (temp DB): record PENDING trade at 1.90 -> sweep close via close_trade(from_statuses=(PENDING,)) landed True -> replayed _update_trade_filled's exact call sequence: CAS returned False with illegal_transition_blocked logged, yet the trades row ended ('closed', 1.62, 0.0, 0.0, 'stale_pending_never_filled', 1) — entry_price rewritten 1.90->1.62 on the $0 CLOSED row — plus 1 orphan open_positions row. Reachability verified: the sweep is SYNCHRONOUS in the loop task (orchestrator.py:3151 -> reconciler.py:749-832) while fill booking runs in a separate event task with a 1.0s debounce sleep (executor.py:741) and mid-pass awaits (executor.py:875/1000/1229) — concrete task-switch points; _cancel_working_entry_order excludes terminal orders (reconciler.py:591-599) so a just-filled order stops deferring the sweep; _live_ibkr_leg_keys reads only the cached ib_insync position stream (reconciler.py:455-473) with freshness demanded only for an EMPTY cache (768-784); the Filled verdict comes from the cached Trade whenever the status message arrives, however late (executor.py:1956-1961), and the sweep never removes the executor's _pending_orders tracker, so ANY later check_fills pass performs the stomp then logs trade_filled and deletes the tracker (executor.py:878-897, 969-970). The codebase documents both race halves — cancel-races-fill (executor.py:1191-1196) and the concurrent-fill-vs-$0-booking 'broker-lag race' (state.py:364-368) — but defends only the close-after-fill ordering, not fill-after-close. A 30-min PENDING with a live tracker is reachable via a trading-halt/cancel-reject or repeatedly-deferred cancel verdicts (executor.py:873-876, 1062), i.e. exactly the degraded regimes R16/R19 comments describe.

**Consequence:** A stale entry that fills as (or just after) the sweep books it '$0 never filled' leaves a REAL multi-leg short-premium position at the broker managed by nothing — check_positions iterates only FILLED/PARTIAL trades (portfolio.py:234-236), so no stop, no take-profit, no touch stop, no expiry exit until the untracked-option freeze's two-sighting debounce (reconciler.py:1026-1059); meanwhile the trades table shows a CLOSED $0 row whose entry_price was silently rewritten to the real fill and whose realized_pnl=0 corrupts P&L history and the go-live verdict metrics, plus a permanent orphan open_positions row.

**Suggested fix:** In _update_trade_filled and _update_trade_partial, gate every side-effect write on the CAS result: if transition(...) returns False, log/page ('fill verdict landed on terminal row — real position exists, row closed') and return without touching entry_price/quantity/open_positions; escalate to the operator instead of silently dropping the tracker, since a Filled-after-close means a live unbooked position.

## concurrency-2  [CONFIRMED | live_money]

**Proof:** All cited sites re-verified: the close commits at executor.py:1129-1134 and its booking rides completed_exits through the rest of the pass (awaits at 1000 and 1229 for OTHER orders), up to 3 rerun passes (805-811), then the fire-and-forget _run task (739-755) whose 'except Exception' logs event_fill_check_failed and discards the list; CancelledError (BaseException) isn't caught at all. Grep proves _bg_tasks is never drained anywhere (only created at executor.py:256 and discard-on-done at 768-772); _shutdown drains only _notify_tasks (orchestrator.py:3065-3070); main.py:186-188 runs stop()+disconnect and asyncio.run teardown then cancels lingering tasks at whatever await they occupy (and the graceful path's wait_for timeout at main.py:85-88 can cancel even run() mid-pass). Executed a probe through the REAL check_fills_safe: pass 1 returned a committed -$412 close, the rerun pass raised -> exits booked to stats/breaker/Thompson = [], only a warning log. Unrecoverability verified on three independent legs: (1) re-emission is refused by design — executor.py:1125-1135 'A refused close must NOT emit a completed-exit event' and the tracker is deleted at 1154; (2) the breaker's daily P&L is a pure in-memory accumulator, circuit_breaker.py:202-204 'self._daily_pnl += pnl', never recomputed from trades; (3) daily_stats is incremental read-modify-write at all four update_daily_stats call sites (orchestrator.py:526, 2133, 2438, 2928 — checked each; none recompute from the trades table) and ThompsonSampler persists its own arm state (thompson.py:77-80), so the divergence never self-heals. The MTM brake also under-counts after the close since it reads the same daily_stats total (orchestrator.py:642-643). Restarts documented common at orchestrator.py:287-292, and the loss window is CORRELATED with restart causes: a pass stalled in _broker_open_order_ids against a dead Gateway is precisely when the supervisor kills/cancels the process; a hard kill between the sqlite commit and the callback loses the booking with no asyncio subtlety at all.

**Consequence:** A stopped-out loss (e.g. -$412 short-strike-touch exit) is CLOSED in SQLite but never reaches record_trade_result, so the daily-loss circuit breaker and the MTM brake under-count the day's realized loss by that amount and fail to halt when the cap is genuinely breached — the bot keeps entering trades for the rest of the day past its risk limit; daily stats, Thompson strategy weights, and the PDT day-trade counter permanently diverge from the trades table with no repair path.

**Suggested fix:** Make booking transactional with the close: write a completed-exit outbox row in the SAME sqlite transaction as close_trade, have the orchestrator book from the outbox (idempotently, deleting after success) on every cycle and at startup; as belt-and-braces, await/drain executor._bg_tasks in _shutdown before the loop closes.

## external-contracts-02-zero-quote-reads-as-max-liquidity  [CONFIRMED | live_money]

**Proof:** Re-read all cited sites and executed the probe with the ACTIVE thresholds (min_volume=0, min_oi=10, max_spread=0.40 — confirmed identically in config.yaml:79-81 and .env:23-25, resolver logged sources): OptionContract(bid=0, ask=0, last=1.85, volume=0, OI=250, source='yahoo_delayed') -> mid=1.85, spread_pct=0.0, is_liquid=True; the IBKR shape (NaN bid/ask coerced to 0.0 exactly as options_chain.py:626-628 does, OI=0, source='ibkr') -> is_liquid=True via the OI-unknown leniency (:165). A genuinely quoted 1.80/1.90 contract scores spread_pct=0.054 — the no-market contract literally outranks every real market at 0.0. mid falls back to `last` (:141-143) and spread_pct computes (0-0)/last (:146-149); filter_liquid (:336-349) applies the same predicate. iron_condor.py wings are selected from this filter with NO delta requirement (:229-256, only the shorts pass the delta band) and price into credit via .mid at :265-267 -> signal.entry_price. Executor: the GOV-1 stale-price rejection is skipped not only on exception (executor.py:664-665) but silently whenever the live combo bid/ask are missing (the `if _bid is not None and _ask is not None` at :640 simply falls through), after which _base_mag = abs(signal.entry_price) (:674) anchors the real order's limit. ibkr_client.py:113-121 confirms delayed-frozen (type 4) is the account's only working feed. Finder's honest caveat stands: today's after-hours Yahoo chain showed no such rows, so the Yahoo trigger is regime-dependent — but the IBKR shape is manufactured by the code's own NaN coercion whenever both BBO ticks miss the 2s snapshot window, a regime the R16 comments themselves document as common.

**Consequence:** In a degraded-quote scan a far-OTM wing with no market at all is admitted as the tightest-spread contract in the chain, its hours-or-days-old last print flows into total_credit and signal.entry_price, and — precisely because the same degraded regime deprives the executor of a live combo NBBO — nothing rejects the stale price before it becomes the limit anchor of a real resting order. The bot bids/offers a 4-leg condor at a price derived from a phantom quote: mispriced entry credit, wrong max_loss/credit-floor gating, and fills at whatever the market actually is.

**Suggested fix:** Make 'no quote' read as spread-unknown, not spread-zero: in spread_pct return 1.0 (illiquid) when bid<=0 or ask<=0; or gate is_liquid on bid>0 and ask>0 outright. Separately, treat a missing live combo NBBO in the executor's GOV-1 block as a rejection (or at minimum require every leg used for pricing to have had a real BBO at signal time) instead of silently falling through to the signal-time price.

## fail-direction-02-range-gate-direction-depends-on-regime  [CONFIRMED | live_money]

**Proof:** Executed probe on the live interpreter (Python313, the one keeper_ait.bat runs): corrupt range.pkl -> `load_models` returns False with only `range_load_failed` WARNING; missing file -> False with NO log; is_trained stays False. Code walk re-derived both directions: orchestrator.py:1378 skips the whole override block when untrained (also when predict() returns None — range_predictor.py has routine None paths: no-edge gate at DEBUG (:477-481), empty features (:487-488), scaling failure (:500-502)), so model_overridden stays empty. Neutral regime (prediction.confidence<0.50, config min_confidence 0.50): lines 1444-1450 drop every IC signal (gates ON: settings.py:294 default True, config.yaml does not override; range_min_confidence 0.65 at config.yaml:170) and `if signals:` at 1451 means an emptied list logs NOTHING. Directional regime: the 1431-1436 floor passes non-overridden ICs, eff_conf=final_confidence (1494-1498), and manager.py:351 admits at >=0.50 — IC entered on a directional model with zero range evidence. trainer.py:213-214 catches range-training failure at WARNING yet :228 stamps _last_train_date unconditionally, freezing the untrained state for the retrain interval. Research contract contrast verified: walkforward.py:1376 sets range_min_confidence=1.0 on training failure (fail-closed); live has no equivalent. Artifact-clobber precedent verified: models/meta_label.pkl.clobbered_20260718 exists; range_predictor.py:40-44 documents a walkforward run overwriting live range.pkl.

**Consequence:** With range.pkl lost/corrupt (documented incident class) or predict() returning None, the payoff-matched gate armed today silently inverts by regime: zero IC entries on neutral days (silent sample stop) and, on directional days, live iron condors admitted on directional confidence — the exact wrong-payoff basis the range override exists to replace — until the next successful retrain (which a failed pass falsely stamps as done).

**Suggested fix:** Define one fail direction: when entry_gates_enabled and the range model is unavailable (untrained or predict None), block IC entries in BOTH regimes (live mirror of walkforward.py:1376) and page after a failure streak; stop stamping _last_train_date in trainer.py when range training failed so needs_training retries next cycle.

## fail-direction-03-earnings-gates-fail-open-every-mode  [CONFIRMED | live_money]

**Proof:** Executed probes reproduce all three modes. P1 (async ctx, cold cache, stubbed 200ms fetch returning a REAL earnings date 3 days out): first-call would_hold_through_earnings=False AND is_near_earnings=False; second call True — earnings.py:56-76 returns EarningsInfo(None) immediately and the gate at orchestrator.py:1971 runs inline in _try_execute, with execution at :2073 in the same call, so the slipped trade has no next tick. P1b: a failure-shaped EarningsInfo(None) IS cached with ttl~21600s (earnings.py:68-69, 39), blinding all three gates 6h per symbol per hiccup; earnings.py:94-95 and 119-120 both treat unknown as allow. Exit-side portfolio.py:698-711 wraps in `except: pass`. No notification anywhere in the family (grep). One mitigation the claim understates: once the cache warms with a real date, the exit-side 3c pre_earnings_iv_crush (portfolio.py:707-709) closes the position 0-2 days pre-event, so 'held through the release' requires mode (b)/(c) (failure cached or unknown date) — mode (a) alone still opens entries the gate exists to refuse. Consequence latent today: config.yaml:19 universe [SPY,QQQ,IWM] (ETFs, no earnings), but line 18 records NVDA/AMZN/AMD in this universe until 2026-07-22.

**Consequence:** Inert while the universe is ETF-only, but the gates run and fail open on every restart/6h expiry today; the day a single name returns, a morning restart or one cached yfinance failure inside the 2-day window opens (and with a persistent fetch failure, holds) short premium through an earnings release with zero indication any guard degraded.

**Suggested fix:** For symbols where earnings exist (non-ETF), treat unknown-earnings as BLOCKING for credit entries (fail closed) or await the first fetch with a short timeout; never cache the failure-shaped EarningsInfo (retry with short TTL); raise earnings_fetch_failed to a paged streak alert.

## fail-direction-05-iv-rank-error-fabricates-50-passes-both-iv-gates  [CONFIRMED | live_money]

**Proof:** All cited sites verified: orchestrator.py:1055-1056 `if isinstance(iv_rank, Exception): iv_rank = 50.0` with NO log (the adjacent hist failure at 1048-1049 logs a warning — the asymmetry is real); :3331-3332 store-read failure swallowed at DEBUG; :3336-3337, 3347-3348, 3354-3355 proxy degenerate cases return 50.0; the proxy's get_historical call at :3335 sits OUTSIDE any try so its exception is what reaches gather. Probe P10: a gather-raised sqlite OperationalError becomes iv_rank=50.0 which passes the IC floor (50>=15, iron_condor.py:144-146) and the VRP cap (50<=85, manager.py:435-436). Journaling verified: scan iv_rank flows into generate_all_signals (:1352), signal.iv_rank -> record at :2108 -> state.py:719 entry_iv_rank — the fabricated 50 is stored as measured and feeds meta_label.py:425 training features. Contrast text at manager.py:385-388 (R7 VIX fail-closed for the identical failure shape) confirmed verbatim. Triggers are normal-op: historical.db store read failing (locked/stale/corrupt) plus the yfinance proxy raising or returning None/degenerate data — every element has documented failure history in this repo.

**Consequence:** During exactly the outages when the IV data layer is broken, every symbol scans with a perfect pass-both-gates iv_rank of 50: the bot can sell premium through top-percentile IV regimes the R12-B cap exists to refuse (the AMD 98.8th-percentile pre-earnings incident is the documented case), and the journal permanently records 50 as a real measurement, corrupting entry_iv_rank research data.

**Suggested fix:** Replace the bare 50.0 with a logged warning and propagate 0.0/None as 'unknown': iron_condor's floor then fails closed (0<15 -> no signals) and manager's cap already skips falsy values; journal NULL instead of a fabricated rank.

## fail-direction-06-liquidity-gate-passes-quoteless-contracts  [CONFIRMED | live_money]

**Proof:** Probe under the resolved production thresholds (env AIT_LIQ_* from .env; log line confirms min_vol=0, min_oi=10, max_spread=0.4): OptionContract(bid=0, ask=0, last=2.50, volume=0, open_interest=0, source='ibkr') -> mid=2.5, spread_pct=0.000, is_liquid=True; control with last=0 -> spread_pct=1.0, illiquid. Chain-build zeroing verified at options_chain.py:626-628 (missing NBBO -> 0.0/0.0, stale `last` kept), mid/spread_pct at :139-149, and the OI unknown-is-ok IBKR carve-out at :165 completes the pass. So the wide-spread criterion — the only active liquidity criterion on this profile — scores a quote-less contract as maximally liquid, silently. One mitigation the claim omits: a FULL outage chain usually also lacks modelGreeks, and iron_condor.py:156-201 fails closed on degraded greeks/delta band, plus the EM gate at :203-215 rejects when expected move is unavailable — so the exposed path is partial degradation (greeks present, NBBO missing on some strikes), where quote-less strikes are selectable as legs and priced off stale last prints; the remaining defense is the GOV-1 combo check, which finding 07 shows fails open under the same conditions.

**Consequence:** During farm reconnects/no-NBBO stretches (routine on this delayed paper feed), quote-less strikes pass filter_liquid as best-in-class and condor credits get computed from stale last prints; combined with the fail-open combo NBBO check, both layers of stale-price protection drop together in the same outage.

**Suggested fix:** In spread_pct (or is_liquid), treat bid==0 and ask==0 as illiquid regardless of `last` (return 1.0) — the author already did this for mid<=0; the bid=ask=0-with-last case must classify the same way.

## fail-direction-07-combo-nbbo-sanity-gate-fails-open-quietly  [CONFIRMED | live_money]

**Proof:** executor.py:620-665 re-read confirms both fail-open modes exactly as claimed: (1) any exception in the fetch/validation lands at `log.debug("combo_quote_validation_skipped")` (:664-665); (2) a returned-but-empty NBBO — probe P11 shows nan bid/ask maps to None via the nan!=nan idiom at :633-634 — fails the `if` at :640 which has NO else, skipping the disorderly/stale rejections with zero log. Either way _live_mid_mag stays None and :674 anchors the marketable limit ladder to abs(signal.entry_price), which the code's own comment (:667-668) concedes 'can be minutes old'. Both failure modes (reqMktData raising, nan combo quotes) occur precisely under the degraded-quote conditions GOV-1 was added for; in a thin/disorderly book the stale limit is the fill price, which is the loss the gate exists to prevent.

**Consequence:** On a combo-quote outage or nan NBBO, an IC entry order goes to IBKR as a marketable limit anchored to a possibly minutes-old scan-time mid with no live-price sanity check — at debug/silent visibility — in exactly the disorderly-book conditions where the limit itself becomes the fill.

**Suggested fix:** Make the empty-NBBO case explicit: add an else branch logging combo_quote_unavailable at WARNING, and either refuse the order (fail closed, consistent with GOV-1's intent) or require a fresh underlying-quote staleness check before falling back to the signal-time anchor; raise the except from DEBUG to WARNING with an _alert_gate streak.

## fail-direction-08-duplicate-order-guards-fail-open-debug-or-silent  [CONFIRMED | live_money]

**Proof:** All three cited code facts verified: GUARD #2 (orchestrator.py:1932-1946) swallows any exception from get_recent_trades at log.debug — and state.py:495-503 is a bare sqlite3.connect/execute with no busy handling, so an SQLITE_BUSY window (concurrent dashboard/status/mirror readers are documented users of this DB) raises straight into the swallow; GUARD #3 (:1950-1956) swallows at log.debug AND ibkr_client.py:592-596 returns [] when not connected, so a reconnect gap silently reads as zero working orders — while execute_signal's own ensure_connected (executor.py:328) reconnects before placing, so the disconnection that blinded the guard does not stop the later placement. The remaining layer is exactly the in-memory pending tracker (:1958-1965) the GUARD #2 comment calls fragile, which is empty after every restart. Double-entry incident class is documented in-code (R6 QQQ+IWM). The composite trigger needs a coincidence (guard-blinding failure + an in-flight/unrecorded same-symbol order), but each element is a recurring normal-op condition and the fail direction of every layer is open at DEBUG-or-silent visibility.

**Consequence:** A scan during a locked-DB window plus a reconnect blip (e.g. shortly after a keeper relaunch with a working DAY entry order resting at the broker) passes all three duplicate layers and places a second live order for the same symbol+strategy, doubling exposure with only two DEBUG lines as evidence.

**Suggested fix:** Fail closed on guard failure: if the cooldown query or working-order check raises (or the client is disconnected during the check), skip the entry for this scan and log at WARNING; returning [] from get_open_orders while disconnected should be distinguishable from a verified-empty book.

## fail-direction-10-circuit-breaker-persistence-fails-open-across-restart  [CONFIRMED | live_money]

**Proof:** Executed probe with the real CircuitBreaker + live RiskConfig: attaching a state whose get_state raises OperationalError('database is locked') yields is_tripped=False, losses=0 (only circuit_breaker_state_load_failed WARNING), while the identical persisted blob through a healthy read restores is_tripped=True, losses=3 — the read failure alone erases an active trip. All three seams verified warning-only: _load_persisted (circuit_breaker.py:89-100), _persist (:129-145), attach_state wiring (manager.py:102-108); none pages. Trigger realism: keeper relaunches are the documented normal mode (~30-60 min kill cycle, per the R16 rationale in circuit_breaker.py:72-79 — the seam exists BECAUSE relaunches happen), and the load path's own author anticipated locked/unreadable DB (that is what the except catches). Partial mitigation noted honestly: the MTM daily-loss halt recomputes from daily_stats within ~30s (attach_state docstring), so the durable loss is the 3-consecutive-losses / api-failure pause — which is precisely the R16 incident being reintroduced through the persistence layer's own error path.

**Consequence:** A relaunch while ait_state.db is briefly unreadable silently clears an active 3-loss pause (and the loss streak), so the relaunched bot re-enters immediately into the same losing conditions the halt existed to pause, with only a WARNING in the file log.

**Suggested fix:** On _load_persisted failure, retry with backoff and fail CLOSED (treat the breaker as tripped / block new entries) until the state is readable, and page via the notification path; on _persist failure, schedule a re-persist instead of dropping the trip.

## fail-direction-11-restricted-list-fails-open-silently  [CONFIRMED | live_money]

**Proof:** orchestrator.py:1804-1811 re-read: bare `except: pass`, no log — contrast the HALT gate directly above (:1796-1800) which cannot fail open. Probe on the LIVE interpreter (Python313, preferred encoding cp1252) is stronger than the claim: a RESTRICTED.txt written as UTF-8-with-BOM parses to {'ï»¿SPY'} and a UTF-16LE file (Windows PowerShell 5.1's classic `>` /Out-File default) parses to null-riddled garbage — in BOTH cases 'SPY' is NOT in the banned set, no exception is even raised, so the bare except isn't needed for the gate to silently no-op; the except additionally swallows OSError (AV/permission lock) and the cp1252-undecodable byte cases. The file does not exist in data/ today, so the control is dormant, but the failing scenario is an ordinary operator action with default Windows tools (`echo SPY > data\RESTRICTED.txt` in PowerShell) at exactly the post-incident moment the control was built for.

**Consequence:** The operator drops a ban file with PowerShell defaults, sees it in place, and believes the post-incident hard-ban is in force while every scan continues trading the banned symbol with no log line recording that the gate failed or mis-parsed.

**Suggested fix:** Read with encoding='utf-8-sig' (and strip NULs / try utf-16 on BOM detect), log symbol_restriction_list_loaded with the parsed set once per change, and on any parse/read failure fail CLOSED for entries (treat like HALT) with a warning — a present-but-unreadable ban file must never mean 'no bans'.

## policy-vs-impl-1  [CONFIRMED | live_money]

**Proof:** master.py:1265-1269 SELECT has no strategy predicate; grep GO_LIVE_VERDICT_STRATEGIES hits only base.py:63 (frozenset({'iron_condor'})), status.py:19/224, PLAN.md:1329 — master.py never references it. Cron verified at master.py:1611-1613 (Fri 16:10). Read-only probe on data/ait_state.db re-executing master's exact math: ALL-STRATEGY n=17/50, 9W-8L, net -$121.46, PF 0.89 (gate >1.3 FAIL), DD $862.52 / concurrent-CAR base $8,332 = 10.4% (gate <8% FAIL). IC-ONLY (status.py:224 verdict math): n=8, 4W-4L, +$180.60, PF 1.42 PASS, DD $272.29 = 3.3% PASS. Non-IC rows in the graded population: short_strangle 4x +$377.60, long_call 3x -$131.51, bear_put_spread 1x +$27.18, long_straddle 1x -$575.33 — the R19d-excluded experiments (PLAN.md:1221-1230). Pace sub-claim verified: master.py:1341 prints 'on track' for any n>=1.

**Consequence:** Every Friday the operator's automated GO-LIVE SCORECARD Telegram reports PF FAIL and DD FAIL while the pinned R19d verdict metric passes both; the divergence flips sign as either book moves, so a funding go/no-go read off the weekly readout grades the wrong population in either direction, and 'pace: on track' asserts schedule health computed from nothing.

**Suggested fix:** Filter the scorecard's gate rows by strategy IN GO_LIVE_VERDICT_STRATEGIES (duplicate the frozenset or import base.py), keep the all-strategy line as a labeled book-honesty footer, and compute pace from closes/week vs the 3-4 target instead of n>=1.

## policy-vs-impl-2  [CONFIRMED | live_money]

**Proof:** master.py:1307-1311 computes lifetime AVG(ABS(price - live_mid)) — a dollar mean — and prints it directly against the percent gate: 'avg |fill-mid| ${slip:.2f} (gate: median <=8% of credit)'; its own comment (1298-1300) claims this IS the PLAN.md:498 metric (median <=8% of credit, trailing 20, no worsening trend). Probe: master prints $0.2040; the policy metric (shadow_referee.py:225-236 verbatim: median |price-mid|/entry_price*100 over trailing 20) = 2.29% over the 5 fills that carry live_mid+entry_price. $0.20 is 2.9% of a $7 credit but 29% of the config.yaml:133 $0.70 floor credit — incomparable to the gate in either direction. shadow_referee.py has no scheduler/cron entry (grep: referenced only from scripts/, PLAN.md, tests). Grep 'worsening|slip.*trend': the trend clause exists only in PLAN prose, GAP_AUDIT prose, and the master.py comment — computed nowhere.

**Consequence:** Every Friday scorecard renders a gate-1 verdict criterion as a lifetime dollar mean labeled against a percent-of-credit median gate: it can read healthy while the pinned metric fails (small credits, outliers diluted by lifetime averaging) or alarming while it passes; the mandatory no-worsening-trend check is unmeasured anywhere in the repo.

**Suggested fix:** Replicate referee check [7] in weekly_scorecard: median(|fill-mid|/entry_credit)*100 over the trailing 20 live_mid fills, printed as % against the 8% gate, plus a first-half vs second-half (or older-window) comparison for the trend clause.

## policy-vs-impl-3  [CONFIRMED | live_money]

**Proof:** PLAN.md:495-497: '>= 50 real closes for a PRELIMINARY read; **>= 100 closes before funding** (R9: at n=50 you need 42/50 wins just to confirm PF>1.0 at 90% confidence — 50 cannot settle PF>1.3)'. Every implemented surface encodes 50 as the completed denominator: status.py:227 'closes {n}/50', master.py:1333 'closes: {n}/50', and RUNBOOK.md:130-131's 'Go-live day checklist (from PLAN.md gates — do not skip)' first item: 'Paper verdict passed (>=50 closes, PF > 1.3, criteria fixed beforehand)'. Grep for any 100-close constant or funding check in code: none (only unrelated n>=100 in ml/features.py:292 and a dashboard 'need 100+ trades' label at app.py:557).

**Consequence:** At the 50th IC close both scorecards read as a completed denominator and the RUNBOOK's do-not-skip checklist item is satisfiable, authorizing real-money funding at exactly half the pinned sample — a sample PLAN itself proves cannot statistically settle the PF>1.3 gate.

**Suggested fix:** Amend RUNBOOK.md:131 to '>=100 closes before funding (50 = preliminary read only)' and render both thresholds on the computed surfaces, e.g. 'closes {n}/50 prelim, {n}/100 funding' in status.py:227 and master.py:1333.

## concurrency-4  [ENFORCEMENT_GAP | live_money]

**Proof:** All cited sites re-verified: earnings.py:75 spawns one daemon thread per cache-missed symbol writing self._cache.set() off-loop (69) while the event loop reads/sets the same TTLCache (52, 79, 92, 118); cache.py has no lock anywhere — set()'s eviction path sorts self._store.items() and bare-deletes (43-49), _evict_expired iterates and bare-deletes (62-67), and exactly one site is race-guarded with an explanatory comment (cache.py:27-28 'pop: expiry race must not KeyError'), proving the class is known and unevenly applied. A concurrent insert during the sorted(items()) iteration raises RuntimeError('dictionary changed size during iteration') and a lost del-race raises KeyError; either is swallowed by earnings.py:70-71's except in _bg, silently dropping the fetched result so the guard keeps returning next_earnings_date=None -> allow (94-95). NOT live today, confirmed independently: the cache holds one 'earnings_{symbol}' key per queried symbol, the default universe is 3 (settings.py:31 ['SPY','QQQ','AAPL']) vs max_size=200 (earnings.py:39), so the eviction branch (cache.py:43, the only unsynchronized ITERATION over the shared dict) is unreachable, and bare dict get/set on non-eviction paths are GIL-atomic. Nothing guards the invariant: tests/test_cache.py (read in full) covers only single-threaded get/set/expiry/eviction — no lock, no thread-concurrency test, and no assertion or validator ties cache capacity to universe size or forbids sharing the instance with a larger consumer.

**Consequence:** Latent only — unreachable at today's 3-symbol universe. If the universe (or any other consumer of this EarningsCalendar/TTLCache instance) grows the store to 200 keys, or max_size is lowered, concurrent _bg threads racing inside set()'s eviction iteration intermittently abort earnings cache writes; downstream is_near_earnings/would_hold_through_earnings then read 'unknown' and fail OPEN, silently allowing entries that hold short-premium positions through an earnings announcement (IV-crush exposure) with only a debug-level log.

**Suggested fix:** Add a threading.Lock to TTLCache guarding set/get/_evict_expired/invalidate (the cache is tiny; contention is irrelevant), or at minimum guard the two unguarded eviction iterations; alternatively pin the gap with a test that hammers set() from multiple threads at max_size and asserts no exception and no dropped write.

## fail-direction-04-touch-stop-except-fails-open-at-debug  [ENFORCEMENT_GAP | live_money]

**Proof:** Code facts all verified: portfolio.py:608-609 catches ANY exception from the whole 80-line touch-stop block (including the `await self._alert_frozen_feed` at :588) at log.debug only — no alert, no streak, evaluated fresh every 30s tick. Probe: a leg dict missing 'strike' raises KeyError at the :532 parse exactly as claimed. Flat credit stop confirmed disabled: runtime_env.py:85 AIT_CREDIT_LOSS_LIMIT contract default "0" (env unset in .env) -> portfolio.py:463-464 effective_stop=-999.0, so the touch stop IS the only loss exit. Asymmetry with the engineered data-missing paths (:570-599 fresh/frozen/confirm logic) confirmed. But: re-probe of the LIVE book shows both open positions (SPY, QQQ iron_condors) carry 4 legs each, all with 'strike'; the executor and reconciler (reconciler.py:105,161) both write legs WITH 'strike'; no current writer produces the malformed shape, so the branch is correct today with nothing guarding it — grep of tests/ finds zero coverage of touch_check_failed or the except path (test_r16_touch_stop.py has none).

**Consequence:** If any exception source appears in this block (a reconciler/migration row deviating from the executor shape, or a future edit — the region has incident history, R13-CRIT-1), the position's ONLY loss exit silently stops evaluating on every tick for the life of the trade, leaving just DTE/expiry exits, visible only as a DEBUG line in the rotating file.

**Suggested fix:** Raise the except to log.warning + a per-trade _alert_gate page after N consecutive touch_check_failed ticks (same pattern as the mtm brake at orchestrator.py:654-668), and add a test that feeds a malformed legs row and asserts the failure is surfaced.

## fail-direction-09-pdt-guard-fails-open-on-corrupt-state-or-empty-calendar  [ENFORCEMENT_GAP | live_money]

**Proof:** Both fail-open modes executed against the real PDTGuard class: P4 — get_state returning '{corrupt json' -> pdt_guard.py:63-64 silently resets to deque() (no log call in the except body) -> can_day_trade()=True; P5 — get_recent_trading_days patched to [] with 3 recorded day trades -> _count_in_window returns 0 (:146-148), _purge_old_trades no-ops (:78-79) -> can_day_trade()=True. Exit-side consumer confirmed at portfolio.py:757-758. However this is a gap, not a live failure: config.yaml:26 pdt_account_under_25k=false disables the guard entirely on the $198k paper account (pdt_guard.py:47), today's stored state parses fine, no current code path writes a corrupt pdt_day_trades value, and grep of tests/test_pdt_guard.py finds zero coverage of the corrupt-state or empty-calendar paths — correct today with nothing guarding the behavior that arms at the documented $2.1k go-live.

**Consequence:** Latent until go-live under $25k: at that point one corrupted bot_state row or one market-calendar helper failure silently converts the guard to always-allow, and the 4th day trade triggers an irreversible 90-day freeze of the real account — the one gate whose violation cannot be unwound.

**Suggested fix:** In _load_state, log the corrupt payload at WARNING and fail CLOSED while enabled (can_day_trade False until state is repaired); in _count_in_window/_purge_old_trades, treat an empty trading-day calendar as an error (assume all recorded trades in-window) rather than zero; add tests for both paths.

## fail-direction-12-executor-market-hours-guard-fails-open-silently  [ENFORCEMENT_GAP | live_money]

**Proof:** Code facts verified and probed: executor.py:319-326 wraps the whole check in try/except with a bare `pass` and no log (probe confirms an exception from is_market_open falls straight through to order construction), and time.py:61 does call a third-party `_nyse.schedule(...)` inside the guard. But the claim's failing scenario is not constructible with real inputs today: is_market_open raising requires pandas_market_calendars version drift or calendar-data corruption that nothing in the current environment produces (the pinned deployment works), AND reaching execute_signal off-hours requires a second, independent scheduler bug — while a calendar broken enough to raise here would typically break the phase scheduler on the same module first. execute_signal has exactly one caller (orchestrator.py:2073) inside the phase-gated loop. No test covers the except path (grep of tests/ for order_refused_market_closed: zero hits). So: correct today, silently unguarded against its own failure — the fail direction ('never let the guard itself block') is inverted for a self-described last line of defense, but no concrete present-day failure exists.

**Consequence:** Only under a double failure (calendar raising inside the executor while a scheduler bug delivers an off-hours signal): a DAY limit combo order would be submitted outside RTH with no record the guard was bypassed, resting until the open and filling against a stale anchor.

**Suggested fix:** Log the swallowed exception at ERROR (market_hours_guard_failed) and fail CLOSED — refuse the order when the guard cannot evaluate; add a test asserting an is_market_open exception blocks submission.

## model-vs-reality-ex-div-assignment-void  [ENFORCEMENT_GAP | live_money]

**Proof:** The void is fully verified; no current failing state exists (no ITM short call is straddling an ex-div date today), and no guard, test, or validator covers the event when it comes — the definition of an enforcement gap. Greps over src/ re-executed: 'dividend|ex_div|ex-div|assignment|exercise' shows only yfinance metadata columns (equity_stats.py:38-39, duckdb_analytics.py), the data-adjustment comment (market_data.py:4-6), and earnings.py:141 which mentions 'Ex-Dividend Date' solely to EXCLUDE it from earnings dates; 'extrinsic|time_value' → zero hits in src. portfolio.py:627-653: the only assignment-risk exit guards dte <= 1 (line 630-631). reconciler.py: the HALT_UNTRACKED freeze file is written only in the OPT branch (:1047-1053); the STK branch (:1061-1068) — the actual post-assignment signature, 100 shares/contract appearing — only appends 'needs manual liquidation/review' + log.critical, does not freeze entries, does not flatten, and nothing anywhere manages a short-stock position (orchestrator.py:452-455 merely forwards the text to Telegram). pricing.py:23-51: black_scholes_price(S,K,t,r,sigma) has no dividend yield q; engine.py has no assignment concept (grep: comments only, :2380/:2388). Prior art agrees it is unbuilt: GAP_AUDIT_R7.md:320 ('block short-call entries straddling an ex-div date... nothing prevents it') and :512-513 list the ex-div scan as SOON/must-exist-before-funding — still absent. Timing check: SPY/QQQ/IWM go ex-div quarterly ~3rd Friday of Sep (Fri 2026-09-18), inside the hold window of 14-30 DTE entries taken from early September.

**Consequence:** First armed window: entries from ~2026-09-04 held across 2026-09-18. A short call left ITM into the last cum-dividend evening (reachable via the documented touch-exit failure windows: R16/R19 staleness-gate delays, the 2026-07-10 dead-bot day, unfilled exit re-placement at orchestrator.py:2772) with extrinsic below the ~$1.80/share dividend gets assigned: ~$77k of short SPY stock plus a $180/contract dividend liability appears overnight in a $2.1k-scale account, the bot does not freeze entries or flatten it, and the backtest side prices this risk as exactly zero, so it can never appear in any research number.

**Suggested fix:** Three small pieces: (1) extend the reconciler STK branch to write HALT_UNTRACKED exactly like the OPT branch; (2) a daily pre-ex-div check (one yfinance/IBKR dividends call) that closes or rolls any short call whose extrinsic < expected dividend the day before ex-div, at any DTE; (3) block new short-call-bearing entries whose expiry straddles a known ex-div date within the hold horizon.

## model-vs-reality-margin-model-unimplemented  [ENFORCEMENT_GAP | live_money]

**Proof:** All code facts confirmed: account.py:254-264 can_afford gates on snapshot.buying_power (IBKR's ~4x leveraged figure, :234) while available_funds is fetched into the snapshot (:235) and never used by any gate; manager.py:500-507 (gate 6) passes request.max_loss as estimated_cost for credit trades. Greps re-executed: 'whatIf' → zero matches in any .py (only docs/GAP_AUDIT_R7.md and BENCHMARKS_R9.md); 'errorEvent|errorCode|Error 201|201' → zero matches in src/; tests only mock can_afford (test_risk_guardrails.py:31, test_r16_risk_fixes.py:301) — no test pins its semantics, so nothing would catch the wrong comparison target. GAP_AUDIT_R7.md:488-489 documented this exact shape and :510/:514 list whatIfOrder + reject-reason capture as SOON/must-exist-before-funding — both still absent 6+ weeks later. Why ENFORCEMENT_GAP rather than CONFIRMED: the finder's concrete trigger ('3rd IC passes can_afford, IBKR rejects 201') does not survive the current gate stack — gates 6b/6b-2 (manager.py:509-537) cap per-trade max_loss at risk.max_position_risk_pct*NLV and aggregate defined-risk at max_portfolio_risk_pct*NLV (~20%), and for a defined-risk book IBKR RegT spread margin ≈ max_loss, so the book's real margin is bounded at ~20-25% of NLV, far below AvailableFunds ≈ NLV at an all-cash launch. I could not construct a defined-risk sequence under today's config where IBKR margin-rejects an entry that can_afford passed. The gap is armed, not firing: it fires when the caps are lifted (GAP_AUDIT_R7:343 — the go-live recipe bumps max_position_risk_pct to 0.07 and the audit's own top-addition argues for sizing to the caps), when AvailableFunds is consumed off-model (assignment, cash withdrawal, fees), or for undefined-risk structures (short_strangle/jade_lizard) whose IBKR margin can exceed the bot's stress-based max_loss estimate — and when it fires there is no whatIf preview, no 201 classification, and no test to make it visible: the reject books as a generic cancelled entry.

**Consequence:** At launch NLV with the go-live cap bump (or any off-model AvailableFunds drain such as the assignment scenario filed separately), entries whose real initial margin exceeds AvailableFunds pass the only capacity gate; IBKR's Error 201 rejection is indistinguishable from routine cancel/timeout churn, so the bot silently retries the same class of entry or, if filled, runs with ExcessLiquidity near zero and no model of it — broker auto-liquidation territory with no distinct alarm.

**Suggested fix:** (1) One-line gate fix: compare estimated_cost against min(available_funds, buying_power) — AvailableFunds is already in the snapshot; (2) ib.whatIfOrder on credit BAG entries with a post-trade ExcessLiquidity floor (GAP_AUDIT_R7:510 spec); (3) subscribe ib.errorEvent and record code 201/110/321/200 distinctly on the trade row (GAP_AUDIT_R7:514) plus a test pinning can_afford to AvailableFunds.

## policy-vs-impl-4  [ENFORCEMENT_GAP | live_money]

**Proof:** orchestrator.py:170-173: _vol_env = int(environ.get('AIT_LIQ_MIN_VOLUME','10')); assertion condition is 'if _spread_env > 0.15 or _vol_env < 1 or _spread_cfg > 0.15' — any value 1-9 passes while violating RUNBOOK.md:133-135's 'AIT_LIQ_MIN_VOLUME >= 10 ... (startup asserts this in live mode and will refuse to run otherwise)'. The spread halves ARE asserted (>0.15 both env and config); the volume half of the documented enforcement is not. No guard exists: grep of tests/ for the GOV-5 assertion or a volume-floor refusal finds only _resolve_liquidity resolution tests (test_r16_tail_fixes.py:443-486), none asserting startup refusal for vol 1-9. Correct today: default is '10', mode is paper, so nothing fails now — the classic guarded-nowhere gap.

**Consequence:** Live-mode startup with AIT_LIQ_MIN_VOLUME set anywhere in 1-9 (e.g. relaxed to 1 during a thin-tape week and forgotten) starts cleanly; the operator believes the documented floor of 10 is asserted while real-money options screening runs a volume floor up to 10x looser than the live profile, exactly the illiquid-chain bad-fill exposure GOV-5 exists to prevent.

**Suggested fix:** Change the condition to '_vol_env < 10' to match the RUNBOOK (or amend RUNBOOK to state the actual asserted floor), and add a test that live-mode construction with AIT_LIQ_MIN_VOLUME in 1-9 raises the GOV-5 RuntimeError.

## policy-vs-impl-7  [ENFORCEMENT_GAP | live_money]

**Proof:** Three contradictory documented floors verified: config.yaml:173 'Re-enable after 50+ clean trades'; PLAN.md:531 '| Meta-labeler | untrained, default-off | 100+ closes |'; GAP_AUDIT_R7.md:185 'Meta-labeler activation below ~100 closes — 30-trade minimum is already generously low'. Code enforces only 30: meta_label.py:76 MIN_TRADES_FOR_TRAINING=30, checked at :187; the documented regeneration path (:55-56) instructs confirming >= 30. orchestrator.py:133-135 instantiates the gate on settings.meta_label.enabled alone — no close-count check at flip time; grep finds no code comparing close counts to 50 or 100. Nuance the finder omitted: the R16 arm guard (meta_label.py:209-228) currently refuses training below 15 informative features (build_training_data supplies 9/20), so arming is blocked today for a DIFFERENT reason — but that guard checks features, not counts, and neither documented close floor exists anywhere in code. Correct today (enabled=false, gate additionally requires non-paper mode at orchestrator.py:1240): a gap, not a live failure.

**Consequence:** At the documented re-enable moment, an operator following config.yaml's own comment flips enabled=true at 50 closes (or arms at 30 per the regeneration path) while current policy (PLAN) requires 100+; once the feature-coverage work is done, the code trains at 30 and gates live entries with an under-sampled model, with no guard encoding either documented floor.

**Suggested fix:** Pick one number — PLAN's 100+ is the current-policy document — set MIN_TRADES_FOR_TRAINING (or an enable-time real-close count check in the orchestrator) to it, and fix the config.yaml:173 comment to match.

## model-vs-reality-commission-constant  [CONFIRMED | research_validity]

**Proof:** Re-read all cited sites and re-executed the ledger probe independently. Code: src/ait/backtesting/engine.py:86 (commission_per_contract=0.65 default), :220, :950 (entry: self._commission*contracts*n_legs), :2473-2476 (exit: same formula in _calc_pnl); src/ait/backtesting/walkforward.py:57 pins 0.65 and passes it into every window at :1408 and :2522; grep of scripts/calibrate_option_spreads.py for 'commission|0.65' returns zero matches, and grep of src/ait/backtesting for 'commission' shows no recalibration path anywhere. My probe on data/ait_state.db executions (excluding the 18 BAG summary rows con_id=28812380, which carry commission=0): 78 leg fills, per contract-leg commission min 0.6195 / median 1.0284 / mean 0.9168 / max 1.0586; modal buckets 1.02-1.06 with a 0.62 rebate cluster; total actual 71.51 vs flat-0.65 model 50.70 = ratio 1.410 — exactly the claimed ~41% understatement (~$5.20 modeled vs ~$7.33 actual per 8-contract-leg-side condor round trip, $2.13/contract). Live is trued-up to the real ledger: orchestrator.py:479-509 swaps TradeExecutor.commission_estimate (executor.py:2295-2308, also 0.65-flat) out and state.total_commission in before stats/breaker/Thompson see realized_pnl, and executor.py:1304-1308 calls the ledger 'the ground truth the PF verdict needs'. So live P&L carries ~0.92/leg while every backtest/walkforward/Optuna result carries 0.65, and nothing feeds the measured value back.

**Consequence:** Every engine-priced condor is ~$2.13/contract cheaper in research than at the broker; over the current 23 closed live trades (net P&L -$121.46) the same friction gap is ~$49 — a full-size bias on a book whose per-trade gross targets are $20-45. Every OOS window and the research side of the live-vs-backtest comparison are structurally optimistic by ~5-10% of expectancy on exactly the 4-leg low-credit class most sensitive to per-leg friction.

**Suggested fix:** Set the research default (engine.py:86, walkforward.py:57) from the executions ledger: a small startup query (median all-in per contract-leg, currently ~1.03, or mean 0.92) with 0.65 as fallback when the ledger is empty; alternatively add commission to the calibrate_option_spreads.py pipeline which already reads the same database.

## model-vs-reality-skew-10x-flat  [CONFIRMED | research_validity]

**Proof:** Code re-verified: engine.py:1310-1324 hardcodes put slope 0.10 and call slope 0.02 per unit |log-moneyness| (skew_factor default 1.0 at :119); _load_calibrated_spreads (:1402-1427) adopts only spread_base/iv_sensitivity/dte_sensitivity, and the option_spread_params table schema (probed: symbol, calibrated_on, spread_base, spread_iv_sensitivity, spread_iv_threshold, spread_dte_sensitivity, spread_dte_threshold, spread_cap, sample_count, rmse) has no skew field — the 0.10/0.02 slopes are uncalibratable by the existing pipeline. My independent regression on the repo's own 3159 IBKR samples in data/historical.db option_spread_samples (2026-08-11, 0.01<iv<0.9, within 20% moneyness, spot from daily_prices): OTM-put dIV/d|lnK| = 1.141 SPY dte20 (n=140), 0.983 QQQ dte20 (n=81), 0.916 IWM dte24 (n=57) — 9-11x the hardcoded 0.10; call side 0.064/0.112/-0.089 vs 0.02. Leg-level probe at independently chosen delta-0.20 strikes (repo's own find_strike_by_delta + engine leg-IV formula, SPY S=770.6 atm_iv 0.1289 dte 20): long put wing engine $0.08 vs real chain mid $0.71 (9x under), short call engine $2.57 vs real $1.48 (1.7x over), short put $2.82 vs $3.17; net credit engine 5.235 vs real-chain 3.850. One finder sub-claim materially corrected: the claim that the NET credit error 'mostly cancels (7% SPY / 1% QQQ)' is too generous — at delta-selected strikes with the sampled ATM IV my probe shows the net credit overstated 36% SPY / 22% QQQ ($139/$132 per contract), i.e. the cancellation is an artifact of the finder's particular strike/IV choices, and the divergence can hit the headline credit too, not only per-side quantities.

**Consequence:** Every credit-structure build and reprice in every study runs on a surface ~10x flatter than the measured one: wing costs are near-free in the model (long put $0.08 vs $0.92-0.71 real) so wide/broken-wing arms and wing_k optimization systematically prefer wider wings than reality prices, put-vs-call touch loss attribution is wrong-signed in magnitude, and — per my probe — entry credits themselves can be overstated by 20-36%, compounding the commission and touch-booking optimism in the same direction across every OOS window.

**Suggested fix:** Extend calibrate_option_spreads.py to fit put/call slope-in-log-moneyness from option_spread_samples (the regression is ~10 lines against data already collected), add two columns to option_spread_params, and have _load_calibrated_spreads adopt them into _get_leg_iv the same way spread params are adopted today.

## model-vs-reality-touch-gap-pricing  [CONFIRMED | research_validity]

**Proof:** Code re-verified: engine.py:2327-2343 touch detection reads only row['Low']/row['High'] (grep confirms row['Open'] is used nowhere in engine.py except the required-columns check at :1030), and engine.py:2258-2262 reprices the buyback at exactly the touched strike (_tu = touch_underlying = the strike from :2337/:2339). Live: orchestrator.py:341-354 runs _trading_loop only in MARKET_OPEN (OFF_HOURS just waits), the 30s monitor _monitor_positions_fast is called only at orchestrator.py:784 inside that loop, and OrderBuilder.stop (orders.py:55-57) has zero callers in src (grep: all '.stop(' hits are lifecycle stops); portfolio.py:832+ documents even the resting GTC take-profit as deliberately unimplemented. My independent reprice probe (repo's own pricing.py + engine's leg-IV/half-spread/IV-blend formulas; S=640 SPY-like condor 620/666 shorts, 576/710 wings, entry iv 0.18, dte 21, held 7): engine books the touch buyback at 7.75/share AT the strike, while repricing at a session open gapped 1%/2%/3.5% below the short put gives 10.99/14.95/21.79 — loss understated by $324/$720/$1405 per contract. Worse than claimed on the IV side: the exit-IV blend (engine.py:2064-2075, 0.70*entry+0.30*rv20*1.15) yields 0.165 on a crash day with a fresh -4% move (rv20=0.114) — BELOW the 0.18 entry IV — while a realistic +6pt spike at the strike prices the buyback at 11.66 vs the engine's 7.75 ($392/ct further understated). Gap frequency from data/historical.db daily_prices reproduced exactly: |overnight gap|>1% on 9.6% of SPY days (n=604, worst 3.99% 2024-08-05), 18.1% QQQ (worst 5.36%), 17.8% IWM (worst 5.51% 2024-11-06). Live corroboration from ait_state.db trades: the sole live touch exit (T-20260817-141950-3d7ded QQQ iron_condor, 'short_strike_touch (spot 703.86 <= put 704.00)', 2026-08-24) lost $272.29 = 0.43x credit (entry 6.12, buyback 8.76) on a bare touch, where the engine books near-scratch. One finder sub-claim corrected: a resting broker-side stop would not fix gap-throughs (a stop triggers at the open too, and options do not trade overnight), so the missing-stop-order observation, while code-true, is not itself the divergence — the structural defect is entirely the engine's at-the-strike booking.

**Consequence:** Any backtest day where the open gaps beyond a short strike (the standard condor loss path after price drifts near the strike; 1%+ gaps occur on 10-18% of days in the exact 604-day sample every study runs on) is booked as a near-scratch 'touch_stop' at the strike instead of the gapped-open loss — a $300-1400/contract per-event optimism that flows into every OOS window, the wing_k/touch-policy promotions, and the research side of the go-live comparison; live meanwhile eats the real gap.

**Suggested fix:** In _check_exit_credit, detect gap-through with row['Open'] (Open <= short_put or Open >= short_call) and pass touch_underlying = Open for those days, keeping the strike only for intraday touches; on touch days floor the exit IV at entry IV plus a calibrated crash bump instead of the rv20 blend that can lower IV on the crash day itself.

## dead-surface-1  [CONFIRMED | ops_reporting]

**Proof:** Re-executed every probe. venv/Scripts/python.exe importlib sweep -> missing exactly ['pywt','optuna','apscheduler','flask']; pyproject.toml:39,42,44,51 declares pywavelets>=1.4, optuna>=3.6, apscheduler>=3.10, flask>=3.0. `venv python -c "sys.path.insert(0,'src'); import ait.bot.orchestrator"` -> ModuleNotFoundError at src/ait/ml/features.py:19 `import pywt`. `venv python -m pytest tests/ --collect-only -q` -> 'Interrupted: 27 errors during collection' (805/829 collected). ait.backtesting.walkforward, ait.ml.features, ait.optimization.optimizer (optuna), ait.orchestration.master (apscheduler:26-27), status_server.py and web_logs.py (flask) all fail to import under the venv; 14 scripts/ modules import the pywt-dependent chain (features/ensemble/walkforward). keeper_ait.bat:14 pins PY=C:\Users\prith\AppData\Local\Programs\Python\Python313\python.exe and that interpreter imports all four (pywt 1.8.0), so the live bot is unaffected. README.md:136-137 and :572 document venv activation as THE setup path; no doc directs anyone to the system interpreter. venv has ait 2.0.0 installed editable (pip show), so the venv predates the dep additions and was never re-synced. Two finder overclaims corrected: ait.backtesting.engine imports CLEAN under the venv (only walkforward/features fail), and run_optimizer.py imports clean because its optuna import is lazy (line 94, inside main) — it fails at run time, not import. Neither changes the verdict: the documented environment cannot import the orchestrator, walkforward backtesting, the master scheduler, either flask surface, or collect 27 of the test files, today, reproducibly.

**Consequence:** Anyone following README setup (activate venv, run pytest / master.py / status_server.py / a study script) hits ModuleNotFoundError today: pytest aborts with 27 collection errors (or silently drops 27 files under --continue-on-collection-errors), and the orchestrator/walkforward/optimizer-run/scheduler/web surfaces are dead in the documented environment. The live bot survives only via keeper_ait.bat's hardcoded system-interpreter pin.

**Suggested fix:** Run `venv\Scripts\pip install -e .` (or explicitly `pip install pywavelets optuna apscheduler flask`) to re-sync the venv with pyproject.toml; add a smoke check (CI or scripts/typecheck.py-style gate) that imports ait.bot.orchestrator + ait.orchestration.master under the venv; state in README that the keeper runs the system interpreter and keep constraints.txt as the venv lockfile.

## dead-surface-2  [CONFIRMED | ops_reporting]

**Proof:** The contradiction is real and the damage has ALREADY occurred — the finder underclaimed by framing it as future-only. Code: restate_r16_executions.py:83-87 zeroes live_mid/signal_price/nbbo_spread on EVERY row with con_id != BAG_CON_ID (28812380, executor.py:54) carrying context, with no date cutoff and no strategy/combo discrimination; the current writer (executor.py:1355-1362, docstring :1325 'single-leg orders: unchanged — price and mid are the same unit') legitimately writes that context onto single-leg rows via the strategy-agnostic placement capture at executor.py:424-428 (_order_ctx_map). Backup diff proves realized destruction: data/backups/ait_state.20260810.db (pre-apply) holds valid single-leg context on the three closed long_call entry fills — T-20260716-124033 con_id 891780172 price 2.60 live_mid 2.66 nbbo 0.04; T-20260716-144706 con_id 891780138 price 2.65 live_mid 2.61; T-20260717-103049 con_id 891780138 price 2.76 live_mid 3.05 — price and mid in the same unit, i.e. valid slippage samples (~2.3%, ~1.5%, ~9.5%). ait_state.20260811.db and the live DB show all three zeroed, and bot_state['r16_executions_restatement'] (updated_at 2026-08-10T20:05) records 'cleared combo context on 15 leg rows' — those 15 included the 3 valid single-leg rows. Guard check: grep of tests/ finds no test of restate_r16_executions.py (only restate_r16_dailystats.py is tested); the script remains re-runnable and currently reports 'nothing to restate' only because the ledger again holds 0 non-BAG rows with context (verified by direct SQL). Single-leg strategies are config-disabled today (config.yaml:91-107, iron_condor only), so recurrence requires a re-enable plus a re-run — but the past apply already destroyed real data.

**Consequence:** The 2026-08-10 --apply run permanently zeroed the only fill-quality ground truth (live_mid/signal_price/nbbo_spread) for the three real long_call fills, while stamping provenance claiming only legacy poison was healed; any future single-leg re-enable followed by a re-run of --apply will silently repeat the destruction against the slippage gate's inputs. Not live_money: bot runtime trades/exits/gating are untouched and the IC-only go-live verdict sample (post 2026-07-22 per config.yaml:83-90) excludes these mid-July single-leg fills.

**Suggested fix:** Restrict the WHERE to the legacy poison it was written for — add `AND exec_time < '2026-08-10'` (writer-fix date) or join to trades and match only combo strategies; restore the 3 zeroed rows from data/backups/ait_state.20260810.db; add a regression test asserting the script never touches a non-BAG row belonging to a single-leg strategy.

## dead-surface-3  [CONFIRMED | ops_reporting]

**Proof:** Independently re-derived and probed. Production DB: SELECT direction_correct, COUNT(*) FROM trades GROUP BY 1 -> [(-1, 28)] with 23 status='closed' — every row sits at the DDL default (state.py:134 'direction_correct INTEGER DEFAULT -1'). close_trade's UPDATE (state.py:386-394) sets status/exit_time/exit_price/realized_pnl/commission/exit_reason_detailed/peak_pnl_pct/time_to_peak_hours and NOT direction_correct, despite record_trade's docstring (state.py:259-261) promising it is 'populated on close'. Repo-wide grep for direction_correct: only the DDL, the INSERT OR IGNORE default, the docstring, the DuckDB mirror DDL/ingest (duckdb_analytics.py:152,240,263 — reads sqlite, writes nothing back), dashboard readers, synthetic test fixtures, and learning/analyzer.py:497+ where `direction_correct` is a LOCAL COUNTER variable, not a column write. Orchestrator :573-588 computes actual_dir solely as an argument to self._trainer.drift_detector.record_outcome — in-memory, never persisted. Dashboard app.py:969 `known = exit_data[exit_data['direction_correct'].isin([0,1])]` is therefore empty on every render -> st.info('No direction accuracy data yet (needs trade context)') at :986, forever. Corroboration: docs/BENCHMARKS_R9.md:72-73,117-118 documented this exact write-never gap months ago ('-1 on all 116 rows') with a fix that was never implemented — the current 28-row post-purge DB is still 100% -1. Distinct from db-contracts-1 as claimed (different column, different consumer).

**Consequence:** The dashboard Trade Intelligence 'ML Direction Accuracy' panel — Direction Correct, Direction Accuracy, and Right-Direction-Lost-$ (the metrics built to distinguish an exit-management bottleneck from a model bottleneck) — can never populate no matter how many trades close; the operator permanently sees 'No direction accuracy data yet' over 23 closed trades that were supposed to feed it.

**Suggested fix:** Write direction_correct in the close path: in close_trade (or the orchestrator exit-fill handler which already computes direction inference at :573-588), set 1/0 by comparing entry direction against the underlying's entry->exit move for directional strategies and leave -1 for market-neutral ones (iron_condor/short_strangle/long_straddle); add it to the UPDATE column list at state.py:386-394 and backfill closed directional trades from historical.db.

## dead-surface-4  [CONFIRMED | ops_reporting]

**Proof:** Re-executed the panel's exact pipeline (query at app.py:910-916 including _REAL_CLOSE_SQL from :28-32, then the apply at :937-940) against data/ait_state.db: 17 exit_data rows, 14 with peak_pnl_pct > 0, and per-trade capture_pct values match the finder to the decimal — NVDA iron_condor realized -$127.69 / peak 0.0078 -> -16376.1%; QQQ iron_condor -$272.29 / 0.0402 -> -6779.9%; IWM long_straddle -$575.33 / 0.1924 -> -2990.5%; QQQ short_strangle $105.40 / 0.1634 -> 645.2%; IWM short_strangle $126.40 / 0.5795 -> 218.1%; mean -1652.7% rendered by c2.metric('Avg Capture Efficiency', ...) at :945. Unit mismatch proven at both ends: peak_pnl_pct is a FRACTION of position cost basis — portfolio.py:417-418 `pnl_pct = unrealized_pnl / cost_basis` feeds update_high_water_mark (:431-434), close_trade persists it as peak_pnl_pct (state.py:373), and app.py:944 itself formats the very same column as a fraction (`f'{avg_peak:.1%}'` -> 27.9%) — while realized_pnl is absolute dollars. So capture_pct = dollars / (fraction*100) * 100 is dimensionally meaningless: doubling position size doubles 'efficiency'. The grouped bar chart (:947-960) plots peak_pnl_pct*100 (percent) and realized_pnl (dollars) on one unlabeled y-axis, as claimed.

**Consequence:** On every render of the Trade Intelligence tab the operator reads 'Avg Capture Efficiency -1653%' — a number whose sign and magnitude are artifacts of dollar position size divided by a percentage, not of exit quality; a $127 loss on a tiny-peak condor dominates the average at -16376% while a genuinely well-captured trade reads 218%, making the profit-giveback panel actively misleading for exit-tuning decisions.

**Suggested fix:** Put both terms in the same basis: compute realized_pnl_pct = realized_pnl / (entry cost basis, i.e. abs(entry_price)*quantity*multiplier, matching portfolio.py:417) and set capture_pct = realized_pnl_pct / peak_pnl_pct * 100 (or persist realized_pnl_pct at close alongside peak_pnl_pct); give the bar chart a secondary y-axis or plot both series as percentages.

## external-contracts-03-telegram-429-retry-after-ignored  [CONFIRMED | ops_reporting]

**Proof:** All cited sites re-read and behavior executed. telegram.py:57-65: any non-200 -> log warning with body, return False; repo-wide grep confirms 'retry_after' appears NOWHERE in src/ — Telegram's documented throttle contract (HTTP 429, JSON parameters.retry_after seconds) is never parsed. Executed probe with a stubbed transport returning the exact Telegram 429 body ({'error_code':429,'description':'Too Many Requests: retry after 31','parameters':{'retry_after':31}}): send() returned False in 0.000s, body read only for the log line. orchestrator.py:3458-3470: the wrapper retries at sleeps of 2s and 4s — all three attempts complete within ~6s, entirely inside any retry_after >= ~7s window (Telegram routinely issues 5-60s) — then logs notification_dropped_after_retries and increments _tg_fail_streak; :3471-3476 writes data/TELEGRAM_DEAD at streak 5, falsely declaring a merely-throttled channel dead; master.py:1529-1546 clears it only via an hourly-gated probe. master.py:89-105: supervisor _alert is a single urllib urlopen — HTTPError 429 raises into the bare except -> False, zero retries, so one throttle loses a supervisor page outright; daily_digest (:1201-1209) writes its sent-marker only on a confirmed send, deferring a throttled digest to the next boot's catch-up. TELEGRAM_DEAD is read only by the supervisor probe — it gates no trading.

**Consequence:** During exactly the alert bursts an incident produces (startup-reconcile discrepancies + circuit-breaker + watchdog pages, each _alert_gate key throttling independently), Telegram 429s with retry_after > ~6s convert throttling into permanent message loss: the operator's pages vanish in the window they matter most, and 5 consecutive losses false-mark the channel dead for up to an hour. No trade, exit, sizing, or gating logic consumes these sends, so the damage is confined to operator alerting and digest delivery.

**Suggested fix:** In TelegramNotifier.send, branch on resp.status==429: parse parameters.retry_after from the JSON body and either sleep-and-retry once (capped, e.g. min(retry_after, 30)) or return the value so the orchestrator wrapper waits retry_after instead of 2/4s; do not count 429s toward _tg_fail_streak. In master._alert, catch urllib.error.HTTPError separately and retry once after the Retry-After header/body value.

## fail-direction-01-vix-fail-closed-silent-full-stop  [CONFIRMED | ops_reporting]

**Proof:** Re-read confirms every cited fact. orchestrator.py:1680-1696 `_get_vix_lkg`: fresh failure -> LKG only if <=2700s old, else `log.error("vix_unavailable_fail_closed"); return None` — log.error is the sole output. manager.py:388-392 then rejects every credit entry; the rejection surfaces only as `log.info("trade_rejected")` + a counterfactual row (orchestrator.py:1999-2017). Grep of the whole orchestrator for 'vix' shows no _send_notification/_alert_gate anywhere on this path; the _alert_gate key list (13 keys) has none for VIX. config.yaml:82-90 `strategies: [iron_condor]` and base.py:45-47 (iron_condor in LIVE_CREDIT_STRATEGIES) make this 100% of entries. Both VIX sources have real failure paths (market_data.py:343 vix_ibkr_failed, :360 vix_fetch_failed; yfinance outages are routine), so a >45-min dual outage is a concrete recurring scenario. The sibling blackout gate got exactly the missing treatment in R18 (orchestrator.py:1847-1862: streak counter + page, with a comment whose argument applies verbatim here). Only passive trace: master.py daily_digest's twice-daily 'today N entries' line.

**Consequence:** Any VIX outage lasting past the 45-min LKG window silently blocks 100% of entries (iron_condor-only book) for its full duration; the operator learns only by noticing zero trades — the same silent-full-stop signature as the 08-11 incident the blackout gate was hardened against. No trades are misplaced, so the harm is a silent trading/sample-collection stop, not direct loss.

**Suggested fix:** Mirror the R18 blackout pattern at the VIX gate: reset a `_vix_fail_streak` on any healthy return in _get_vix_lkg, increment it on the fail-closed return, and after >=3 consecutive failures page via `_alert_gate("vix_gate_broken", 3600)` + _send_notification stating entries are being refused fail-closed.

## log-contracts-1  [CONFIRMED | ops_reporting]

**Proof:** Re-executed everything. Producers: venv ib_insync wrapper.py:288 logs 'updatePortfolio: ...' at INFO, ib.py:1803 'Unknown contract:' at WARNING, wrapper.py error() routes code 10167 to _logger.info — all pass the R6 pin logging.getLogger('ib_insync').setLevel(INFO) at src/ait/utils/logging.py:71 into the DEBUG file handler (lines 56-63, 20MB/10 backups per config.yaml:231-232). Census: ait.log.1 = 80,302 lines, only 1,077 JSON (19,174 'Unknown contract' + 18,140 'Warning 10167'); four rotations TODAY at mtimes 10:36/12:19/14:00/17:30. Today's events live only in rotated files (ml_prediction 27+27+28+30, signals_generated 56+54+56+54, ibkr_connected 2 in .4); current ait.log = 0 for every counted event (593 lines, 587 updatePortfolio noise). status.py reads ONLY logs/ait.log with a 2MB byte cap (line 30), 3000-line tail for _last_connect (line 60), 60000-line for _count_event_today (line 87). Live run at 23:11 while the bot is RUNNING (2 procs, heartbeat 23:11:03) printed: 'last IBKR connect: none', TODAY ml_prediction/signals_generated/trade_executed/trade_rejected all 0, 'last activity: �'. scripts/check_first_rth_liveness.py:29 TAIL_BYTES=4MB — at the measured ~10MB/RTH-hour flood that is ~25 min of history, so its 'trading_cycle_error today' claim (lines 76-80) cannot see a morning error at the 09:40 gate.

**Consequence:** Every RTH afternoon the status CLI and status_server dashboard report zero today-activity and 'last IBKR connect: none' for a bot that predicted 112 times and generated 220 signals that same day; the post-deploy liveness gate's error window shrinks to the last ~25 minutes.

**Suggested fix:** Silence the three ib_insync noise sources (level pin or logging.Filter dropping updatePortfolio/Warning 10167/Unknown contract), and make status.py/check_first_rth iterate ait.log plus same-day rotated backups instead of a byte-capped tail of the current file.

## log-contracts-2  [CONFIRMED | ops_reporting]

**Proof:** orchestrator.py:1666-1669: the string 'LOOP IMPAIRED — {kind}: {n} consecutive failures' exists ONLY inside the f-string passed to _send_notification; repo-wide grep shows no log.* call carries it (only src/ait/bot/orchestrator.py:1667, the liveness script, and pycache). Telegram success path (telegram.py:62) logs log.debug('telegram_sent', chars=len(message)) — no body; the string reaches ait.log only on FAILURE branches (telegram_disabled message[:100], notification_dropped_after_retries preview=msg[:80] at orchestrator.py ~3470), so a healthy Telegram channel guarantees zero matches for check_first_rth_liveness.py:75's re.findall(r'LOOP IMPAIRED'). The R8 fast-monitor death class logs log.warning('fast_monitor_error', ...) at orchestrator.py:699; grep of src/ait confirms the only 'trading_cycle_error' producer is orchestrator.py:795 (main cycle), and the script checks only that event plus the never-logged string — 'fast_monitor_error' is not checked anywhere in it. tests/ grep: zero references to 'LOOP IMPAIRED'.

**Consequence:** A recurrence of the R8 incident (fast monitor throwing every 30s, stops/TPs dead) with Telegram healthy yields hundreds of fast_monitor_error events in ait.log and the 09:40 deploy gate still prints '[ok] no_loop_impairment: LOOP IMPAIRED x0, trading_cycle_error x0 today' and LIVENESS PASS.

**Suggested fix:** In _note_loop_error, emit log.error('loop_impaired', kind=kind, n=n) before notifying, and have the liveness check grep that structured event plus count fast_monitor_error events.

## log-contracts-3  [CONFIRMED | ops_reporting]

**Proof:** src/ait/main.py:43-48: _fatal_log = open('logs/fatal.log','a'); faulthandler.enable(file=_fatal_log) — the R5-F4 comment on the same lines states dumps were deliberately moved OFF stderr/bot_stdout.log; stderr fallback only if logs/ is unwritable. No faulthandler in run_orchestrator.py or src/ait/orchestration/master.py (grep), so no process writes the 'Windows fatal exception' header to bot_stdout.log anymore. status.py:67-69 still counts that string in bot_stdout.log. Executed: grep -c across bot_stdout.log/.1/.2 = 0/0/0; logs/fatal.log exists (0 bytes, the always-open sink created Jul 9). Live status output: 'native crashes: 0' beside 'keeper relaunches: 8'. tests/ grep: no reference to 'Windows fatal exception' or fatal.log.

**Consequence:** The next c0000005 access-violation crash writes its traceback to fatal.log while the status CLI and web dashboard keep reporting 'native crashes: 0', hiding the crash-frequency signal the keeper exists to mitigate.

**Suggested fix:** Point status.py's _crash_count at logs/fatal.log (counting 'Windows fatal exception' headers there), keeping the bot_stdout.log grep as a legacy fallback.

## log-contracts-4  [CONFIRMED | ops_reporting]

**Proof:** web_logs.py:184-187 reads the entire file (f.readlines()) and keeps all_lines[-200:]; line 199 counts 'Trades Today' and line 203 tracks 'Last Signal' only within that window; JS polls /api/logs every 5s (line 94). Executed probe on production logs/bot_stdout.log: 76,134,916 bytes / 290,225 lines accumulated since the Aug 24 23:03 rotation (~one day). The last 200 lines span 2026-08-26T01:00:29Z to 03:00:29Z (2 quiet evening hours) and contain 0 ml_prediction and 0 trade_executed/order_placed lines, while the full file holds 99 ml_prediction events today. At the measured RTH write rate, 200 lines covers well under a minute, so any trade older than that window is invisible to the 'Trades Today' tile.

**Consequence:** During any RTH viewing of localhost:8502, a trade executed 20 minutes earlier is outside the 200-line window and 'Trades Today' shows 0; each browser tab also forces a full 76MB readlines() every 5 seconds.

**Suggested fix:** Seek-and-read a bounded tail (e.g. last 2MB) instead of readlines(), and compute the day counters from a time-bounded scan or an incremental counter rather than the display window.

## log-contracts-5  [CONFIRMED | ops_reporting]

**Proof:** scripts/run_integration_test.py:1191-1193 calls setup_logging(LoggingConfig(level='DEBUG', file='logs/ait.log'), ...) — cwd-relative, so a repo-root run attaches a second RotatingFileHandler (defaults max_bytes=10,485,760/backup_count=5 per settings.py:399-403) to the production file the live bot's handler (logging.py:56-63) already holds open; keeper_ait.bat relaunches run_orchestrator.py within 90s of any death (verified in the batch file) and status.py confirms the bot up now. Executed two-process probe on this box (Python 3.13, Windows): holder keeps an open RotatingFileHandler on dual.log; writer (maxBytes=2000) emits 50 x ~200-byte records — result: 9 of 50 records survived, 41 '--- Logging error ---' blocks on stderr, each 'PermissionError: [WinError 32] ... being used by another process' inside doRollover -> rotate -> os.rename, zero backup files created, writer exit code 0. So whichever process crosses its threshold first while the other holds the file silently drops every subsequent record; since production ait.log routinely sits above the test handler's 10MB default and the bot reaches its 20MB threshold every ~2 RTH hours, the collision is immediate-to-hours away on any normal run.

**Consequence:** Running scripts/run_integration_test.py on this machine while the keeper-supervised bot is up causes one side to silently discard its entire log stream after the next rollover attempt — if it is the live bot, order_placed/trade_executed/trading_cycle_error forensics vanish for the duration with only unmonitored stderr noise.

**Suggested fix:** Have run_integration_test.py log to its own file (e.g. logs/integration_<timestamp>.log), or refuse to attach to logs/ait.log when the bot heartbeat/process is live.

## log-contracts-6  [CONFIRMED | ops_reporting]

**Proof:** logging.py:103: the single structlog processor chain ends in ConsoleRenderer if sys.stdout.isatty() else JSONRenderer, and both the console and ait.log file handlers format '%(message)s' (lines 52, 62), so one render feeds both sinks. Executed probe importing the REAL setup_logging with a tty-wrapped stdout and a scratch file: file content began '\x1b[2m2026-08-26T03:14:08.376172Z\x1b[0m [\x1b[32m\x1b[1minfo...ibkr_connected...'; '"timestamp": "' not in content; check_first_rth's scan_symbol_timing regex = 0 hits (status.py's _count_event_today pattern also 0; _last_connect degrades to '?'). run_orchestrator.py:30-31's R16 comment explicitly contracts a bare `python -m ait.main` as a supported entry point, and that launch from an interactive console is a tty. Current production (master.py:404-413 Popen with stdout redirected to bot_stdout.log) writes JSON — verified in today's ait.log — so the break requires the interactive entry point; no test or runtime check pins the file format (grep).

**Consequence:** Any debugging/recovery session that starts the bot from an interactive console writes ANSI ConsoleRenderer lines into the production ait.log for the whole session — its ibkr_connected/scan_symbol_timing/trade_executed events are invisible to status.py, the liveness gate, and forensic greps, with no error anywhere.

**Suggested fix:** Use structlog.stdlib.ProcessorFormatter so the file handler always gets JSONRenderer regardless of tty (console can keep ConsoleRenderer), and add a test asserting a line written through setup_logging's file handler parses as JSON.

## multiprocess-db-1  [CONFIRMED | ops_reporting]

**Proof:** Every cited site re-read and re-derived. duckdb_analytics.py:114-117 __init__ unconditionally calls _init_schema(), which uses _get_conn() whose default is read_only=False (:119-123) — so every consumer takes a read-write open at construction, directly under the A11 comment claiming 'Readers pass read_only=True'. Consumers verified: dashboard app.py:74-80 _get_duck() constructs DuckDBAnalytics() fresh on every tab render (:232, :718) — docstring says 'cached', no cache exists; master.py:937-948 daily_report subprocess (CronTrigger mon-fri 16:30 at :1671-1673, plus boot-time catch-up at :1679-1681) constructs TradeAnalytics() -> _init_duck() -> DuckDBAnalytics() (analytics.py:110-120); smoke_deploy.py:463 and :495 do the same. Executed a two-process probe on the installed duckdb 1.5.0 (venv, scratch DBs): child RO while parent holds RW -> IOException; child RW while parent RW -> IOException; child RW while parent RO -> IOException; RO+RO -> connected. So every construction excludes all other processes for its duration, and any held RO reader excludes the bot's RW ingest. Bot-side drop sites verified: state.py:421-423 (close_trade), :536-537 (daily), :723-724 (context), :862-864 (commission) all catch Exception and only log.warning; sync_from_sqlite (duckdb_analytics.py:735) has zero production callers (grep: tests/test_duckdb_analytics.py only), so a dropped ingest is never systematically repaired. One nuance the finder overstated: a trade-row drop at close CAN be healed seconds later because orchestrator.py:556-559 calls update_trade_commission, whose re-ingest (state.py:854-861) upserts the full row — but only if commission>0 and that second open doesn't also collide; daily_stats (day's last write) and trade_context (entry-time single shot) have no second attempt, and update_trade_realized_pnl true-ups (orchestrator.py:499, :2925) never re-ingest at all. Production state: sqlite=28 trades vs duck=23; the 5 missing are exactly 3 cancelled + 2 status='filled' (open) — expected; zero duckdb_*_sync_failed / duckdb_unavailable events in 823MB of logs (bot_stdout.log* shows 7x duckdb_analytics_enabled, 14x duckdb_initialized, 0 failures). Armed, fires on any overlap; the dashboard is in active use (logs/dashboard.log written today).

**Consequence:** Operator has the Streamlit dashboard open at 16:00-16:05 while the bot closes a trade: the rerun's duck open (RW _init_schema or any RO query connection) is alive at the instant state.py:421 calls ingest_trade -> duckdb.connect raises IOException -> caught at :423 as a warning -> that closed trade never reaches the analytics mirror (permanently if its commission re-ingest also misses or commission==0). Dashboard P&L/PF panels, the 16:30 daily_*.json report, and the daily-summary Telegram 30-day stats (orchestrator.py:2999, duck-first) silently diverge from the sqlite ledger. The go-live scorecard is NOT corrupted — master.py:1251-1345 reads sqlite directly — and nothing in trade gating reads duck, hence ops_reporting, not live_money.

**Suggested fix:** Make reads genuinely read-only: give DuckDBAnalytics a read_only constructor mode that skips _init_schema when the file exists and passes read_only=True to every query connection (dashboard, TradeAnalytics, smoke). On the single writer (bot StateManager), wrap each ingest in a short retry-with-backoff, and add a per-boot or per-close sync_from_sqlite reconciliation sweep so a dropped ingest is self-healing instead of permanent.

## multiprocess-db-3  [CONFIRMED | ops_reporting]

**Proof:** All sites re-read. Docstring contract at smoke_deploy.py:35-41: 'read-only by design... never inserts/updates/deletes rows'; NoWriteGuard opens sqlite mode=ro (:167-168) and duck read_only=True (:196-197) with the comment 'DuckDB dislikes mixed ro/rw connections', and _duck_snapshot anticipates the lock case, only WARNing (:198-201). Yet check_state_manager (:462-463) — right after refusing to create a missing DB 'smoke is read-only' (:459-461) — constructs StateManager(SQLITE_PATH), whose __init__ (state.py:102-103) runs _init_db (DDL/ALTER migrations + PRAGMAs on the real ledger) AND _init_duckdb -> DuckDBAnalytics(parent/'ait_analytics.duckdb') (state.py:246-247) -> _init_schema READ-WRITE open of the production duck (duckdb_analytics.py:117, 125-126). check_analytics_duckdb (:492-502) constructs TradeAnalytics(SQLITE_PATH) -> _init_duck -> DuckDBAnalytics() default path = data/ait_analytics.duckdb, which _bootstrap's os.chdir(ROOT) (:97) makes the production file — a second RW open. Failure semantics verified in both directions against my executed duckdb 1.5.0 two-process probe (RW-while-any and RO-while-RW all raise IOException): (a) if the bot holds ANY duck connection at that instant, analytics.py:116-120 swallows the IOException, a._duck is None, and :496-499 raises RuntimeError 'primary analytics path is dead (check duckdb install...)' — a false red misdiagnosing a transient lock as a dead component (contrast the guard's own WARN-only handling of the identical condition); (b) while the smoke's RW open is alive, a concurrent bot ingest_trade fails and is dropped per finding db-1. Smoke-while-bot-up is designed usage, not hypothetical: :67-77 LIVE_WRITE_TABLES + heartbeat-freshness logic exists precisely to tolerate the live bot writing during the smoke, and the keeper (keeper_ait.bat) relaunches the bot within 90s of any deploy kill. The contract violation executes on every smoke run today; only the harmful overlap is probabilistic.

**Consequence:** Operator runs scripts/smoke_deploy.py after a deploy while the keeper-relaunched bot is booting (StateManager duck init, TradeAnalytics init at orchestrator.py:250) or ingesting a close: either the smoke prints a false 'FAIL: primary analytics path is dead' verdict for a healthy deploy, or — the reverse interleaving — the bot's close-time duck ingest hits the smoke's exclusive open and that trade silently never reaches the analytics mirror.

**Suggested fix:** Have both checks honor the smoke's own contract: check_analytics_duckdb should open the duck store via a read-only factory (read_only=True, skip _init_schema when the file exists) and treat an IOException lock as WARN like NoWriteGuard._duck_snapshot does; check_state_manager should point StateManager at a temp copy of the DB or gain a no-duck/read-only constructor flag.

## policy-vs-impl-5  [CONFIRMED | ops_reporting]

**Proof:** status.py:219-222 computes dd as raw dollars over realized_pnl only — no capital_at_risk read anywhere in status.py, though the column exists and master.py:1292-1295 uses _max_concurrent_car for exactly this base; :226-227 prints 'PF {pf} (>1.3) | maxDD ${dd:,.0f}' — PF carries its gate annotation, DD carries neither gate nor denominator, and no slippage line exists on the surface at all. PLAN.md:497 pins 'max drawdown < 8% **of deployed risk**' (D2). The whole block lives only in main() (:196-232), not get_status() (:107-168 returns no gate keys), wrapped in try (:197) / 'except Exception: pass' (:231-232). Probe: IC verdict DD $272.29 = 3.3% of the $8,332 concurrent-CAR base — a PASS unreadable from the verdict surface.

**Consequence:** The only surface computing the R19d IC-only verdict cannot answer two of gate 1's five criteria (DD-vs-deployed-risk and slippage), and any future exception in the block (e.g. a schema change) silently deletes the entire gate readout with no error shown.

**Suggested fix:** In status.py, compute the D2 base (port master.py's _max_concurrent_car or the referee's max_concurrent_risk over IC closes + open positions), print 'maxDD ${dd} = {pct}% of deployed risk (gate <8%)' plus the trailing-20 slippage median, move the block into get_status() so both surfaces share it, and replace 'except Exception: pass' with a printed one-line warning.

## policy-vs-impl-6  [CONFIRMED | ops_reporting]

**Proof:** README.md:16 (in the maintained Dashboards table, which post-dates the line-1 stale banner — it cites status_server.py and docs/RUNBOOK.md, both July-audit creations): 8503 shows 'go-live gate scorecard'. status_server.py:36-41 renders exactly four cards (Health, Today, Realized P&L, Open Positions); the JS (:43-81) renders only get_status() keys; /api/status (:90-92) returns get_status() verbatim; get_status() (status.py:107-168) contains no gate field — no closes/50, no PF, no DD. The gate block exists solely in status.py main()'s CLI print path (:196-232).

**Consequence:** An operator following the README to monitor gate progress on 8503 (from another device, or with the CLI unavailable) finds no gate data and may read the page's 'since 07-06 reset' book-level P&L (currently -$121.46, all-strategy) as the verdict number — the exact mixed-population misread R19d was decided to end.

**Suggested fix:** Move the IC-only gate computation into get_status() (per finding 5) and add a gate card to status_server.py's PAGE, or correct README.md:16 to say the scorecard is CLI/Telegram-only.

## policy-vs-impl-9  [CONFIRMED | ops_reporting]

**Proof:** master.py:1341-1342 footer of every Friday Telegram scorecard: '— see docs/GAP_AUDIT_R7.md for gate definitions'. GAP_AUDIT_R7.md's structure (heading grep): 'Round 7 Gap Audit — component-by-component' with Market Data / Event/Calendar / ML / Strategies / Risk Stack / Execution / Ledger / Broker / Orchestrator / Learning / Backtest / Sentiment / Ops sections, each Defects/Missing/Skipped — no gates section; its gate mentions (lines 36, 812, 827, 830) cite 'go-live gate 1/2/3' as externally defined. The definitions live in PLAN.md:491-519 ('Go-live gates (decided 2026-07-07; amended by R9...)'), and the amendments the scorecard is supposed to implement — R9's 100-close funding floor (:495), D2's DD base, R19d's IC-only verdict (:1221-1230) — appear only in PLAN.md, unreachable from the pointed-to document.

**Consequence:** The single operator-facing pointer from the automated gate readout lands in a ~870-line defect audit with no criteria; an operator resolving a scorecard dispute (e.g. finding 1's FAIL-vs-PASS contradiction with status.py) cannot find the gate definitions or any of their amendments from the message.

**Suggested fix:** Change the footer to 'see PLAN.md "Go-live gates" for gate definitions' (master.py:1342).

## external-contracts-04-deadman-check-spec-vs-pinger-duty-cycle  [ENFORCEMENT_GAP | ops_reporting]

**Proof:** Every cited artifact re-verified: verify_deadman.py:8 instructs 'Period 5 min, Grace 15 min' (a 24/7 expectation) and its ARMED verdict tests only `r.returncode == 0` on one ping (:53-58) — period/grace/schedule are never checked. keeper_ait.bat is the sole pinger (repo-wide grep: master.py never touches the URL, only references the keeper in a docstring at :1144): a ~90s loop that pings only while the box is on AND a python process matching 'run_orchestrator|ait\.main' exists (:43-55). master.py:1632-1634 and :1690-1691 document the box as routinely off outside market hours (no auto-logon, boots ~08:45-09:00; off Sunday 20:00). data/deadman_url.txt exists with a valid hc-ping UUID (armed; deliberately not pinged during verification). Nothing in the repo reconciles spec and duty cycle — grep finds no schedule/cron-check instruction and no pause-on-shutdown ping anywhere. The secondary prong is unconditionally confirmed from the repo: master.py:337 sets _max_restarts=10 and :595-598 gives up with a Telegram-only 'BOT DOWN' alert while the master process (command line contains run_orchestrator) lives on — the keeper's OR-match keeps healthchecks green in exactly that bot-dead state, contradicting keeper_ait.bat:52's 'bot down' claim. Verdict is ENFORCEMENT_GAP rather than CONFIRMED because the failing behavior depends on server-side check configuration that is unobservable from the repo: per-spec config guarantees a false DOWN ~15-20 min after every shutdown evening, an off-spec widening hides real mid-day deaths, and a correctly hand-rolled market-hours cron schedule (which the repo never instructs) would behave — and verify_deadman.py reports ARMED identically in all three cases, so nothing in the repo can distinguish them.

**Consequence:** The one alert channel built to survive whole-machine death is either crying wolf every night (per-spec: last ping ~20:00 Sunday -> false DOWN ~20:20, UP at Monday boot — the alert-fatigue mode master.py R16 #54 documents), or quietly widened until a real market-hours death fits inside the tolerance — and in the reachable restart-budget-exhausted state the check stays green with the trading bot dead, leaving Telegram (finding 03's lossy channel) as the only signal. verify_deadman.py's ARMED report cannot detect any of these states.

**Suggested fix:** Change verify_deadman.py's instruction to a cron-schedule check matching the actual duty cycle (e.g. schedule '*/5 13-21 * * 1-5' UTC with grace ~20 min, or instruct BIOS auto-on + document the expected off-hours), have the keeper (or master shutdown path) curl the /fail or pause endpoint on clean shutdown, extend verify_deadman.py to fetch the check's period/grace/schedule via the healthchecks.io read-only API and fail when it diverges from the duty cycle, and split the process test so the ping requires the ait.main bot specifically (master-only alive must NOT ping).

## log-contracts-7  [ENFORCEMENT_GAP | ops_reporting]

**Proof:** Re-verified every claimed-intact pair at the cited producer lines: ibkr_connected (ibkr_client.py:216), order_placed (:508), trading_cycle_error (orchestrator.py:795), entries_frozen (:815), ml_prediction (:1112), scan_symbol_timing (:1476), trade_rejected (:2001), signals_generated (selector.py:136), trade_executed (executor.py:431), bot_started/bot_healthy/bot_down (master.py:417/486/566 via _log writing '[ts] LEVEL orchestrator.<event>' matching status.py:54's regex), 'DOWN - relaunching' (keeper_ait.bat:45). Production JSON line confirms the consumed spacing/order: '{"component": ..., "event": "scan_symbol_timing", ..., "timestamp": "2026-08-25T18:01:19..."}' — event before timestamp with ', '/': ' separators, exactly what status.py:97/check_first_rth:69,77 regexes require. Enforcement check: grep of tests/ for signals_generated/order_placed/bot_healthy/bot_down/bot_started/entries_frozen/'DOWN - relaunching'/ibkr_connected finds only an unrelated parameter name in test_ibkr_intraday.py — zero contract tests; the only loud enforcement is smoke_deploy.py:419-433 raising RuntimeError on missing capital_tier_active/ml_prediction/scan_symbol_timing (plus trade_rejected in the risk-validation check), backed by test_hot_path_smoke.py's LogSpy over all five levels. So the pairs are correct today but nothing guards them; the two already-broken pairs are reported separately as log-contracts-2 and -3.

**Consequence:** Any future rename or level change of the unenforced names (e.g. signals_generated -> signals_ranked, or demoting order_placed below INFO) zeroes the corresponding status.py/web_logs/liveness counters silently, with no failing test — the same silent-zero shape as the two pairs that already broke.

**Suggested fix:** Add a contract test that emits each consumed event through the real logging setup and asserts every name/regex used by status.py, web_logs.py, and check_first_rth_liveness.py matches, or centralize the event names in a shared constants module imported by both sides.

## multiprocess-db-2  [ENFORCEMENT_GAP | ops_reporting]

**Proof:** Probes on the live files (read-only URIs): data/historical.db journal_mode='delete', data/ait_state.db journal_mode='wal'. Grep across the whole tree: the ONLY journal_mode/busy_timeout statement is state.py:107 (ait_state.db), correcting GAP_AUDIT_R7.md:439 which discusses only ait_state.db and claims no busy_timeout exists anywhere — Python's implicit default is 5s, confirmed by a two-process scratch probe: with a BEGIN IMMEDIATE txn held in delete mode, a second process's INSERT raises 'database is locked' after exactly 5.0s (a concurrent plain read during the held RESERVED lock succeeded immediately — reader exclusion happens only during the commit's EXCLUSIVE window, so the finder's 'any writer's commit excludes all readers' is right for the commit window; writer-vs-writer excludes for the whole txn). Writers verified: bot per-scan save_intraday + cleanup_old_intraday DELETE (orchestrator.py:1074-1075, and :1074 sits OUTSIDE the :1062-1070 try/except so an OperationalError propagates to symbol_scan_failed at :971-972), save_daily_iv (:3286); retrain subprocess HistoricalDataStore() (master.py:833) with trainer.py:196 store.save() writing daily_prices (DATA-H2 comment at historical.py:183-187 documents this exact retrain-vs-backfill write collision class); scripts default to data/historical.db (backfill_historical_data.py:76). Routine mid-RTH overlap confirmed: master.py:1648-1656 schedules catch-up retrain +2min after any weekday boot >=07:35 with no retrain log, and comments at :1659-1661 document boots at 13:43 and 11:44; the :1637 'Safe intraday' comment covers only model reload. Why not CONFIRMED: firing requires a single lock held >5s continuously, and today every transaction is ms-scale (27k intraday rows, 6.7MB file; save/DELETE/read all sub-0.1s), so no realistic input can currently produce the failure — and grep of 823MB of logs finds zero 'database is locked' evidence. No test or validator pins historical.db's journal mode or timeout (grep tests: nothing).

**Consequence:** Correct today by timing luck only. As the 2-year intraday retention and symbol universe grow (or a backfill/calibration script runs during RTH), a >5s write txn overlapping the bot's scan makes save_intraday raise 'database is locked', aborting that symbol's whole scan cycle (silently logged as symbol_scan_failed — the symbol is not evaluated that cycle), or aborts the retrain subprocess mid-save leaving models stale.

**Suggested fix:** In HistoricalDataStore._init_db, mirror state.py: execute PRAGMA journal_mode=WAL once (database-persistent, immediately fixes reader/writer exclusion) and pass a busy timeout on every connect; optionally move save_intraday/cleanup inside the :1062 try so a lock error costs the persist, not the symbol's evaluation.

## multiprocess-db-4  [ENFORCEMENT_GAP | ops_reporting]

**Proof:** Sites verified: app.py:23 DB_PATH = Path('data/ait_state.db'), _get_conn (:87-90) plain sqlite3.connect — no mode=ro URI, no existence guard; duckdb_analytics.py:29 DUCK_DB_PATH = Path('data/ait_analytics.duckdb') consumed by __init__'s default (:114) whose _init_schema (:125-126) opens read-write and CREATEs the full schema; analytics.py:29 shares the sqlite default and analytics.py:118 DuckDBAnalytics() the duck default. Contrasts confirmed: status.py:21-22 anchors to __file__, runtime_env.py:211-212 and smoke_deploy.py:167-168 use mode=ro URIs; the R11 note (state.py:236-241) records this exact defect class actually firing (pytest fixture trades dual-written into the PRODUCTION analytics store, dashboard 7d P&L +$1,613 vs true +$383) and its fix covers only StateManager — analytics.py/app.py still use the cwd-relative defaults. Executed probe from a scratch cwd containing a data/ dir, using the project venv: replicating app.py's connect created a 0-byte data/ait_state.db, the trades query raised 'no such table: trades' (exactly what _safe_query at :93-98 swallows into an empty DataFrame), and DuckDBAnalytics() manufactured a fully-schema'd 274,432-byte empty duck store whose get_performance(7) returned total_trades=0 with no error — a silently all-empty dashboard. Why not CONFIRMED: launched from trade_v2 (the documented 'streamlit run src/ait/dashboard/app.py' from repo root, README:360) everything resolves correctly, the production dashboard is currently running correctly (dashboard.log active), master's daily_report subprocess passes cwd=str(ROOT) (:949) and the smoke chdirs to ROOT — so it is correct today, with nothing enforcing the cwd: no path anchor, no existence check, no read-only open, and no test pins app.py's or TradeAnalytics' default paths.

**Consequence:** Any future launch of the dashboard (or construction of TradeAnalytics in a tool/script) from a cwd other than trade_v2 that contains a data/ directory silently creates parallel empty stores there and renders every panel 'no data' with zero errors — the operator debugs a healthy bot, or worse, reads the empty dashboard as 'no trades happened'.

**Suggested fix:** Anchor all three defaults to the repo root via __file__ (as status.py:21-22 does); open the dashboard's sqlite with a mode=ro URI so a wrong path errors instead of creating a file; fail loudly (st.error) when the DB file does not exist.

## multiprocess-db-5  [ENFORCEMENT_GAP | ops_reporting]

**Proof:** state.py:106-108 re-read: both PRAGMAs issued on one connection that _init_db closes; grep of the entire tree confirms state.py:108 is the only foreign_keys statement anywhere (src, scripts, tests — tests contain no foreign_key reference at all, so nothing guards this). Executed probes: (1) live data/ait_state.db reads journal_mode='wal' via a fresh read-only connection — confirming the WAL half persists in the database file; (2) scratch DB created with PRAGMA foreign_keys=ON on connection 1: a FRESH second connection reads PRAGMA foreign_keys = 0 and ACCEPTS an orphan child insert referencing a nonexistent parent — confirming foreign_keys is strictly per-connection and defaults OFF, so the declared FKs (open_positions.trade_id :163, trade_context.trade_id :175) are enforced on zero real operations: none of StateManager's ~30 per-method bare connects (incl. the _connect() helper at :754-762, which is a bare sqlite3.connect), nor status.py, dashboard app.py:88, meta_label, or master.py connections re-issue it. The code demonstrably treats both pragmas as equally persistent; only one is. (3) Live-DB state today: 0 orphan trade_context rows, 0 orphan open_positions rows, PRAGMA foreign_key_check returns 0 rows — the gap has produced no divergence yet, which is exactly why this is an enforcement gap rather than a confirmed corruption: declared integrity constraints that no real operation has ever run under, with past purge/restatement activity evidenced by data/ait_state.pre_legacy_purge.db and ait_state.db.bak_learnreset_* snapshots (adjacent to but distinct from GAP_AUDIT_R7.md:439, which covers WAL/busy_timeout and never mentions foreign_keys).

**Consequence:** Any partial write across the parent-child pairs — a future purge/restatement script deleting trades rows without their trade_context/open_positions children (purges have happened before, per the pre_legacy_purge snapshots), or a crash between insert_open_position and record_trade — silently lands instead of being rejected, leaving orphan rows that skew per-trade context joins, learning-cycle inputs, and dashboard/report aggregates until someone manually runs foreign_key_check.

**Suggested fix:** Route every StateManager connection through one _connect() helper that executes PRAGMA foreign_keys=ON per connection (and use it in the ~30 bare sqlite3.connect sites), or drop the two FK declarations as dead and add a periodic PRAGMA foreign_key_check to the health check / smoke so orphans are detected instead of silently accumulating.

## external-contracts-01-bracketorder-signature  [CONFIRMED | cosmetic]

**Proof:** Executed probe with the project venv (ib_insync 0.9.86): OrderBuilder.bracket('BUY', 1, 2.5, 3.5, 1.5) raises `TypeError: BracketOrder.__new__() takes 4 positional arguments but 6 were given`. trade_v2/venv/Lib/site-packages/ib_insync/order.py:330-333 defines `class BracketOrder(NamedTuple): parent: Order; takeProfit: Order; stopLoss: Order` — a result container built by IB.bracketOrder(), not a price-taking constructor. Probe also confirmed the sub-claim: a 3-positional call succeeds but returns BracketOrder(parent=2.5, takeProfit=3.5, stopLoss=1.5) — a tuple of floats, not placeable Orders. Repo-wide grep: zero call sites for .bracket( in src/, tests/, scripts/ (OrderBuilder.market/limit/combo_limit ARE used live in orchestrator.py, but never bracket); zero tests cover it; the defect was already recorded in docs/GAP_AUDIT_R7.md:533 (R5-D8) and never fixed, while orders.py:4 still advertises 'bracket (with stop-loss/take-profit)' support.

**Consequence:** Every possible invocation of the advertised bracket builder crashes at order-build time. Dead code today (no caller exists), so no live trade is affected — but the natural implementation path for broker-side stop-loss/take-profit protection (which the bot lacks; exits are software-monitored only) is a landmine: any future adopter gets an immediate TypeError, and the module docstring plus the exported OrderBuilder API actively invite that adoption.

**Suggested fix:** Either delete OrderBuilder.bracket and the BracketOrder import plus the docstring claim, or reimplement it via the real contract: build parent/takeProfit/stopLoss LimitOrder/StopOrder objects with parentId linkage and transmit flags (or call ib.bracketOrder(action, quantity, limitPrice, takeProfitPrice, stopLossPrice)) and return the NamedTuple of three placeable Orders. Add a unit test that constructs one against the installed ib_insync.

## model-vs-reality-cleared-checks  [CONFIRMED | cosmetic]

**Proof:** All four clearances re-verified with independent probes; the note is accurate and these should not be re-hunted. (1) Carry/dividend structure error immaterial: my re-implementation of BS with dividend yield q prices the reference condor (S=640, iv 0.18, dte 21, strikes 620/666/576/710) at credit 6.1696 (r=5%, q=0) vs 6.1847 (r=4%, q=1.1%) — a $1.50/contract difference with the sign netting across the symmetric 4-leg structure; the material dividend risk is assignment, correctly filed separately. (2) Entry slippage no longer 15%-of-credit: the 5 BAG summary rows in ait_state.db executions with live_mid populated show fill-vs-mid of +5.8%, +1.7%, -7.0%, -1.0%, -2.2% (mean -0.56%, median -1.04%) — centered near zero post-R7 reprice ladder; and exit commissions are modeled on both sides (engine.py:946-951 entry + :2473-2476 exit; live estimate at executor.py:2295-2308 with ledger true-up at orchestrator.py:479-509). (3) No leg-by-leg close margin sequencing on the normal path: orchestrator.py:2273-2278 submits multi-leg closes as one reverse combo via _close_multi_leg. (4) Corporate-action/split handling: grep 'split|corporate action|strike.*adjust' in reconciler.py → zero matches, matching the note's 'noted, not filed' disposition for a SPY/QQQ/IWM-only universe.

**Consequence:** None — these are the class's checked-and-cleared items; the only consequence of this entry is process hygiene (prevents future hunts from re-filing the immaterial carry/slippage/close-sequencing/split candidates as findings).

**Suggested fix:** No code change. Keep the note attached to the hunt record so the four cleared shapes stay excluded; the only watch-item is that the BAG slippage clearance rests on n=5 fills and should be re-checked once the ledger has ~20+ combo fills.

## policy-vs-impl-8  [CONFIRMED | cosmetic]

**Proof:** config.yaml:205-214: 'enabled: true', 'weight: 0.20 # Sentiment contributes 20% to final signal', 'news + fear_greed remain'. orchestrator.py:1037-1041: unconditional 'sentiment = None' under the R12-C retirement comment; the guarded 'if sentiment:' block at 1086-1090 is therefore dead. settings.py:319-332: SentimentConfig is an explicit tombstone stub (enabled default False, extra='allow', 'Nothing reads this config anymore'); grep confirms SentimentConfig/settings.sentiment appears only inside settings.py — zero consumers of enabled/weight/sources. Under R19c (PLAN.md:1202-1203, 'config.yaml is THE operating source for trading values') the operating source asserts a 20% signal contribution computed nowhere; the R19c enforcement tests guard that trading keys HAVE config homes, not that a config section's claimed behavior has a reader.

**Consequence:** Anyone reading the operating config concludes a 20% sentiment weighting drives entries and may 'tune' weight/sources, silently changing nothing; no trade, verdict, or report number is affected, so the damage is a false claim in the authoritative config.

**Suggested fix:** Rewrite the sentiment block to 'enabled: false' with a one-line R12-C tombstone comment (or delete the section — the permissive stub tolerates absence), removing the '20% to final signal' claim.

# Round 3 — composition flows

## bot-day-03-hang-detector-kills-slow-startup-no-grace  [CONFIRMED | live_money]

**Proof:** master.py:494-507: heartbeat check has no startup grace (self._last_restart never consulted), no budget increment, and fires stop()+start() whenever is_market_open() and mtime >15 min; stop() (:469-477) is Popen.terminate() = TerminateProcess. Grep confirms orchestrator.py:760 is the ONLY writer of data/bot_heartbeat, and it executes only inside the MARKET_OPEN while-loop (:745-762). run() (:277-337) awaits ensure_models_ready (:285) before reconcile and before the loop; trainer.py:465-476 falls through to synchronous train_all_symbols when needs_training() (7-day interval settings.py:280, recovered from artifact dates :85-158, spec-mismatch, drift), with the R9 comment (:60-64) pricing boot training at ~110s-7.5min. So a mid-RTH boot with stale artifacts has a heartbeat mtime hours old, and the first 2-min health tick kills a healthy training bot; the restart repeats the same blocking startup and is killed at every subsequent tick while startup exceeds the ~2-min inter-tick budget. train_all_symbols retrains all symbols each boot and needs_training keys on the OLDEST artifact, so convergence requires the whole startup to fit under one tick. master.py:865-867 itself anticipates 'legitimate slow training on older hardware'. The hang branch spends no restart budget, so nothing escalates; the keeper pings green throughout (processes exist).

**Consequence:** On a documented late boot (PLAN.md:694-695, host down 5/10 nights, returns 10:22-13:43) with lapsed models, the bot is hard-killed mid-startup every ~2-3 min for the rest of the session: open positions get no reconcile, no touch stop, no marks all day, while 'BOT HUNG ... restarting' pages claim recovery and the dead-man stays green.

**Suggested fix:** In the hang branch, skip when (now - self._last_restart) < ~20 min, or have the bot touch data/bot_heartbeat at run() entry (before ensure_models_ready) so a starting process reads fresh; optionally have BotManager.start() delete/refresh the stale file.

## bot-day-04-startup-training-blocks-protection-arming  [CONFIRMED | live_money]

**Proof:** orchestrator.py:277-346: run() awaits self._trainer.ensure_models_ready (:285) BEFORE the startup reconcile (:294), before exit-order re-adoption (:300-306), and before the phase loop (:337); check_positions is only reachable from _monitor_positions_fast/_trading_cycle inside _trading_loop (:735-801, :843), so no exit rule evaluates until training completes. trainer.py:465-476 trains synchronously in-process when the 7-day interval lapsed / artifact missing / spec_mismatch / drift; historical cost ~110s-7.5min (trainer.py:60-64). portfolio.py:463-464: AIT_CREDIT_LOSS_LIMIT defaults 0 -> effective_stop=-999 (flat credit stop disabled); the short-strike touch (:522-609) is the only credit loss exit, and it is not running during startup. The bot places no resting stop orders at the broker, so a mid-RTH restart (master restarts within one 2-min tick, master.py:604-612) with open credit positions leaves the book with zero loss exits for the full training duration. Each stage verified individually correct; the composition (slowest optional work ahead of the only loss exit) is as claimed.

**Consequence:** A 14:00 restart with 7-day-stale artifacts and an open condor leaves a breaching short strike unprotected for the multi-minute training window; the touch stop can only fire after training + reconcile + first loop tick, and the same window recurs on every kill of the bot-day-03 loop.

**Suggested fix:** Reorder run(): reconcile + adopt_exit_order + start position monitoring first, then ensure_models_ready (models gate only NEW entries, which can simply be refused until training completes, e.g. train in a background task and have _trading_cycle skip scans while untrained).

## bot-day-05-exit-order-placed-before-closing-cas-restart-duplicates-close  [CONFIRMED | live_money]

**Proof:** Ordering verified: _execute_exit builds and submits via _close_multi_leg at orchestrator.py:2278, which ends 'return await self._ibkr.place_order(combo, order)' (:2890, orderRef=trade_id stamped :2889); the CAS to CLOSING is only at :2292-2294 and register_exit_order at :2310. A hard kill (master stop() = TerminateProcess; python-killer; power cut) in that window leaves a live broker exit combo with the trade FILLED and no tracker. Every recovery layer misses it as claimed: reconciler.py:903-905 fetches working orders only 'if any(t.status == TradeStatus.CLOSING ...)' — none is, and a FILLED trade with live legs just counts matched (:916-947); the fast monitor's closing-skip set is the in-memory _executor._pending_exit_orders (:671-673), empty after restart (executor.py:236); _execute_exit's liveness gate (:2179-2234) checks POSITIONS (legs still live -> 'live' -> proceed); the reject backoff _exit_attempts is an in-memory instance dict (:3383-3385); check_fills iterates only the empty in-memory _pending_orders (executor.py:856). So a persisting breach gets a second reverse combo ~30-60s after restart -> both fill = reversed book, or the orphan DAY order fills later against a flat book = untracked inverse position -> reconciler 2-sighting HALT_UNTRACKED (reconciler.py:1009+). The asymmetry with entries is real: executor.py:336-341 documents the R12 row-before-order fix for entries, and the reconciler already recovers the safe direction for exits (CLOSING-with-no-working-order promotes back to FILLED, :976-990), so CAS-before-place is strictly recoverable.

**Consequence:** A kill in the seconds between exit placement and the CLOSING write yields a duplicate close on the still-breached position: an inverted condor plus an untracked fill, or a phantom-flat book that later freezes ALL entries via HALT_UNTRACKED with an unmanaged overnight position.

**Suggested fix:** Mirror the entry fix: CAS the trade to CLOSING (and persist the pending-exit intent) BEFORE placeOrder; on failure to place, revert CLOSING->FILLED. The reconciler's existing CLOSING-no-order promotion (:976-990) already cleans up the residue of that ordering.

## bot-day-09-mtm-gap-brake-baseline-lost-when-close-window-missed  [CONFIRMED | live_money]

**Proof:** Grep confirms exactly two writers of mtm_sod_<date>: the prestamp (orchestrator.py:1532-1533, keyed to next_market_open().date()) called ONLY from _post_market (:3017), and the lazy first-tick capture (:636-641) which writes the CURRENT post-open unrealized when the key is empty, with no log and no alert that the degraded path was taken. _post_market runs only if the bot is alive during POST_MARKET (close->close+15, scheduler.py:52-63); a bot down across that window (documented nightly shutdowns, PLAN.md:694-695) or restarted after 16:15 (phase = OFF_HOURS, run() :352-354) never prestamps. The post-15:45 hang blind spot verified: master.py:496 gates the hang check on is_market_open() (RTH-only, utils/time.py:55-67), so a 15:46 hang reaches 15-min staleness only after the close and is never restarted in time. Next gap-down session: baseline = already-collapsed post-gap unrealized, mtm_day = realized + ~0, check_daily_loss_mtm (:643-645) never trips — the R14 #8 documented blindness reintroduced exactly on outage/late-boot days, which are also the days positions ran unmanaged overnight.

**Consequence:** On the mornings the gap brake was built for (overnight gap after an outage evening with open condors), it silently reverts to the blind lazy baseline and does not block new entries on a bleeding day — with no signal anywhere that the baseline is degraded.

**Suggested fix:** At the lazy-capture site, when the key is empty AND open positions exist, reconstruct the baseline from persisted leg marks / prior-close data instead of current unrealized (or at minimum log+page 'MTM baseline degraded — captured post-open with overnight positions'); also stamp the key opportunistically at bot shutdown.

## bot-day-10-scan-duration-silently-stretches-the-30s-protection-cadence  [CONFIRMED | live_money]

**Proof:** Structure verified at orchestrator.py:745-801: one sequential task; the if/else (:765-784) makes _trading_cycle and _monitor_positions_fast mutually exclusive per iteration, and 'await self._trading_cycle()' (:777) runs to completion with NO asyncio.wait_for/timeout anywhere, then sleep(30) — so the real inter-evaluation gap is 30s + (scan duration past its own early check_positions at :843). _trading_cycle's tail is unbounded network work: per-symbol history/chain scans (:967-972), 1.5s combo-NBBO waits per candidate (executor.py:628-631), 4s partial-exit fill waits whose comment (:2365-2370) explicitly concedes 'while it sleeps NO fast-monitor pass runs — every other position's stop/TP check is delayed', plus retry sleeps (:3469). Nothing measures the gap between consecutive check_positions calls; the file heartbeat (:747-760) is written per-while-iteration so a long scan reads alive until the supervisor's 15-min backstop (master.py:499). The calibration dependence is real: settings.py:350-362 justifies the 180s staleness budget 'because the fast monitor runs on a 30s cadence', portfolio.py:99 hard-codes '~5 min at the 30s fast-monitor cadence', touch_confirm_ticks=2 means a degraded-quote touch needs two evaluations (portfolio.py:578-591), and the R16 incident record (portfolio.py:122-127) prices +30-60s on this exit as a live loss. R18 (:767-775) fixed only the exception-starvation path, not duration. Distinct from the registered constants finding as claimed: the constants agree; the runtime loop violates their shared assumption.

**Consequence:** A touch breach starting just after a scan's own position check waits the whole remaining scan plus 30s (plus a second confirming tick on degraded feeds) before the exit can fire — multi-minute delay on the only credit loss exit, precisely during fast tapes when scans run longest, with no measurement or alert until the 15-min supervisor backstop.

**Suggested fix:** Run _monitor_positions_fast as its own independent 30s asyncio task (or wrap _trading_cycle in asyncio.wait_for with a budget of ~60-90s), and record/alert on the inter-check_positions gap exceeding ~2x the 30s cadence.

## money-flow-01  [CONFIRMED | live_money]

**Proof:** Every link holds. (1) Reconciler books real P&L via state.close_trade ONLY: trade_v2/src/ait/execution/reconciler.py:417-422 (_book_stale_trade; 'reconciler_ibkr_realized' summing leg realizedPNL at 331-333, 'reconciler_expired_intrinsic' at 366-372), book_vanished_trade 441-453 delegates to it, stale-pending at 824-828; grep of reconciler.py for update_daily_stats|record_trade_result|daily_stats|circuit returns ZERO matches. (2) close_trade (src/ait/bot/state.py:346-424) writes trades + deletes open_positions; touches no stats/breaker, commission defaults 0.0 and reconciler never passes one. (3) The only stats/breaker/PDT/commission bookkeeper is _process_completed_exits (src/ait/bot/orchestrator.py:463-602: stats 516-526, breaker.record_trade_result 525, PDT record_day_trade 543-551 — repo grep shows 549 is the sole call site — commission true-up 479-512), fed exclusively from executor check_fills_safe (686, 881) and the fill-event callback (240-243); the only other breaker feeder is the single-leg ladder partial (2436-2440, record_partial_pnl). The post-market true-up (2916-2934) only iterates trueup_pending_* keys, which only _process_completed_exits sets (505). (4) Gate 8 (src/ait/risk/manager.py:572-574) calls check_daily_loss, which reads breaker._daily_pnl (src/ait/risk/circuit_breaker.py:268-283) — fed only at 525/2440; the R16 restore blob (89-127) restores the breaker's own last-persisted value, never recomputes from trades. (5) MTM brake realized term reads daily_stats.total_pnl (orchestrator.py:642); daily summary prints stats.total_pnl (3023). (6) The verdict/ops surfaces DO count reconciler closes: master.py:1262-1269 and status.py:159-166 filter only %migrated%/%pending%/%never_filled% — both reconciler reasons pass. (7) Live mid-RTH path exists: orchestrator.py:2185-2199 (exit_refused_position_not_at_broker -> book_vanished_trade); startup/pre/post reconcile call _book_stale_trade at reconciler.py:1139/1196 from orchestrator.py:294/379/2937. (8) DB probe (data/ait_state.db): 6 'stale_pending_never_filled' rows booked via the reconciler sweep and a real -$575.33 'stop_loss_manual_flatten_restated' close prove both the reconciler close path and operator flattens occur in production.

**Consequence:** A broker-derived realized loss (manual TWS flatten mid-day, or a position that expired ITM while the bot was down) lands in trades.realized_pnl — counted by the go-live scorecard, status pnl_today and the master digest — while circuit_breaker._daily_pnl, daily_stats.total_pnl (hence the MTM brake's realized term), the PDT counter and the commission true-up all miss it. Gate 8 keeps passing and new entries keep opening the same day; the 16:xx daily summary prints a realized number that disagrees with the digest/status for the same day's dollars.

**Suggested fix:** Route reconciler bookings through the same bookkeeper as executor fills: have _book_stale_trade/book_vanished_trade return a completed-exit dict and have every call site pass it to _process_completed_exits (or a shared book_realized_close() that updates daily_stats, breaker, PDT and schedules the commission true-up), instead of calling state.close_trade directly.

## money-flow-02  [CONFIRMED | live_money]

**Proof:** All links verified. (1) portfolio.py:382-384: marks_missing -> unrealized_pnl = 0.0; the flag lives only in a local + log; PositionStatus (39-55) has no missing-marks field and the 0.0 is returned as the status value at 786-800. check_positions (220-254) appends these statuses; _get_option_marks (256-268) returns {} on an empty/post-reconnect ib.portfolio(), making every leg lookup fail -> None -> 0.0; the MARKS MISSING x10 alert machinery (393-411) documents multi-tick outages as a real class. (2) orchestrator.py:634 sums p.unrealized_pnl over statuses (the zeros included); 637-639 lazily stamps the SOD key with that sum, UNGUARDED against marks_missing; 642-643 computes mtm_day = daily_stats.total_pnl + (unreal_now - SOD); 645 feeds check_daily_loss_mtm which trips at 2% of NLV (circuit_breaker.py:249-266; config.yaml:45 max_daily_loss_pct 0.02). During an outage with SOD carry -X the brake reads realized + X (phantom gain) and cannot trip on the actual bleed until marks return. (3) The prestamp exists only in _post_market (orchestrator.py:3017 -> _prestamp_mtm_baseline 1519-1537, keyed to next_market_open, lazy write only fires when key empty) — a bot that was down at 16:xx has no prestamp, so the next morning's first fast tick stamps whatever _unreal_now is, including outage zeros; every later mtm_day then mis-books the entire prior carry as today's move. (4) The divergent-consumer contrast is real: the DB mark writer is guarded (portfolio.py:433-438, 'not marks_missing and persist') so open_positions keeps the last REAL mark (master.py:1162-1166 digest reads SUM(unrealized_pnl) FROM open_positions), while the brake consumes the in-memory zeros.

**Consequence:** The one control built to halt entries on a bleeding gap day is disarmed exactly during a marks outage (phantom gain equal to the book's carry), and an outage-stamped SOD baseline makes every subsequent mtm_day over- or under-state the day by the whole prior carry — a false 'DAILY MTM LOSS HALT' page + entry block, or a missed genuine halt. At the PLAN gate-3 funded scale ($2.1k, cap ~$42) ordinary overnight carry exceeds the entire daily-loss budget, so the misfire is material precisely at go-live.

**Suggested fix:** Carry marks_missing on PositionStatus (or return (value, ok) tuples); in the fast monitor, skip the MTM evaluation AND the lazy SOD stamp for any tick where any position's marks are missing (or fall back to the last persisted open_positions marks, which the digest already trusts), and refuse to stamp an SOD baseline of 0.0 while open positions exist with no usable marks.

## money-flow-04  [CONFIRMED | live_money]

**Proof:** Code and data both confirm the fork. Code: master.py weekly_scorecard's SELECT (1265-1269) fetches realized_pnl/entry_time/exit_time/car/comm with NO strategy column or filter — PF/win-rate/net/maxDD (1270-1284) and the 'closes: {n}/50' clock (1333) run over ALL strategies; grep of master.py finds zero GO_LIVE_VERDICT_STRATEGIES references (the frozenset lives in strategies/base.py:63 and is imported only by status.py:19/224); the D2 DD base (1292-1295) feeds all trades' car into _max_concurrent_car; the slippage line (1307-1311) is AVG(ABS(price - live_mid)) in dollars over ALL executions ever (mean not median, entries+exits, no trailing-20 window, no trend test) yet is labeled '(gate: median <=8% of credit)' — not the PLAN.md:498 pinned statistic. The scorecard is the scheduled surface: CronTrigger fri 16:10 (master.py:1611-1613). status.py:203-230 implements R19d (IC-only verdict + all-strategy line underneath), matching PLAN.md:1221-1230 which records the fix as status.py-only; status_server.py serves get_status() only — pnl_life/pnl_today are all-strategy and no IC verdict field exists on the web surface. Data (probe of data/ait_state.db replicating both computations exactly): master's math -> n=17/50, PF 0.89, net -$121.46, maxDD $862.52; status's R19d IC-only math -> n=8/50, PF 1.42, net +$180.60, maxDD $272.29 — the two surfaces sit on opposite sides of the PF>1.3 gate TODAY, and the Telegram number is the retired one.

**Consequence:** The scheduled Friday 16:10 'GO-LIVE SCORECARD' — the gate-number surface the operator receives — reports PF 0.89 / 17-of-50 / maxDD $863 on the metric R19d explicitly retired, while the pinned verdict metric (PF 1.42, 8/50, $272) is visible only by running status.py locally; the go-live decision's scheduled input is not the pre-registered criterion, and the fork can flip a failing IC verdict to a passing Telegram number (or vice versa, as today).

**Suggested fix:** In weekly_scorecard, add strategy to the SELECT and compute the headline gate block over GO_LIVE_VERDICT_STRATEGIES (imported from ait.strategies.base), keeping the all-strategy line underneath as status.py does; restrict the D2 DD base rows the same way; replace the slippage line with the pinned statistic (median entry-combo slippage as % of credit over the trailing 20 entry fills, plus a trend check) or relabel it honestly until implemented.

## research-to-live-03  [CONFIRMED | live_money]

**Proof:** Every recipe divergence verified in code plus artifact probe. LIVE: trainer.py:197-199 predictor.train(df, symbol, market_context) with NO intraday_store -> ensemble.py:331-333 sets include_vlmc=False; trainer._fetch_market_context:268-315 adds VIX+SPY+FRED 'macros' (:304-313); lookback=504 trading days (settings.py:281, config.yaml:168). Executed probe of models/ensemble.pkl: SPY/QQQ/IWM each 75 feature_names, ZERO VLMC names, all 8 yield-curve/DXY macro features PRESENT, versions v-20260825-080915/26/37 (retrained today pre-market). RESEARCH: walkforward.py:2454-2457 trains WITH intraday_store (:2431-2434) and _market_ctx built ONLY from vix/spy (:2436-2445) — features.py:887-903 therefore fills all 9 macro features with CONSTANTS in every research model (tree models can never split on them); train_days=365 (ablation_confirm.py:49). Range: research rp.train has NO market_context (walkforward.py:2394) vs live WITH market_context (trainer.py:206-209). Within-research serve skew: OOS feature cache computed WITHOUT intraday_store/symbol (walkforward.py:1383-1389), passed as features_cache (:1407) -> engine.py:1203-1219 predict_from_features -> ensemble.py:248-249 fillna(0.0) zero-fills the 26 VLMC features (FeatureEngine.get_feature_names probe: 101 with VLMC vs 75 without) for windows overlapping the 2026-06+ store. The go-live linkage is explicit: settings.py's comment block directly above entry_gates_enabled=True (:283-294) cites the 08-08 research-recipe OOS numbers (gates OFF PF 1.05 DD 34.57 vs ON PF 1.27 DD 9.97) as the reason the live gate defaulted ON. Grep of tests/: files touching both trainer and _train_window_model exist but none compares the train call signatures/contexts; the register's units-scale-05 pins only the range threshold/horizon spec.

**Consequence:** The estimator gating real entries tonight (504d, no VLMC, real FRED macro variation, trainer fetch path) is a structurally different object from the estimators that produced the OOS evidence used to switch the entry gates ON (365d, VLMC-in-then-zero-filled, macros frozen constant, range model context-blind): a macro-regime move drives live gate admit/veto decisions through feature dimensions that were constants in every validated window, so the gate's live behavior in exactly those regimes is unvalidated by construction while the scorecard treats the 08-08 numbers as its evidence.

**Suggested fix:** Extract one shared training-recipe function (feature set, context contents incl. macros, intraday_store policy, window length, range-model context) parameterized only by data window, called by both trainer.train_all_symbols and walkforward._train_window_model/_train_window_range_model; add a parity test asserting the two paths produce identical feature_names and context keys, and extend the spec guard to pin the full recipe, direction ensemble included.

## research-to-live-04  [CONFIRMED | live_money]

**Proof:** Full chain verified. orchestrator.py:1034 fetches get_historical(symbol, days=504) on every RTH per-symbol scan; the single `hist` from :1042 flows unmodified to the direction predict (:1102-1107), the range-gate predict (:1379-1384, whose probability_in_range OVERRIDES iron-condor confidence at :1396-1400 and is filtered by the 0.65 floor at :1431-1436 — the binding entry gate with entry_gates_enabled=True), regime analysis (:1168) and entry_price (:1317). Grep of the scan region, ensemble.py and range_predictor.py: NO code drops a row dated today — the only last-row handling is .iloc[[-1]] (ensemble.py:163, range_predictor.py:495). Yahoo branch: market_data.py:634-642 yf .history(period='2y', interval='1d') then tail(days) — yfinance's daily history includes the current session's in-progress candle during RTH, so at 10:35 ET the final feature row treats a 65-minute bar as a finished day; rsi/realized_vol/bb_position/roc computed on it feed the gate. Polygon branch: :552-585 requests to=today but free-tier daily aggs deliver end-of-day, so the frame ends at YESTERDAY's close for an intraday decision; which branch runs is decided at runtime by the R16 auth breaker (:588-610, POLYGON_API_KEY is configured in .env) and the success path logs nothing about the source (only failure debug + one-time disable warning); the 3600s cache (:240) additionally freezes whichever frame served for up to an hour. Training never sees either shape: models retrained pre-market (artifact versions v-20260825-0809xx = 08:09 ET) on complete bars, labels from complete closes (ensemble.py:326). The repo's own DATA-M8 diagnosis of exactly this skew (historical.py:486-495: 'a half-day bar ... a bar shape the models never saw in training') guards ONLY resample_to_daily, which the live scan path (get_historical = Polygon->Yahoo) never calls. One trigger detail is overstated: a tripped Polygon breaker sets _polygon_client=None permanently for the process, so a mid-day 'recovery' flip requires a restart — but the composition claim (two divergent unvalidated staleness regimes, runtime-selected, unlogged) does not depend on same-day flipping.

**Consequence:** The range probability that is today's binding entry gate is computed with its decision feature row built either from a mid-session partial daily bar (deflating realized-vol/momentum features on a quiet morning and inflating range-containment probability) or from a frame ending at yesterday's close for a 15:30 decision — both bar shapes absent from the training distribution and from the walkforward OOS evaluation (which decides on complete bars), so real entries are admitted/vetoed under a feature regime no evidence covers, with no log of which regime served.

**Suggested fix:** In get_historical (or immediately after :1042 in the scan), drop any final row whose date == today-ET before feature computation — the same DATA-M8 guard already applied to resample_to_daily — and log the serving source (polygon|yahoo) plus last-bar date on every successful fetch so the staleness regime is visible and testable.

## trade-life-close-booking-two-owners  [CONFIRMED | live_money]

**Proof:** Every link verified. (1) orchestrator.py:463-526 is the only place daily stats won/lost+total_pnl, circuit_breaker.record_trade_result, PDT record_day_trade (543-549), Thompson/drift (562-588), and the commission true-up/commission stamp (479-512, 556-559) run; the docstring itself says it must be called from every check_fills site. (2) grep confirms record_trade_result has exactly two callers (orchestrator.py:525 and record_partial_pnl at 2440) and update_daily_stats has exactly four writer sites (526, 2133 entry-count, 2438 partial, 2928 post-market true-up) — all on the executor-detected path. (3) Every reconciler booking goes through state.close_trade only: _book_stale_trade (reconciler.py:417-422, called from the zero-options loop at 1139 and the stale-local loop at 1196), the sweep $0 close (824-828), and book_vanished_trade (424-453, called from orchestrator.py:2199, which after booking only sends a Telegram line, 2200-2210). (4) state.py:384-424 close_trade writes the trades row, deletes open_positions, dual-writes DuckDB — no stats/breaker/PDT hook, and no code recomputes daily_stats from trades. (5) _book_stale_trade attributes REAL broker P&L (summed leg realizedPNL 293-332, or intrinsic-at-expiry 354-372), so a genuine loss lands in trades but never in the breaker. (6) The R16 irony is real: circuit_breaker.py:69-127 persists _consecutive_losses/_daily_pnl across restarts precisely for the crash-relaunch case, yet a loss whose close is discovered BY the relaunch's reconcile bypasses record_trade_result entirely. (7) The MTM brake reads daily_stats.total_pnl as its realized term (orchestrator.py:642-643), so a reconciler-booked loss makes the open unrealized loss vanish while realized never lands. Trigger path is live: exit order working, process dies (documented Gateway-drop scenario), fill lands at broker, startup reconcile books via 1139/1196, or fast-monitor liveness='gone' books via 2199.

**Consequence:** A stop-out that fills while the bot is down is recorded in trades.realized_pnl but the circuit breaker's daily P&L and consecutive-loss streak never move — the daily-loss halt and 3-loss pause undercount by the entire loss, allowing new entries a functioning breaker would block; PDT day-trade count undercounts vs the broker; Telegram DAILY SUMMARY and the MTM brake report a better day than happened; Thompson/drift never see the outcome.

**Suggested fix:** Make the reconciler's close paths emit the same completed-exit event: have _book_stale_trade/sweep return {trade_id, exit_price, realized_pnl, exit_reason} records and route them through orchestrator._process_completed_exits (or a shared booking hook that updates daily_stats, breaker, PDT, Thompson) instead of calling state.close_trade in isolation.

## trade-life-commission-trueup-gate-measures-wrong-quantity  [CONFIRMED | live_money]

**Proof:** Both paths verified in code. Path A: orchestrator.py:482-494 — the R16 comment states the async thing is commissionReports, but the gate tests row count (_exec_n = count_executions >= 2*_legs_n, state.py:806-813 COUNT(*)) plus _real_comm0 > 0. executor.py:1445 runs _sweep_executions at the TOP of check_fills (inside _cancel_stale_orders), before the exit-fill booking at 1089-1141, and 1363-1368 writes each row with comm=0.0 whenever commissionReport is absent/empty — so the exit rows exist (satisfying the row-count gate; BAG summary rows at 1355-1360/_is_bag_row inflate the count further: ~10 rows vs the 8 required for a condor) while their commissions are still placeholders, and entry-side commissions make _real_comm0 > 0. delta = commission_estimate − entry-only ledger permanently ADDS the missing exit-side commissions to realized_pnl; no trueup_pending marker is set on this branch (505-506 is the elif), and nothing ever re-reads the ledger for a trade already trued. Path B: orchestrator.py:2916-2931 — the post-market loop has NO count_executions/completeness condition (only _real > 0), books the delta into get_daily_stats() = TODAY regardless of close date, deletes the marker unconditionally at 2931, and never calls update_trade_commission (trades.commission stays whatever 556-559 stamped at booking, i.e. the then-partial sum). The restart-loss premise holds: ib_insync wipes the Trade cache on disconnect (executor.py:1629-1641) and the permId->trade_id attribution maps are in-memory only (1643-1657), so exit-side executions from before a crash never re-enter the trade's ledger, and the deferred true-up settles realized_pnl against the entry-only commissions.

**Consequence:** Closes whose commissionReports lag the ~1s debounced sweep, and any close whose process died between exit fill and sweep, get realized_pnl permanently overstated by the exit-side commissions (~$2.60-3.25 per condor) with trades.commission understated — a systematic optimistic bias in trades.realized_pnl and the 'commissions recorded' line, both direct go-live scorecard inputs; the deferred delta also lands in the wrong day's daily_stats.

**Suggested fix:** Gate on the actual quantity in question: require every execution row for the trade to carry commission > 0 (or count only rows with commission recorded) before truing up, both at booking and in the post-market loop; keep trueup_pending set when the ledger is commission-partial; stamp trades.commission in the post-market path too.

## trade-life-credit-econ-floors-signal-time-only  [CONFIRMED | live_money]

**Proof:** Every link verified: iron_condor.py:278-298 applies AIT_IC_MIN_CREDIT and the credit/width ratio floor to the SIGNAL's mid-quoted credit only, and 304 sets max_loss=(max_width−total_credit)*100 from that same credit. executor.py:651 admits any signal-to-live-mid drift up to 35% (live mid down to ~0.74x signal credit); 674-678 re-anchors the ladder to the LIVE mid with offset=max(0.10, 15% of it) (combo_entry_limit, 145); _ladder_limit (102-104) lets the credit side surrender the FULL offset with only a $0.01 floor; 693-699 caps ONLY the debit side at validated max_loss ('if not is_credit') — the asymmetry is explicit; the reprice ladder escalates 0.25→0.60→1.00 every 45s with the debit-only cap re-applied (1258-1280). No fill-time re-check exists: the fill path corrects trades.entry_price to reality (executor.py:2340-2341) but the risk stamps are written once from signal.max_loss×qty at orchestrator.py:2090-2101 (_position_capital_at_risk 1569-1571; grep confirms set_trade_capital_at_risk has exactly one caller) and never restated. Consumers verified: the aggregate 20% cap sums those per-position max_loss values (manager.py:283-324) and capital_at_risk is the go-live DD denominator (master.py:1265-1296). Worked example at the admitted extreme: signal credit 2.20 on $14 wings (ratio 0.157) with live mid 1.63 (34.9% drift, admitted) ladders to a 1.39 fill — ratio 0.099 < the 0.10 floor that gated the signal, true max loss $1,261 vs the stamped $1,180; even with zero drift a full-ladder fill concedes ~15% of mid below the floor-checked credit. One-directional, on every laddered credit fill of the only live strategy.

**Consequence:** Condors can legally fill at credits below both economics floors that justified the entry, and every laddered credit fill carries a true max loss above the validated figure while trade_maxloss_/capital_at_risk stay at the signal-time number — the aggregate-risk cap and the go-live DD-vs-deployed-risk denominator systematically understate deployed tail risk for the position's whole life.

**Suggested fix:** Mirror the debit cap for credit entries: floor the ladder's limit at the credit implied by the validated max_loss and the credit/width floor (refuse escalation below it), and restate trade_maxloss_/capital_at_risk from the actual fill ((width − filled_credit)×100×qty) in _update_trade_filled or the post-execute hook.

## bot-day-08-fallback-clientid-blinds-entry-order-sweep-after-restart  [ENFORCEMENT_GAP | live_money]

**Proof:** Mode A's load-bearing link — 'the sweep's same-clientId openTrades() shows nothing' — is false at runtime, but only by an unguarded library side effect. The bot runs on the Python313 interpreter (start_bot.bat/keeper set PY; trade_v2\venv has no ib_insync), whose ib_insync wrapper.openOrder (site-packages\ib_insync\wrapper.py:342-385) stores EVERY openOrder message — explicitly including reqAllOpenOrders responses for other clientIds — into wrapper.trades keyed (order.clientId, orderId), and ib.openTrades() (ib.py:481-484) returns ALL cached non-done trades, not same-clientId ones. The exact trigger condition of the claim (first connect on a fallback id, ibkr_client.py:196-212 + _should_stash_foreign :290-293) CALLS reqAllOpenOrdersAsync, so at sweep time the pre-crash entry order IS in ib.openTrades(); _cancel_working_entry_order Pass 1 matches orderRef==trade_id (reconciler.py:601-608; entries carry orderRef per the R19 comment :574-576, and Pass 2 catches untagged same-symbol BAGs anyway), requests the cancel, and returns True — so the sweep DEFERS (:814-820) instead of booking $0; even if TWS rejects the foreign-client cancel, deferral repeats each cycle and a later fill is rescued by the clientId-agnostic legs-live promote (:796-809). However, NOTHING enforces this: the reconciler has zero 'foreign' references (grep confirmed), its docstring asserts the opposite ('same-clientId orders only', :565-567), ibkr_client.py:186-188 asserts openTrades() cannot see foreign orders, and the protection exists only as an undocumented cache side effect of a call made for the executor's benefit. Mode B is real but bounded: a fill between startup reconcile and the 30-min sweep age gate (STALE_PENDING_MINUTES=30, reconciler.py:83) is seen by nothing (check_fills iterates the empty in-memory dict, executor.py:856) until the 5-min-cadence sweep (orchestrator.py:3151) promotes it — a ~20-35 min unmonitored window that is the designed rescue ladder, not the claimed fiction.

**Consequence:** Currently blocked by an ib_insync cache behavior the code's own comments deny; any change (stashing via a throwaway connection, an ib_insync/ib_async upgrade altering cache population, or 'fixing' the docstring's assumption) silently re-opens the R16 fiction: $0 'stale_pending_never_filled' booked while a live DAY order rests, then an untracked fill and HALT_UNTRACKED.

**Suggested fix:** Have the sweep consult authoritative broker truth explicitly: pass the connect-time foreign stash / a fresh reqAllOpenOrders snapshot (prune_foreign_open_orders already exists on the client) into _cancel_working_entry_order, and add a test pinning that a foreign-clientId working order defers the $0 booking.

## money-flow-05  [ENFORCEMENT_GAP | live_money]

**Proof:** Currently impossible, and nothing guards the blocking assumption. Blocked today: config.yaml:38 max_contracts_per_trade: 1 (comment invites raising it for volume) enforced as the hard cap in position_sizer.py:156/171, so quantity is always 1 — the ladder gate (portfolio.py:747, quantity > 1) never fires and an exit-BAG true partial (executor.py _book_partial_exit:2231-2256 requires 0 < filled_units < quantity) cannot occur; DB probe confirms today's identity sum(daily_stats.total_pnl) == sum(trades.realized_pnl) == -$121.46. Once qty>1, each fork link behaves as claimed: state.py:614-642 record_partial_exit writes ONLY open_positions.partial_exits JSON; state.py:407-411 close_trade DELETEs that row (history destroyed); the ladder path (orchestrator.py:2419-2442) books the slice into daily_stats.total_pnl + breaker.record_partial_pnl but never trades.realized_pnl; the final close books only the remaining slice as the whole trade's P&L (executor.py:2209/2213 via _calculate_realized_pnl:2264-2292 on the reduced trades.quantity — update_trade_quantity state.py:672-682); the commission true-up (orchestrator.py:494-496) computes commission_estimate on the reduced trades.quantity (executor.py:2294-2308) minus total_commission over ALL execution rows (state.py:798-804, entry legs at original qty + partial legs), pushing realized_pnl further from the account's round trip; all verdict surfaces (master.py:1266, status.py:162/209, analytics, Thompson at 563-567, meta-label) read the forked trades.realized_pnl. No guard anywhere refuses partials at qty>1 or reconciles partial_exits into the close. ONE CORRECTION to the finder's trigger: a 2-lot CONDOR cannot take the ladder partial — orchestrator.py:2398-2410 refuses multi-leg ladder partials outright (only contract_type call/put reaches the booking branch at 2343). For condors the fork instead runs through the executor exit-partial-fill path, where the slice P&L is never appended to completed_exits — so it reaches NO ledger at all (daily_stats/breaker miss it too; trades and daily_stats then agree with each other while both misstate the account's round trip). The two-irreconcilable-ledgers divergence (daily_stats vs trades) arises via the ladder path for single-leg strategies (CSP/covered_call) at qty>1. Either path corrupts the verdict sample's per-trade P&L unrecoverably.

**Consequence:** One config edit (max_contracts_per_trade > 1 — the documented Phase-2 'learning volume' intent) arms silent P&L forking: a multi-contract trade that scales out books a trades.realized_pnl (and PF/win-rate/meta-label/Thompson input) that is not what the account realized on the round trip, daily_stats and trades diverge by every ladder slice, and close_trade's DELETE destroys the only record that could reconcile them.

**Suggested fix:** At close_trade time, fold partial-exit history into the final booking: read open_positions.partial_exits before the DELETE, add the slices' P&L into the trade's realized_pnl (and archive the JSON onto the trades row), make the ladder path stop double-feeding daily_stats for slices once trades carries the full number, have the executor partial path also feed daily_stats/breaker (or route slices through _process_completed_exits), and base commission_estimate on the ORIGINAL entry quantity (persist it) rather than the mutated trades.quantity. Until then, add a startup assertion refusing max_contracts_per_trade > 1.

## trade-life-partial-slice-pnl-erased  [ENFORCEMENT_GAP | live_money]

**Proof:** Every code link holds: executor.py:2231-2247 (true-partial branch: record_partial_exit + update_trade_quantity + CLOSING->FILLED, and unlike the full-close branch at 1129-1141 nothing appends to completed_exits, so stats/breaker never see the slice on this path); state.py:614-642 (the slice's pnl lives ONLY in open_positions.partial_exits JSON); state.py:407-411 (close_trade DELETEs that row in the same transaction, and the DuckDB dual-write ingests the trades row with remainder-only realized_pnl); executor.py:1098-1114 and 2264-2282 (final realized_pnl computed on trade.quantity, which 2243 reduced); orchestrator.py:2419-2442 (single-leg partial books stats/breaker but never touches trades.realized_pnl); orchestrator.py:494-500 with state.py:798-804 (true-up delta = commission_estimate(remainder qty) − SUM(commission) over ALL executions — entry full-qty + partial-exit + final-exit legs — so the whole trade's commissions are charged against the remainder-only P&L). Master.py:1265-1296 scorecard PF/realized/commissions read trades.realized_pnl/commission. The sequence is currently IMPOSSIBLE, and nothing guards it: config.yaml:38 max_contracts_per_trade=1, every strategy emits quantity=1 signals (orchestrator.py:1544-1546), manager.py:609 caps position_size at min(request.contracts=1, ...), and the only path to adjusted_size>1 — orchestrator.py:2035 max(1,int(1×strategy_mult)) — is inert because config.yaml:193 paper_trading_mode=true makes the adaptor return 1.0 (adaptor.py:72-79,135-144); multiplier ceiling is 2.0 (adaptor.py:50), so even active learning arms this. No assert, refusal, or ledger-merge protects the qty>=2 case.

**Consequence:** The day sizing exceeds 1 contract (Phase-2 scaling or active learning multiplier at its 2.0 ceiling), every partially-scaled trade permanently loses the closed slice's P&L from trades.realized_pnl and double-charges its commissions — go-live scorecard PF, win rate, and realized totals (the verdict inputs) are wrong on exactly the scaled trades the verdict is about, and executor-path slices also never reach daily stats or the breaker.

**Suggested fix:** On close_trade, merge SUM(partial_exits.pnl) into the final realized_pnl (or persist partial exits to a durable table and have the scorecard sum trade+partials); make executor._book_partial_exit append a partial event that the orchestrator books like _execute_partial_exit does; base the commission true-up on original entry quantity, not the reduced remainder.

## trade-life-promotion-ignores-broker-quantity  [ENFORCEMENT_GAP | live_money]

**Proof:** All links verified: reconciler promotions adopt the local row as-is — PENDING->FILLED (reconciler.py:932-947), CLOSING->FILLED (977-990), sweep promotion (796-808) all call _ensure_open_position_row which writes quantity from the LOCAL trade (85-106); grep confirms reconciler.py contains zero update_trade_quantity calls (only orchestrator.py:2429 and executor.py:2243/2369/2391). The quantity mismatch IS computed (991-1000) but only does discrepancies.append + log.warning — report-only. position_liveness (208-276) is a pure key-set comparison ('present == keys' at 239-241 and 271-273) with no quantity term, so a 1-of-2 position answers 'live'. The exit then reverses trade.quantity combo units (orchestrator.py:2831-2833 and 2859-2861: OrderBuilder.combo_limit(quantity=trade.quantity)), closing what exists and opening the difference inverted — the incident class the in-session path fixed via orderStatus.filled (executor.py:2310-2369, R12 F4.4), which the reconciler path has no equivalent of. Currently impossible: every quantity is 1 (config.yaml:38, signal qty=1, sizing min() cap, adaptor inert under paper_trading_mode — same dormancy chain as the partial-slice finding), and a 1-lot guaranteed combo fills atomically. Nothing guards the assumption: no quantity adoption, no refusal to promote on mismatch, no liveness quantity check.

**Consequence:** With qty>=2 sizing: a combo order that partially filled while the process was down is promoted to FILLED at the ordered quantity; the later exit reverses the full local quantity — one unit closes the real position and the surplus units OPEN an untracked inverted structure at the broker (the R12 F4.3 reversal incident class, resurrected on the restart path).

**Suggested fix:** During promotion, reconcile quantity against the broker: read the matched legs' IBKR position size and call update_trade_quantity (and fix open_positions) to the broker figure before promoting; make position_liveness return 'partial' when leg quantities are below trade.quantity; refuse CLOSING->FILLED promotion on a quantity mismatch until adopted.

## bot-day-07-commission-trueup-replay-nonidempotent-crash-window  [CONFIRMED | research_validity]

**Proof:** orchestrator.py:2916-2934: _delta = round(TradeExecutor.commission_estimate(_t) - _real, 2) where commission_estimate (executor.py:2295-2308) is a pure function of static trade fields (0.65 * legs_per_side * qty * 2) and _real = total_commission(_tid) is static once the day's reports landed — the delta does NOT depend on whether it was already applied. The three writes are separate sqlite statements in separate connections with no transaction: update_trade_realized_pnl is a bare UPDATE (state.py:837-842), update_daily_stats a second write (:2926-2928), delete_state(f'trueup_pending_{_tid}') a third (:2931); the marker is set at booking (:504-509). A hard kill between :2925 and :2931 — or a caught exception in delete_state itself (the per-trade except at :2932 swallows it) — leaves the marker, and the next _post_market (same evening via bot-day-06's rerun, or tomorrow's 16:00, since guards _real>0 / realized_pnl is not None / |delta|>=0.01 all still pass) applies the identical delta again: realized_pnl ends at P+2*delta permanently, and daily stats absorb the delta a second time (on the WRONG day if replayed tomorrow). Corruption flows to every trades.realized_pnl consumer including the Friday scorecard (master.py:1262-1343), PF/win-rate, and the learning cycle. Note: magnitude is bounded at the commission-estimate error (~$1-5/trade) and requires a crash/failure inside a sub-second window, hence the severity call.

**Consequence:** A crash (or failed delete) between the P&L write and the marker delete permanently double-applies the commission delta on that trade and double-adjusts daily stats — a small but unrepairable drift in the exact P&L records the go-live scorecard, PF/win-rate, and learning read.

**Suggested fix:** Make the settle idempotent: perform pnl update + stats update + marker delete in ONE sqlite transaction; or store the applied delta in the marker/trade row (e.g. set trades.commission to _real in the same statement) and skip when already recorded.

## money-flow-03  [CONFIRMED | research_validity]

**Proof:** Every cited numerator site verified as a peak-to-trough loop over CUMULATIVE REALIZED P&L of closed trades only: master.py:1280-1284 (weekly scorecard, over status='closed' rows ordered by exit_time), status.py:219-222, analytics.py:345-361 (_calculate_drawdown over closed pnls + capital base), duckdb_analytics.py:378-393, scripts/shadow_referee.py:188-194, backtesting/result.py:82-93 (equity += t['pnl'] per closed trade) and backtesting/walkforward.py:314-325 (same convention) — so research DD numbers share the blindness. The intra-hold chain persists ONLY favorable excursion: state.py:562-568 update_high_water_mark is MAX-only; close_trade stamps peak_pnl_pct from the HWM (373, 389-393) then DELETEs the open_positions row (407-411); update_position_mark (570-585) overwrites with no history; repo-wide greps for low_water/adverse/MAE across src/ and scripts/ find zero persistence sites (only unrelated rvol_mae in walkforward and prose in circuit_breaker); daily_stats.max_drawdown is a schema column nothing ever computes (grep: only the dataclass default, DDL, and pass-through writes). Gate text: PLAN.md:495-499 'max drawdown < 8% of deployed risk'; the denominator's own rationale (master.py:1229-1231, 'what was actually at risk when the loss happened') describes at-risk-during-the-hold while the numerator can only see post-realization equity. DB probe reproduces the executed evidence: IC closes in exit order cum to peak 452.89 then -272.29, maxDD 272.29 == exactly the single QQQ short_strike_touch loss. The trigger is then arithmetic: two condors riding to -60% of deployed risk unrealized and recovering to +40% TP contribute two wins and $0 to every maxDD above, and with MAE never written the honest number is unrecoverable from the DB after close.

**Consequence:** The go-live gate 'max DD < 8% of deployed risk' can read PASS over a sample whose account rode intra-hold excursions many times the gate threshold; because only MFE survives (trades.peak_pnl_pct) and per-position mark history is destroyed at close, the ridden drawdown cannot be retro-computed, and backtest DD numbers sharing the closes-only convention make research-vs-live agreement mask the gap rather than check it.

**Suggested fix:** Persist a low-water mark alongside the HWM (state.py: update_low_water_mark MIN-only, stamped onto trades as trough_pnl_pct at close, exactly mirroring peak_pnl_pct), and report an MAE-aware DD line (e.g. max per-trade adverse excursion vs deployed risk) next to the realized-equity DD on the scorecard; alternatively log a daily NLV/unrealized equity curve and compute gate DD from it.

## research-to-live-01  [CONFIRMED | research_validity]

**Proof:** Every link executed and verified. (1) Probe of data/historical.db: SPY/QQQ/IWM each hold EXACTLY 60 distinct 5m sessions (2026-06-01..2026-08-25); orchestrator.py:1063-1075 appends one session per trading day (get_intraday_since -> save_intraday -> cleanup_old_intraday(keep_days=730)). (2) Executed store.resample_to_daily(sym, days=4100) today: 59 rows, last=2026-08-24 — the drop-today guard (historical.py:486-495) removes 08-25. (3) market_data.py:89 'if len(df) < 60:' -> Yahoo full-history fetch; else :100-101 returns ONLY the IB resample. Today 59<60 keeps Yahoo alive by one row; tomorrow (2026-08-26) the 08-25 session stops matching 'today', resample returns 60, 60<60 is False, and every load_daily_ohlcv call returns the ~60-row IB frame — this flips by pure calendar even if the bot never scans again. (4a) walkforward.py:2667 'if df is not None and len(df) > 100' silently drops all symbols (no else, no span check); run() then hits :903-905 log.error('no_data_for_backtest') and returns an empty result. (4b) ablation_confirm.py:95-98 (days=4000, accepts any non-empty frame), touch_stop_impact.py:102, range_floor_study.py:103, shadow_round3_gated.py:91 preload and pass to run(data=data); _generate_windows (walkforward.py:2625-2649) with train 365 + gap 5 + test 63 = 433 days > the frame's ~85-day span breaks on the first iteration -> ZERO windows -> n=0/windows=0 JSON written as a completed study (ablation_confirm.py:69-84). (4c) optimizer.py:530-532 requires only _MIN_BACKTEST_ROWS after tail(); once >100 IB sessions accumulate (~Oct 2026) walkforward accepts the frame and an '11-year' study silently measures ~5 months. Also walkforward.py:942-948 loads its SPY context via the same load_daily_ohlcv, so the context frame collapses too. No requested-vs-received span check exists at any consumer.

**Consequence:** From 2026-08-26, every research entry point silently loses its multi-year Yahoo history: walkforward runs die with a misattributed 'no_data_for_backtest', preloading study scripts write n=0 JSONs as completed studies, and from roughly late-Oct 2026 (>100 stored sessions) '11-year' confirm/ablation runs complete normally while actually measuring ~5 months of the calmest recent regime — the same vacuous-evidence failure class as the 08-03 ablation that previously flipped a live gate.

**Suggested fix:** In load_daily_ohlcv, fall back to Yahoo (or merge) whenever the IB resample's earliest date is materially later than the requested start (span check, not row-count check), and log source+span at info level; make walkforward._fetch_data and the study loaders fail loudly when received span < requested span instead of silently dropping or accepting.

## research-to-live-05  [CONFIRMED | research_validity]

**Proof:** All links verified with executed DB probe. Probe: SPY daily_prices.implied_vol has 508 rows spanning 2024-07-10..2026-08-25, continuous to 2026-07-09 (0.12756755), ZERO rows 2026-07-10..2026-08-16, resuming 2026-08-17 (0.11715176) — the 38-day hole over the wide-wing epoch (minor finder slip: 2024-03-26 is the OHLCV start; IV rows start 2024-07-10 — immaterial). Two writers, two definitions, one column: the backfill stores IBKR OPTION_IMPLIED_VOLATILITY bar CLOSES (backfill_historical_data.py:300-337, historical.py:98-102 documents these as IV-OHLC bars of IBKR's 30-day standardized index) into daily_prices.implied_vol via save_daily_iv (historical.py:252-291); the live self-heal stores the MEDIAN of chain atm_iv across the fetched expiry band (orchestrator.py:3280-3287) into the SAME column. Every research frame joins the column (market_data.py:106-127, both IB and Yahoo paths), and walkforward's test_with_context (walkforward.py:1324) retains it into the engine. Asymmetric freshness: engine._get_iv priority 1 (engine.py:1351-1357) takes hist['implied_vol'].dropna().iloc[-1] with NO age bound — for every simulated bar in 07-10..08-16 it returns the frozen 07-09 value for up to 38 days, bypassing the R16-calibrated per-symbol VIX proxy at :1375-1389 (whose own comment cites 2026-08-11 VIX 15.6 vs VXN 22.9, the exact day the frozen value most understates QQQ IV); the live sibling consumer of the identical series enforces a 5-day gate (orchestrator.py:3315-3323) and falls back to the proxy. And _estimate_iv_rank (:3305-3330) now ranks a median-ATM head against a 252-row window that is ~97% 30-day-standardized backfill rows — a percentile across two instruments feeding the live iv gates. Tests: test_iv_model.py covers the _get_iv priority chain but sets implied_vol constant and never tests age; grep of tests/ finds no staleness/continuity/provenance check for implied_vol.

**Consequence:** Any backtest or sim-vs-live divergence study whose windows cover 2026-07-10..08-16 — the live trading record's own period and the first place divergence is meant to be checked — silently prices every condor credit, wing width, and spread-model input at a five-week-old frozen IV instead of the calibrated proxy those same audits declared the honest fallback, understating credits exactly where engine fidelity was claimed fixed; separately every live iv_rank since 08-17 is a percentile computed across two different IV definitions, quietly biasing the 85th-percentile IV cap and iv_floor gates.

**Suggested fix:** Give engine._get_iv the same freshness bound as _estimate_iv_rank (if the last non-NaN stored IV is more than ~5 trading days older than the simulated bar, fall through to the VIX proxy); tag stored IV rows with a source column (backfill|chain_median) and either normalize the self-heal estimator to the 30-day-standardized definition or compute iv_rank percentiles within a single source; add a store-continuity check (max gap in the trailing 252 sessions) to the data validators.

## trade-life-entry-vix-refetch-defeats-lkg  [CONFIRMED | research_validity]

**Proof:** Every link verified: orchestrator.py:1976-1981 fetches current_vix via _get_vix_lkg (1680-1696: fresh fetch, then <=45-min last-known-good, else None fail-closed — the R7 hardening) for the risk gates; orchestrator.py:2107 then performs a second, independent fetch inside save_trade_context — 'vix=await self._market_data.get_vix() or 0' — the exact 'or 0' pattern R7 removed, with current_vix in scope and unused. market_data.py:319-362 confirms get_vix is uncached network on every call (fresh reqMktData snapshot then Yahoo in a thread), so the second call can fail seconds after the first succeeded. entry_vix=0 is then permanent in trade_context (state.py:171,718 dual-write), and the thesis vix_spike branch is disarmed forever by it: orchestrator.py:3130 'if vix and entry_vix and entry_vix > 0' short-circuits on 0. The consequence-latency caveat is also correct: 3090-3091 exempts iron_condor/iron_butterfly from the whole thesis check and config.yaml:82-90 shows the live book is IC-only, so the disabled vix_spike exit is armed only for re-enabled directional/strangle strategies — but the entry_vix=0 rows are written on today's live IC entries and poison trade_context/meta-label training data regardless.

**Consequence:** A transient VIX fetch failure in the seconds between validation and context-save records entry_vix=0 permanently: trade_context/meta-label training rows carry a false zero for a real feature on live trades today, and the vix_spike thesis exit is silently disabled for the position's whole life once any thesis-checked strategy is re-enabled.

**Suggested fix:** Pass the already-fetched current_vix into save_trade_context (vix=current_vix or 0) — one-line change; optionally have _check_thesis_valid treat entry_vix<=0 as 'unknown, re-resolve from LKG' rather than silently skipping.

## trade-life-gatesoff-reintroduces-neutral-autoreject  [ENFORCEMENT_GAP | research_validity]

**Proof:** The chain is real in code: neutral_only = prediction.confidence < min_confidence (orchestrator.py:1142; threshold 0.50 via config.yaml:49, adaptor override inert in paper mode); the ONLY writers of model_overridden sit inside 'if self._settings.ml.entry_gates_enabled:' (1396-1400 range, 1420-1424 vol-mag); the neutral-only filter keeps IC/strangle signals when gates are off (1444-1450 'or not ...entry_gates_enabled'); eff_conf then falls back to final_confidence (1494-1498) because model_overridden is empty; and manager.py:351-355 rejects any request.confidence < 0.50 (TradeRequest gets eff_conf at orchestrator.py:1989). The R16 comment 1391-1395 states the flag's purpose ('With gates off, ML observes; never vetoes') and the comment at 1486-1493 records that eff_conf override exists exactly because neutral-only confidence 'would have auto-failed the risk manager's own confidence gate' — the gates-off branch disables the fix while keeping signals flowing into the still-armed gate. One overstatement in the claim: it is not literally 100% — final_confidence = prediction.confidence + MTF boost (−0.10..+0.15, multi_timeframe.py:80-100) minus fractal penalty, so a sub-0.50 prediction with full trending alignment can clear 0.50; but that makes the surviving entries precisely the trending-aligned ones, reinforcing the adverse selection (condors enter only in their worst regime). Currently impossible: entry_gates_enabled defaults True (settings.py:294) and is absent from config.yaml (grep confirms), so the veto-move fires only after an operator flips the flag; nothing guards the flag's advertised observe-only semantics (no test, no gate-2 bypass, no warning).

**Consequence:** Any gates-off ablation or observe-mode run silently vetoes iron-condor entries in exactly the low-directional-conviction range regime the strategy targets (rejected per-signal as ordinary 'confidence 0.4x < min 0.5' lines), while trending days still enter — the ablation arm measures a systematically adversely-selected book, and if run live it takes condors only in their worst regime.

**Suggested fix:** When ml.entry_gates_enabled is false and neutral_only is set, exempt neutral strategies from the risk manager's min_confidence gate (e.g. pass a neutral flag on TradeRequest, or floor eff_conf at min_confidence for neutral strategies with gates off), mirroring the model_overridden exemption the gates-on path already has.

## bot-day-01-master-death-leaves-unsupervised-bot-and-green-deadman  [CONFIRMED | ops_reporting]

**Proof:** Every link holds. keeper_ait.bat:43-56: the guard exits 0 (alive branch) when EITHER 'run_orchestrator' OR 'ait\.main' matches; relaunch happens only on errorlevel 1 (neither matches), and the dead-man curl (:53-55) sits inside the alive branch — so a master-only death with a surviving bot logs 'orchestrator alive' and keeps pinging healthchecks.io. start_bot.bat:14-18 uses the identical regex and 'exit /b 0' with no alert, so the 07:30 task silently refuses. master.py:219-221 confirms an orphaned ait.main survives a master death (no Job object). The advertised defense (keeper comment :40-42 'BotManager refuses ... and alerts') lives only inside BotManager.start() (master.py:352-371), which requires a running master — unreachable while both launchers refuse. Grep confirms data/bot_heartbeat is read only by master.py:494/1168 and scripts, so the sole hang detector (master.py:494-507), the 2-min crash restart, fresh-models restart, Telegram-dead probe (:1599), 17:00 backup, digests and 07:30 retrain (master.py:1601-1714) are all dead with the master. No other automated path in the repo starts run_orchestrator.py.

**Consequence:** After the machine's documented python-killer takes only the master, the bot trades unsupervised for hours-to-days with a green external dead-man; a bot hang in that window (the R5-D1 class) leaves stops/TPs silently dead with no detector and no alert, and missed 07:30 retrains arm the bot-day-03 kill loop.

**Suggested fix:** Split the keeper's check: ping the dead-man only when 'run_orchestrator' itself matches; when only 'ait.main' matches, log ORPHAN-BOT, stop pinging (so healthchecks.io fires), and/or curl the check's /fail endpoint. Make start_bot.bat's refusal alert instead of exit /b 0.

## bot-day-02-deadman-ping-attests-supervisor-liveness-not-bot  [CONFIRMED | ops_reporting]

**Proof:** keeper_ait.bat:43-55 pings whenever the master process matches, regardless of bot state. All three sustained bot-down-with-master-alive states verified: (a) master.py:575-584 — bot dead + port closed -> 'bot_restart_deferred_gateway_down' and return, indefinitely, no budget (and this path never calls ensure_gateway, so a crashed Gateway is never auto-restarted from it); (b) :595-602 give-up returns with the bot dead until the 30-min reset (:592-593); (c) _verify_launch (:432-443) + the 2-min health loop retry a <8s-exiting child forever. In every case the master survives, so the keeper pings green — the advertised 'or bot down' contract (keeper_ait.bat:49-52) is false whenever bot and master die separately, which is always (separate processes). The only cover is _alert (master.py:64-105), which returns False silently without Telegram; the TELEGRAM_DEAD recovery probe (:1522-1548) itself needs Telegram. data/bot_heartbeat exists and is consumed by master.py:494 and scripts/check_first_rth_liveness.py:62, but the keeper pings on process-table match alone.

**Consequence:** Telegram-dead + gateway-down (or a bad deploy) produces a fully silent multi-hour market-hours outage with live positions and no exit engine running, while the one external monitor built for dead alert channels reports green every 90s.

**Suggested fix:** Gate the dead-man ping on bot liveness, not process existence: keeper checks data/bot_heartbeat freshness during RTH (or at minimum requires BOTH run_orchestrator and ait.main to match) before curling; on bot-down, hit the healthchecks.io /fail endpoint.

## bot-day-06-postmarket-eod-pipeline-reruns-on-crash-restart  [CONFIRMED | ops_reporting]

**Proof:** _post_market (orchestrator.py:2892-3054) read in full: no date/ran-today marker anywhere — it re-runs true-ups, reconcile, EOD RECON Telegram (:2975-2980), learning cycle (:2985-2989), prestamp, and DAILY SUMMARY (:3053) on every entry. scheduler.py:43-65: POST_MARKET = close -> close+15min, so any bot boot in that window enters _post_market from the phase loop (run() :348-349). Grep confirms _in_post_market_window is consulted at exactly ONE site: master.py:527, inside the fresh-models-marker branch; the bot-down restart path (:565-612) starts the bot with no window check, and main()'s bot.start() (:1588, the keeper-relaunch path) is unconditional. master.py:294-300's own comment states the bot re-runs the whole EOD pipeline 'with no already-ran-today guard' when a restart lands in the window — the deferral was applied only to the marker path. A 16:03 crash restarted at ~16:04-16:06 (models fresh -> fast startup) therefore re-runs the full pipeline, and a crash mid-pipeline re-runs it from the top.

**Consequence:** Any bot crash between ~15:58 and ~16:13 (the known python-killer's window is all day) produces a duplicate EOD RECON + DAILY SUMMARY page, a second reconcile, a second learning cycle (up to double the daily adaptation budget), and a second pass over the commission true-up loop — the same R13 duplicate-page class observed on 5 of 8 sessions before the partial fix.

**Suggested fix:** Add the guard where the master comment says it is missing: a bot_state key eod_ran_<date> checked/set at _post_market entry (idempotent across restarts), or consult _in_post_market_window in BotManager's bot-down restart path as well.

## research-to-live-02  [CONFIRMED | ops_reporting]

**Proof:** All links verified against code. results.py:93-98 _PARAM_MAP = {min_confidence, stop_loss_pct, trailing_stop_pct, breakeven_trigger_pct}; param_spaces.py:39-58 IRON_CONDOR_SPACE = {delta_short, max_hold_days, wing_k, iv_rank_rise_threshold, min_edge_over_baseline} + 3 FRACTAL_GATE params — intersection is EMPTY, and the space's own comment (:26-33) says stop/profit/min_confidence are deliberately frozen out of the IC search. Optimizer keys are 'iron_condor__<name>' (optimizer.py:382); apply_to_config partitions on '__' and 'silently skipped' is its own docstring at results.py:88 — so for an iron_condor-only run applied={} always. results.py:100-116 then UNCONDITIONALLY yaml.safe_load + open(path,'w') + yaml.dump — verified config.yaml is 232 lines of which 176 carry comments (e.g. :134 region 'credit_loss_limit: 0 # 0 = DISABLED (R6/R12-B1...)', the dte-band and VIX-tier decision records) — all destroyed even when zero params were applied. run_optimizer.py:137-148: with AIT_ALLOW_APPLY=1 (the documented opt-in; usage line :32 shows --apply) it calls apply_to_config then prints 'Best params written to: config.yaml' with no check that `applied` is non-empty. Grep of results.py/run_optimizer.py/tests: nothing verifies config.yaml against the winning trial post-apply. Registered findings (live-bt-mirrors-07/08) concern export_production_params' dead key and the stop_loss_pct mapping semantics — neither the empty-apply for the live arm nor the comment destruction.

**Consequence:** An operator running the documented apply flow for the sole live strategy gets zero parameters written, loses every decision-record comment in the single R19c operating-authority file (the annotations that distinguish deliberate values like credit_loss_limit:0 from typos), and is told 'Best params written' — live trades old params while the operator believes the winning trial is deployed.

**Suggested fix:** In apply_to_config: skip the file rewrite entirely and print/log an explicit 'NOTHING APPLIED — no searched param maps to a config field' when the applied set is empty; use a comment-preserving round-trip (ruamel.yaml) or targeted line edits for real applies; have run_optimizer print the applied dict and diff, and add a post-apply assertion that each applied key reads back from config.yaml.

# Critic: never-audited surfaces, severity elevations, interaction chains

- NEVER-AUDITED — IBC/TWS session lifecycle: C:\IBC (config.ini, 'Start TWS (autorestart).xml' scheduled task, Restart/EnableAPI/ReconnectAccount .bats) appears in zero findings across both rounds, yet every quote and order path sits on top of its daily auto-restart window, auto-relogin, and paper-vs-live port selection. Neither hunt checked the bot's reconnect/watchdog assumptions against IBC's actual restart schedule or what happens to open-position monitoring during the nightly TWS restart.
- NEVER-AUDITED — paper-fill realism of the go-live evidence itself: the 50-IC-close gate is proven entirely on IBKR paper fills on a delayed feed. model-vs-reality compared research vs the paper broker, but no finding examines paper-vs-real execution (optimistic combo fill model, no queue/size interaction), i.e. whether the sample the whole go-live decision rests on measures anything about live fills.
- NEVER-AUDITED — broker-statement P&L parity: no trade-by-trade reconciliation of booked realized_pnl against IBKR's own statements/Flex reports exists in either round. Only commissions were cross-checked (model-vs-reality-commission-constant). Given the 2026-06 audit found P&L accounting broken (real perf ~ -$29k), booked-P&L ground truth for the 23 closes feeding every scorecard remains unverified.
- NEVER-AUDITED — feature/label lookahead in the ML pipeline: features.py and the walkforward label construction were audited for units, scale, horizon and mirror parity, never for leakage (same-bar close in features, label windows overlapping training windows, retrain-timing contamination). A lookahead bug would inflate every direction/range/meta OOS stat both rounds treat as the reference authority.
- NEVER-AUDITED — scripts/backfill_historical_data.py, backfill_intraday.py, calibrate_option_spreads.py: named under-swept by round 1's critic and still absent from round 2. They write the production intraday store and the engine cost model that live-bt-mirrors-04, the spread calibration, and the retention-growth finding all depend on.
- NEVER-AUDITED — deprecated/ subtree (src/, scripts/, configs/, ops/): round-1 critic flagged that nothing verifies live launchers/cron don't invoke deprecated/ops or import deprecated/src, and no round-2 class touched it either. Zero findings reference it.
- NEVER-AUDITED — test-fixture DDL drift: 5 test files hand-write CREATE TABLE vs state.py's real schema/migrations (round-1 critic item; no round-2 class covers it). test_duckdb_analytics already proved the failure mode by encoding the wrong iv_rank scale and passing green.
- NEVER-AUDITED — early-close (13:00 ET) sessions: early_close handling exists in scheduler.py/utils/time.py/historical.py but no finding in either round exercises a half day — EOD flatten timing, fast-monitor and staleness windows, 'today activity' log windows, and deadman-ping duty cycle on half days are all unexamined.
- NEVER-AUDITED — config.yaml as a multi-writer file: run_optimizer --apply / export_production_params write config.yaml while keeper+bot may be running. Round 2's multi-process class swept only the DBs; YAML write/reload races and torn partial writes were never audited.
- NEVER-AUDITED — host/ops substrate: disk growth (2-year intraday retention plus log rotation), existence/testing of any ait_state.db backup (fail-direction-10 shows what an unreadable DB does; nothing audits whether a backup/restore path exists), Windows Update reboots vs keeper, and the root CAUSE of the recurring c0000005 crashes (log-contracts-3 audited only their reporting).
- SEVERITY — multiprocess-db-1 understated at ops_reporting: a dashboard duck open that blocks the bot's 16:00 close booking corrupts the live trades book, not a report — the deferred/failed close write is exactly the closed-at-broker/open-locally divergence that feeds reconciler $0 sentinels and the 50-close sample. Should be live_money-adjacent.
- SEVERITY — dead-surface-2 understated at ops_reporting: permanently zeroing the only fill-quality ground truth (live_mid/signal_price/nbbo_spread) makes the confirmed cost-model errors (commission constant, 10x-flat skew) unfalsifiable from live data. It destroyed go-live evidence irreversibly; at least research_validity.
- SEVERITY — model-vs-reality-commission-constant and touch-gap-pricing arguably transcend research_validity: ~$2.13/contract x 4 legs plus gap-day loss understatement is the scale of the whole per-condor expectancy, so together they can flip the sign of the researched edge that authorizes go-live. For a book whose only purpose is generating go-live evidence, these gate real money as directly as the live_money items.
- SEVERITY — log-contracts-5 understated at ops_reporting: silently killing the keeper-supervised bot (or the integration test) while condors are open leaves touch-stops/TPs unmanaged on live positions — unmanaged open risk, not a reporting defect.
- SEVERITY — policy-vs-impl-5 understated at ops_reporting: the only surface computing the R19d go-live verdict cannot evaluate 2 of gate 1's 5 criteria. A go-live authorization gate that structurally can't answer its own criteria is a live_money defect (it can authorize real money on an incomplete verdict).
- SEVERITY — register hygiene: round-1's own critic elevated string-contracts-1/-4 (fabricated $0 rows poisoning scorecard inputs and meta-label training) and db-contracts-1 (false 0% direction accuracy scheduled to write a live stop_loss override via adaptor.py), but the register still carries them at research_validity/ops_reporting. The severities were never restated after the critic pass.
- INTERACTION — common-cause fail-open entry chain: fail-direction-05 (IV outage fabricates iv_rank 50, passes both IV gates) + fail-direction-06/external-contracts-02 (quoteless strikes rank as the MOST liquid) + fail-direction-07 (combo NBBO sanity fails open, marketable limit anchored to stale quotes). One degraded-feed event defeats the regime gate, the liquidity gate, and the price-sanity gate simultaneously, so the bot preferentially enters, on the worst possible strikes, at stale prices, exactly during data outages — far worse than any single fail-open.
- INTERACTION — concurrency-1 x external-contracts-03 (+ log-contracts-3): the real multi-leg short-premium position booked '$0 never filled' is untracked by every exit monitor; the only surface that would flag it is the startup-reconcile/discrepancy Telegram burst — precisely the burst the 429 Retry-After bug drops — while status CLI/dashboard stay green. The recovery channel for the worst state-corruption finding fails under the same conditions that produce it.
- INTERACTION — concurrency-2 x fail-direction-10: the 3-loss circuit breaker is defeated from both ends — stop-out losses that never reach record_trade_result (never counted) and an active pause/streak silently cleared by a relaunch while the DB is briefly unreadable (never persisted). Together the worst-day halt effectively cannot fire on exactly the crash-loop, repeated-stop days it was built for.
- INTERACTION — go-live denominator squeezed from both sides: string-contracts-1/-4 + concurrency-1 pad the 50-close sample with fabricated $0 'closes' (and 43% of the meta negative class), while units-scale-05's over-strict 30d/5% live gate and fail-direction-01's silent VIX full-stops starve it of genuine entries biased toward calm regimes; policy-vs-impl-3 then auto-satisfies the checklist at the 50th row. The gate can trip on a sample that is simultaneously inflated, unrepresentative, and smaller in real closes than policy assumes.
- INTERACTION — under-$25k launch stack: time-authority-4 (local-clock entry_time stamp silently undercounts PDT day trades) x fail-direction-09 (PDT guard fails open on corrupt state/empty calendar) x model-vs-reality-margin-model-unimplemented (documented ~$2.1k launch NLV). At the stated launch account size PDT protection is defeated by two independent mechanisms on the account class where a violation triggers broker restriction, with margin simultaneously unmodeled.
- INTERACTION — live-bt-mirrors-09 x model-vs-reality-margin-model-unimplemented: at $2.1k NLV the live budget cap builds $1-wide wings no backtest or study has ever priced, while margin/AvailableFunds behavior is also unmodeled — go-live day deploys a structure and a capital model that the entire 50-close paper evidence base never exercised, compounding rather than merely adding.
- INTERACTION — multiprocess-db-1 x string-contracts-4: the dashboard-blocked 16:00 close write manufactures the closed-at-broker/open-locally divergence whose reconciler resolution books a $0 'closed' sentinel that passes the not-real-close filter — an operator merely viewing the dashboard at the wrong time can create a fake clean close that dilutes PF/win-rate in the go-live scorecard.

# Flow maps (composition round, truncated excerpts)

```
TRADE-LIFE FLOW (trade_v2, one trade end-to-end; all paths read in full):

S1 SCAN TICK — orchestrator._trading_loop (orchestrator.py:735-801): full cycle every scan_interval, fast monitor every 30s (742); scan cadence advances even on failure (776-781). _trading_cycle (803-990): breaker trip gate (819-837) -> risk-manager sync + stale-PENDING sweep (840 -> 3140-3211 -> reconciler.py:722-835) -> portfolio.check_positions (843) -> full exits (846-849) / partial exits (851-856) / thesis re-eval (858-866, _check_thesis_valid 3075-3138, condor-exempt 3090) -> last-15-min block (875) -> check_fills_safe + _process_completed_exits (879-881) -> NLV/tier/universe (883-920) -> hour gate (922-927) -> macro skip (929-937) -> risk-budget refresh + coherence check (940-954) -> VIX + market context (956-959).

S2 FEATURE/PREDICTION — _scan_symbol (1019-1517): hist+iv_rank gather (1034-1056; iv_rank exception -> 50.0), intraday store (1058-1076), data quality (1079-1082), DirectionPredictor.predict (1102-1110), drift record (1117-1122), min_confidence -> neutral_only (1124-1159), regime (1168), MTF boost (1178-79), entry-feature snapshot for trade_context (1181-1200), fractal penalty (1202-1222), earnings proximity abort (1234-1236), meta-label gate (1240-1290; inert: meta_label.enabled=false), chains + liquidity filter (1296-1311), self-healing IV persist (1306 -> 3265-3293).

S3 SIGNALS + RANKING — generate_all_signals + calendar/event straddles (1321-1364), adaptor strategy filter (1367-1370), range-model confidence override for iron_condor/short_strangle gated on ml.entry_gates_enabled (1372-1400), vol-mag override (1402-1424), RANGE_MIN_CONFIDENCE floor (1426-1436), neutral-only filter (1444-1453), Thompson re-rank (1455-1462), defined-risk partition (1471-1473); per-signal eff_c
```

```
A DAY IN THE BOT'S LIFE (trade_v2, all paths verified by reading; anchors file:line):

S0 SCHEDULED START — Windows task AIT-Bot-Start 07:30 runs start_bot.bat (dup-guard :14 matches 'run_orchestrator|ait\.main', exit if either alive) -> run_orchestrator.py:119 main(). In parallel, keeper_ait.bat loops every ~90s (ping-pacing :68-70): rotate keeper.log :24-32; process-table guard :43 (same regex); if NO match -> relaunch run_orchestrator.py :46; if match -> log 'orchestrator alive' :48 and curl the healthchecks.io dead-man URL :53-55 (keeper is the ONLY pinger in the repo; PLAN.md:17 'dead-man ARMED').

S1 MASTER (src/ait/orchestration/master.py) — main() :1555: singleton byte-lock :1559/:161-189; bot.start() :1588; web.start() :1589 (dashboard 8501 with orphan-port clear :654-694/:239-291, weblog 8502 :697-710 and status 8503 :632-651 with NO port clear); jobs: health_check 2-min :1601, retrain cron mon-fri 07:30 :1604, backup 17:00 :1609, scorecard Fri 16:10 :1611, digests 09:35/16:05 :1614-1619; startup catch-ups: backup :1623-1630 (synchronous), retrain date-job +2min :1645-1658, digest :1664-1670, daily report :1677-1683, weekly backtest deferred/pushed past 16:30 :1693-1703; scheduler.start :1721.

S2 BOT LIFECYCLE (BotManager) — start() :347: foreign ait.main scan/refusal :352-371 (_scan_foreign_bot_pids :218-236); ensure_gateway :374-377 (gateway.py auto-starts IBC/Gateway, wait up to 120s); Popen 'python -m ait.main --paper' :409-415; _verify_launch 8s :420-446. health_check() :483: if running -> HANG check :490-509 (data/bot_heartbeat mtime >15min during is_market_open() -> alert + stop()+start(), NO startup grace, NO budget spent); fresh-models marker :510-551 (defer while market open :526 or post-market window :527-539 via _in_post_market_window :294-323, el
```

```
RESEARCH->LIVE PROMOTION PIPELINE (trade_v2, reconstructed 2026-08-25, all paths read + key hops executed):

S1 RAW FETCH — market_data.py: MarketDataService.get_historical:208-242 (Polygon list_aggs:546-610 -> Yahoo _get_yahoo_historical:612-648; Yahoo period HARD-CAPS at "2y":617-629, auto_adjust=False basis per module docstring:1-15); get_intraday:244-279 (IBKR 5m TRADES useRTH -> Yahoo 5m, Yahoo capped 60d); get_intraday_since:281-310 (incremental IBKR); get_vix:319-362. load_daily_ohlcv:62-129 = THE research daily source: IB-store resample first, "if len(df) < 60" Yahoo full-history fallback at :89-99, left-joins stored implied_vol at :106-127.

S2 BACKFILLS — scripts/backfill_intraday.py (IBKR 5m, source="TRADES" at :166; seeded the store 2026-06-01+; probe: SPY/QQQ/IWM = exactly 60 distinct sessions, source '' 06-01..07-07 then TRADES); scripts/backfill_historical_data.py (MIDPOINT-vs-TRADES guard historical.py:361-374); scripts/calibrate_option_spreads.py -> option_spread_params (historical.py:712-761) -> engine._load_calibrated_spreads engine.py:1402-1419.

S3 STORES — data/historical.db: daily_prices OHLCV written nightly by trainer.py:196 store.save (upsert preserves implied_vol, historical.py:182-197; probe: 606 rows/symbol from 2024-03-26); daily_prices.implied_vol written by (a) the retired 2y IBKR OPTION_IMPLIED_VOLATILITY backfill and (b) live scan self-heal _persist_daily_iv orchestrator.py:3265-3291 (median chain ATM IV, once/day) — probe shows backfill ends 2026-07-09, self-heal resumes 2026-08-17 (38-day hole spanning the wide-wing epoch); intraday_prices written every live scan orchestrator.py:1063-1075 (get_intraday_since + save_intraday + cleanup_old_intraday(keep_days=730)) — the LIVE BOT grows this store daily. resample_to_daily historical.py:46
```

```
MONEY FLOW, END TO END (all paths read in full; probes executed against data/ait_state.db on 2026-08-25):

S1 BROKER ACCOUNT VALUES: ibkr get_account_values -> AccountManager.get_snapshot (broker/account.py:166-252; 30s cache account.py:47-49; stale->breaker escalation 150-164; AIT_SIMULATED_CAPITAL scales NLV/BP/AvailableFunds/ExcessLiquidity/Cash 210-229 — MaintMarginReq left unscaled, but get_margin_usage_pct (266-276) has ZERO consumers, so no live effect). NLV consumers: can_afford BP gate (254-264), get_net_liquidation (266-269).

S2 CAPITAL BASE / TIERS: scan cycle caches NLV to bot_state.last_net_liquidation (orchestrator.py:887-896) -> capital_base() authority env>cached NLV>196k default (runtime_env.py:177-221) -> DD%/return% denominators in analytics.py:41-54/345, duckdb_analytics.py:378, dashboard app.py:35-46,506-512. Capital tiers gate universe/strategy menu (orchestrator.py:897-920, risk/capital_tiers.py). Consistent chain (simulated capital propagates coherently).

S3 RISK BUDGETS / PER-TRADE CAPS: per-scan _risk_budget = NLV*risk.max_position_risk_pct (orchestrator.py:945-954; config.yaml:50 = 3%) -> condor width cap (registered R22 live-bt-mirrors-09). RiskManager.validate gates (manager.py:380-634): VIX fail-closed 388-398, VIX-tiered credit cap 412-426, BP gate 6 estimated_cost=max_loss for credit 500-507, per-trade 3% cap 509-517, aggregate 20% cap 519-537 (KV trade_maxloss_* + _structural_max_loss fallback, deliberately gross-of-credit, manager.py:149-198), concentration 6c 539-557, realized-only daily-loss gate 8 -> circuit_breaker.check_daily_loss (572-574; breaker fed ONLY at orchestrator.py:525/2440), sizing (position_sizer.py:58-199; hard cap config.yaml:38 max_contracts_per_trade=1; manager never passes iv_rank -> sizer iv_rank_adj pinned neu
```
