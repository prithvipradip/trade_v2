# R10 Maturity Assessment — external best-practice checklists (2026-07-11)

## TRADING

**Engineering maturity is far above its $3k scale (8 audit rounds, pre-registered gates, real pre/post-trade controls), but the shop-practice gaps cluster in two places: measurement capture exists with zero read-out (TCA, execution benchmarks, exit-reason attribution are all plumbed into SQLite and never aggregated — the go-live gate criterion "stable slippage" is currently unmeasurable), and the last-resort layers have never been exercised (HALT kill switch never drilled, DB restore never rehearsed, dead-man ping never activated, no written benchmark for what PF>1.3 is being compared against).**

- **[PARTIAL] Transaction cost analysis (slippage vs arrival mid, cost attribution per trade)** — Capture layer is genuinely good: trade_v2/src/ait/bot/state.py:210-661 R7 `executions` table stores per-fill exec_id/price/REAL broker commission plus signal_price, live_mid, nbbo_spread; real fill overwrites optimistic signal price (state.py:545-548  
  Action: S — add one aggregation to the Friday scorecard + status.py: median and worst (fill−live_mid)/credit per week, entry and exit separately, plus commission share of gross. Define the numeric 'stable slippage' threshold NOW (gate 1 of PLAN.md cites it b
- **[PARTIAL] Execution benchmarks (fill rate, time-to-fill, reject rate)** — Raw events all exist: executor.py logs wide_spread_rejected (:345), combo_spread_disorderly_rejected (:467), trade_cancelled (:619-633), timeout ladder (:829-839); trades table has pending/filled/cancelled lifecycle; PLAN R2.10/R6 show velocity aware  
  Action: S — three counters in the existing daily digest (master.py daily_digest), derived from trades+executions tables: fills/attempts, median seconds-to-fill, rejects by reason. Reject-by-reason directly diagnoses the binding constraint on reaching 50 clos
- **[PARTIAL] Best-execution review cadence** — A weekly automated scorecard exists (master.py:917-919, Fri 16:10 ET Telegram) and R7 shipped a mid-first reprice ladder + NBBO-anchored limits — the mechanics of best-ex are worked. But the review content omits execution quality entirely, so no huma  
  Action: S — fold TCA + execution counters (rows above) into the existing Friday scorecard; a 10-minute monthly glance at the worst 5 fills is the full right-sized version. No committee, no report.
- **[PARTIAL] Model validation independent of development (holdout governance, champion/challenger)** — Solo operator = no organizational independence (inherent); the substitutes exist: walkforward harness with purge-gap fixes (R3), R6-exit-parity backtest + parameter-parity manifest (PLAN R7), GOV-2 model_version stamped on every trade, A8 trainer rol  
  Action: Right-sized as-is; the one adoption is discipline, not code: keep honoring the existing 'no optimizer output into live config' rule (AIT_ALLOW_APPLY gate already shipped) until R5-D5 is fixed. No further effort warranted pre-verdict.
- **[HAVE] Strategy capacity & sizing policy documented** — PLAN.md go-live gates §3: tuition sizing decision dated and versioned ($3,000 CAD, rev 2, 2026-07-07), phase-switch recipe with exact env/config knobs, R7-SOON budget-aware condor construction + launch-size coherence self-test at startup (asserts bud  
  Action: None. This is a model of right-sized sizing governance — the coherence self-test is better than what many funded shops have.
- **[PARTIAL] Risk limits reviewed on a schedule (vs set-and-forget)** — Limits are documented, evolving, and decision-logged (R2.8 VIX-28 halt + credit cap 6, R6 max_open_positions 8→10 with rejection-count rationale, MTM daily-loss brake A1) — far from set-and-forget in practice. But review happens only when an audit ro  
  Action: S — one line in RUNBOOK's Friday routine: monthly, compare each hard limit (VIX halt level, 1.25x credit loss cap, breaker size, position caps) against the realized distribution in the scorecard; note 'reviewed, unchanged' or change with a dated PLAN
- **[HAVE] Pre-trade risk checks** — Layered and executor-enforced: validate_trade sizing/caps with multiplier-before-validation fix (2.3), GOV-1 live-NBBO fat-finger + >35% signal-drift refusal at submit, INST-4 order-rate token bucket (>8/min = refuse + CRITICAL), INST-5 defined-risk-  
  Action: None. Coverage exceeds what's typical for this scale.
- **[HAVE] Post-trade surveillance** — Executions swept every check_fills (R7); daily EOD reconciliation vs broker with CLEAN/N-breaks Telegram + book-level −10% gap-stress number (GOV-4); untracked-position auto-freeze HALT_UNTRACKED (INST-3); assignment detection CRITICAL (R2.6); MTM da  
  Action: None beyond completing the dead-man ping (see DR row) — the surveillance itself is complete for a 1-lot book.
- **[PARTIAL] P&L attribution (greeks decomposition; at this scale, exit-reason attribution)** — Per-trade attribution inputs all exist: exit_reason_detailed on every close, leg-aware realized P&L (R3), real commissions (R7), model_version lineage (GOV-2). Nothing aggregates them: analyzer.py:589 uses exit_reason_detailed only as a row filter, a  
  Action: S — one SQL GROUP BY exit_reason (P&L, count, avg hold) in the weekly scorecard. This also supplies the data the R7 event-severity-tier decision explicitly says it lacks — whether flatten protection costs more than the tail it prevents.
- **[HAVE] Reconciliation cadence + break SLAs** — Daily EOD recon with per-break-type operator actions mapped in docs/RUNBOOK.md (alert→action table covers ASSIGNMENT, UNTRACKED, breaks); daily 2-minute routine is the de facto SLA; backup integrity-check + catch-up on stale start (R6).  
  Action: Optional S: add one sentence to RUNBOOK — 'any recon break must be resolved or explicitly accepted before the next open'. A formal SLA doc would be enterprise theater here.
- **[MISSING] Kill-switch drills (HALT tested live?)** — Mechanism shipped and documented (data/HALT checked at orchestrator.py:1493, RUNBOOK kill-switch table), but zero evidence of a single exercise: no 'manual halt file present' event in any retained log (grep across ait.log*/bot_stdout.log*), no HALT f  
  Action: S (15 min, this week, during RTH): touch data/HALT, confirm the halt log line + that the fast monitor still ticks exits, delete, confirm resume; repeat with a ticker in RESTRICTED.txt. Record 'drilled YYYY-MM-DD' in RUNBOOK. An untested kill switch i
- **[MISSING] Disaster-recovery drill (restore from backup rehearsed?)** — Backup side is solid: daily sqlite3 .backup 17:00 ET (master.py:915), snapshots present in data/backups/ (…20260710.db), mirror ~/Documents/ait_backups/ait_state.latest.db exists, integrity-check shipped R6. But: zero 'restore' mentions in RUNBOOK or  
  Action: S (30 min, once): copy latest snapshot to a temp dir, point status.py/sqlite3 at it, verify trades + open_positions readable, write the 5-line restore procedure into RUNBOOK; create the healthchecks.io check and paste the URL into data/deadman_url.tx
- **[MISSING] Benchmark selection (written baseline comparator for the strategy)** — Go-live gate is absolute (PF>1.3, DD<8%, PLAN.md gates); grep for benchmark/baseline across PLAN.md and docs/ finds only ML-internal uses (edge-over-baseline veto). Nowhere is it written what the 50-close result is compared AGAINST — a benign low-vol  
  Action: S — one paragraph in PLAN.md, written before the verdict: name the comparator (right-sized: period-average VIX + VRP note alongside the scorecard, plus 'T-bill yield on capital' as the hurdle; optionally a same-period naive benchmark — mechanical 0.2
- **[PARTIAL] Verdict statistics / sample-integrity governance (pre-registration, p-hacking defenses)** — The strong half is genuinely rare: criteria fixed before results, 'do NOT relitigate on a winning streak' (PLAN gates), TP change rejected because 'changing it changes the measured strategy' (R6), track-record reset dated 07-06, capital_at_risk retai  
  Action: S — two sentences in PLAN gates: (1) report a bootstrap/CI or at least win-rate ±2σ next to PF in the final verdict; (2) scorecard SQL tags closes before/after the last strategy-affecting change so a heterogeneous sample is visible. No new infrastruc
- **[HAVE] Incident management / postmortems** — PLAN round tables are a de facto incident log with root causes and dated fixes; the R5 'kill-mystery verdict' (WER forensics on c0000005 crashes, evidence-destruction root cause, recurrence playbook) is a real postmortem; 'Operating rules (learned th  
  Action: None. Format is unconventional but the function is fully served for a solo shop.
- **[N-A] Greeks-based portfolio risk decomposition / delta hedging governance** — Hedging module and portfolio-delta gate DEAD (greeks feed ~0, dormant inventory) — and correctly so: a defined-risk 1-lot condor book's risk is contractually capped by the long wings (R4 conditional pass rests exactly on this); the −10% gap-stress nu  
  Action: N-A at this scale. Revisit only if strangles ever earn live status (per gate 2's own 3-month rule).
- **[N-A] Broker-side protective orders (OCA brackets), HA/failover, secrets vault** — All three explicitly evaluated and right-sized OUT with recorded rationale (PLAN R4 deferred: 'no OCA gold-plating needed at this size'; R4b: vault deferred, dashboards bound to 127.0.0.1, IBC password outside repo).  
  Action: N-A — the project already did the right-sizing analysis; re-adding these would be the enterprise theater this assessment is told to avoid.

**Top 5:**
1. 1. Drill the untested last-resort layers in one 45-minute session (S): exercise data/HALT live and confirm exits keep managing, rehearse a DB restore from data/backups/ and write the 5-line procedure into RUNBOOK, activate the dead-man ping (data/deadman_url.txt is still absent), verify the backup folder cloud-syncs. Every control here already exists in code; none has ever been observed working — and the track record these protect is the entire product of this phase.
2. 2. Make 'stable slippage' measurable before it must be judged (S): aggregate the R7 executions table (signal_price/live_mid/nbbo_spread are already captured per fill, state.py:210-661) into a weekly median/worst slippage-as-%-of-credit line in the Friday scorecard, and fix the numeric threshold NOW — it is a go-live gate-1 criterion (PLAN.md) that currently has no measurement and no number, i.e., it will be graded by feel after seeing the results.
3. 3. Write down the benchmark and the verdict's statistics (S): one PLAN.md paragraph naming the comparator (period-average VIX/regime note + T-bill hurdle, optionally a naive mechanical-condor baseline) and committing to a confidence interval on the 50-close PF. Without this, a benign vol regime produces a PF>1.3 'pass' that is beta, not edge — the most likely way this project funds $3k on a false positive.
4. 4. Exit-reason P&L attribution in the scorecard (S): one GROUP BY exit_reason_detailed. It answers the question R7 explicitly says is unanswerable today — whether the macro-flatten/earnings protection regime costs more than the tail it prevents — which is also the data the pending event-severity-tier decision needs, and event blocks are a binding constraint on reaching 50 closes by mid-October.
5. 5. Execution benchmark counters in the daily digest (S): fill rate, median time-to-fill, rejects by reason, from tables that already exist. Close velocity is the critical path to the verdict; today the reject mix (spread gate vs position cap vs event block vs GOV-1 refusal) is discoverable only by reading logs, so the binding constraint on the project's schedule is invisible.

## SRE

**Ops maturity is reactive-heroic: strong in-band guardrails built after incidents, but the out-of-band SRE basics — armed dead-man switch, tested alert paths, a deploy smoke check, config inventory, clock/disk watching — are missing, and the 07-10 dead-loop Friday (green tests, 661 unpaged component-down states) is the direct bill for it.**

- **[PARTIAL] Golden signals for the trading loop (latency/traffic/errors/saturation)** — Errors+liveness well covered: per-component heartbeat/error-count/latency in src/ait/monitoring/watchdog.py; trading-loop heartbeat file + supervisor >15min-stale restart (PLAN.md R6, data/bot_heartbeat exists); marks-missing x10-tick alert (RUNBOOK.  
  Action: Log one structured line per trading cycle (cycle_ms, symbols scanned, candidates, orders, errors, marks age) — greppable history is the right-sized time series for one operator. Effort S.
- **[MISSING] SLOs with error budgets (even informal)** — Thresholds exist (heartbeat 15min, marks x10 ticks, watchdog error_threshold=10) but no target for how often protection must be ON. The 07-10 incident (commit c9bce20) ran a full RTH session with stops/TPs effectively OFF and nothing measured that ou  
  Action: Matters here because the product IS the 50-close track record — sessions with protection OFF are asterisks on the verdict. Write 3 informal SLOs in RUNBOOK.md (marks fresh <5min for 99% of RTH; loop heartbeat gap <15min always; digest delivered 2x/da
- **[PARTIAL] Alert policy doc (what pages vs what only logs)** — docs/RUNBOOK.md alert→action table is a real de facto policy for what pages. But page-vs-log is vibes at the source: order_rate_limit_tripped is log-CRITICAL only (RUNBOOK line 28), and watchdog component-down was log-only until 2026-07-11 (commit fa  
  Action: Grep every log.critical site, add a RUNBOOK column 'pages: yes/no', adopt the rule 'CRITICAL ⇒ Telegram, no exceptions'; wire the 2-3 stragglers. Effort S.
- **[MISSING] Alert testing (are Telegram paths ever test-fired?)** — No test-fire mechanism anywhere (grep: no test_alert, no startup 'bot up' send in src/ait/main.py or notifications/telegram.py). Proven cost: watchdog set_alert_callback existed but was NEVER called — 661 component-down states during 07-10 paged nobo  
  Action: A dead pager on a solo bot = nobody is on call. Add (1) a 'bot started, alerts OK' Telegram on every startup — free daily proof the channel works; (2) `status.py --test-alerts` that fires each distinct route (orchestrator notifier, master.py supervis
- **[PARTIAL] Runbook completeness + drill** — docs/RUNBOOK.md is genuinely good (alert→action, kill switches, 2-min daily routine, go-live checklist; GOV-5). Zero evidence any procedure has been rehearsed: DB restore-from-backup never performed, HALT-file flow untested by hand, and the runbook's  
  Action: One 30-minute drill before go-live: create/delete data/HALT and watch entries freeze; restore ait_state.latest.db to a scratch path and open it; fire --test-alerts; kill the bot process and time supervisor recovery. Log results in RUNBOOK. Effort S.
- **[MISSING] Deploy pipeline (verification steps, checklist)** — The repo IS production; commits go straight into the live-trading tree with pytest as the only gate. Commit c9bce20: R7 schema migration shipped with green tests, TradeRecord(**row) raised on every open-trade read, trading loop dead the whole Friday   
  Action: No canary/staging theater — a 10-line DEPLOY checklist in RUNBOOK.md: (1) pytest, (2) restart outside RTH unless hotfix, (3) verify data/bot_heartbeat fresh + one trading_cycle_complete log line + status.py green + open positions listed, (4) startup 
- **[PARTIAL] Config management (inventory + drift detection)** — Config sprawls across 4 layers: .env (secrets + AIT_LIQ_* paper relaxations), run_orchestrator.py os.environ.setdefault (AIT_MARKET_DATA_TYPE, AIT_ALLOW_UNDEFINED_RISK, AIT_SKIP_MACRO_EVENTS...), config.yaml (174 lines), and ~20 AIT_* vars read ad ho  
  Action: Go-live is literally 'flip 5 scattered vars in concert' (RUNBOOK checklist) — drift here corrupts the verdict. At startup, log one 'effective config' block (every AIT_* read + key yaml values) and persist it per session; add docs/CONFIG.md table (var
- **[PARTIAL] Monitoring-of-monitoring (dead-man switch)** — The plumbing shipped in R6: keeper_ait.bat lines 26-31 curl a healthchecks.io URL from data/deadman_url.txt every loop, and PLAN.md R6 says 'USER: create check + paste URL'. Verified today: data/deadman_url.txt DOES NOT EXIST — the external dead-man   
  Action: Zero code needed: 10 minutes at healthchecks.io, paste one URL into data/deadman_url.txt, then unplug-test it once. This is the only monitor that survives the whole box dying. Effort XS — do it today.
- **[PARTIAL] Capacity/resource monitoring (disk, memory)** — Memory: watched (watchdog.py _get_memory_mb, >500MB → DEGRADED). Disk: nothing — grep finds no free-space check anywhere; the RUNBOOK's own 'STATE DB BACKUP FAILED → check disk space' makes the human the disk monitor after the fact. Full disk = SQLit  
  Action: 10 lines in the existing 2-min health job: shutil.disk_usage on the data drive, CRITICAL Telegram below 5GB, WARN below 20GB. Effort S.
- **[PARTIAL] Log retention policy vs forensics needs** — Mechanics now work after two destructions: stdout rotation at 50MB (was 2GB unrotated, 'rotation had NEVER succeeded', crash dumps destroyed — master.py:148-166 comments), ait.log 20MB×10 after error spam 'destroyed forensic evidence' (R2.12), 30-day  
  Action: The track record is the product; its supporting logs should outlive it. Exempt logs/fatal.log + crash artifacts from cleanup, and have the monthly cleanup zip the pruned month into ~/Documents/ait_backups instead of deleting. Effort S.
- **[MISSING] Time synchronization monitoring** — No w32tm, no NTP check, no IBKR reqCurrentTime-vs-local comparison anywhere in src (grep). The codebase invested heavily in ET-pinning (A3, R3) — which all silently trusts an OS clock that has already run 2h off once. A wrong clock shifts entry windo  
  Action: ib_insync already exposes broker time: at startup and once daily, compare IB server time vs local; CRITICAL Telegram if |drift| > 60s. Add `w32tm /resync` to keeper_ait.bat startup. Effort S (~15 lines).
- **[PARTIAL] Dependency/security update policy** — Pins reconciled to the real venv in R5 (pyproject.toml comments); secrets handling mostly sane (.env untracked, only .env.example in git; IBC password outside repo per PLAN operating rules; Telegram token log-redaction fixed R3). But no update cadenc  
  Action: Right-sized = mostly freeze: do NOT chase updates mid-sample (dependency churn is the bigger risk to the 50-close record — the OpenMP segfault saga proves it). Policy: pip-audit once now and again at go-live; rotate the Telegram bot token before fund

**Top 5:**
1. 1. Arm the dead-man switch TODAY: create the healthchecks.io check and put its URL in data/deadman_url.txt (keeper_ait.bat:26-31 already pings it). XS effort, and it is the only monitor that fires when the whole machine — bot, supervisor, Telegram sender — is dead. It has been shipped-but-unarmed since 2026-07-09.
2. 2. Written deploy checklist + post-restart loop smoke (RUNBOOK section): pytest → restart outside RTH → confirm fresh data/bot_heartbeat, one completed trading cycle in logs, status.py green, startup Telegram received. This is the exact 5-minute check that would have turned the 07-10 dead-loop Friday (green tests, stops OFF all session) into a non-event.
3. 3. Test-fired alerting: send 'bot started, alert path OK' on every startup and add status.py --test-alerts covering each distinct sender (orchestrator, master.py supervisor, keeper). The watchdog pager was dead since birth and 661 component-down states paged nobody — an untested alert is indistinguishable from no alert.
4. 4. Effective-config dump at startup + docs/CONFIG.md inventory of all ~20 AIT_* vars across .env / run_orchestrator.py / config.yaml, with paper-vs-live values. Go-live is a multi-file config flip; the delayed-data default (AIT_MARKET_DATA_TYPE='4' outside the orchestrator) shows this drift class is already biting.
5. 5. Two 15-line silent-corruption sentinels in the existing health job: clock drift vs IBKR server time (CRITICAL >60s — this box has run 2h off) and disk free space (CRITICAL <5GB — full disk kills SQLite state and marks silently). Cheapest insurance for the two failure modes nothing currently watches.

## MLOPS

**MLOps maturity is lopsided: the direction ensemble got real MLOps (versioned artifacts, atomic saves, rollback, drift-settled accuracy) while the range model — the single live money gate for the iron-condor book — is unversioned, unmonitored in production, uncalibrated, and silently overwritten by any walkforward run (RangePredictor.train() unconditionally saves to models/range.pkl at range_predictor.py:300). Experiment tracking is genuinely good for a solo shop; the gaps that matter all cluster around one artifact and one missing feedback loop (settling gate decisions against realized outcomes).**

- **[PARTIAL] Model registry / artifact versioning** — Ensemble: timestamped versions, atomic tmp+os.replace write, rollback(), keep-last-5 cleanup (c:/Users/prith/Documents/Git/agent_trade/trade_v2/src/ait/ml/ensemble.py:423-489; models/ensemble_v-*.pkl on disk). But range.pkl and vol_magnitude.pkl are   
  Action: S — backtest/walkforward RangePredictor takes a save=False flag or writes into the run dir; mirror ensemble's versioned save + rollback for range.pkl. Already triaged R7-SOON ('range-model artifact separation', PLAN.md Round 7 remaining) — ship it be
- **[PARTIAL] Training/serving skew detection** — Feature-name contract is pickled with the model and predict() reindexes to it — but only in the ensemble (ensemble.py:366,410; R7:195 notes the fix was NOT propagated to range/directional/vol_mag). Known open skews: FRED macro features ffill'd with n  
  Action: S — shift(1) the macro series before reindex, propagate the reindex-safe predict to the other three predictors, and return None instead of falling back to another symbol's model. Skip automated skew detectors — at 8 symbols the three known leaks ARE 
- **[PARTIAL] Model performance monitoring in production (accuracy settled vs realized outcomes)** — Exists for the DIRECTION ensemble only: DriftDetector records at prediction (src/ait/bot/orchestrator.py:913-919) and settles against realized direction at exit (orchestrator.py:459-479, src/ait/ml/drift.py) — and only works since the R8 key-mismatch  
  Action: S — the R7 ~20-line addition: persist every range-gate decision (symbol, P, threshold, horizon, model_version, taken/vetoed) to SQLite and settle in-range/breached at expiry. Must start accumulating NOW during the paper phase or the 50-close verdict 
- **[MISSING] Calibration monitoring** — Only a crude single-bucket check on the direction model (drift.py:116-121: predictions with conf ≥0.80 expected ~80% accurate). The range model's P(in-range) is never calibrated at training time (no isotonic/Platt anywhere in range_predictor.py) and   
  Action: S — piggyback on the gate scoreboard (row above): a monthly reliability readout, mean predicted P vs realized in-range rate per 0.1 bucket, in the EOD/weekly digest. Defer actual recalibration (isotonic) until >100 settled predictions; at this scale 
- **[PARTIAL] Data quality SLAs on features** — DataQualityValidator exists and validate_historical IS wired at prediction time (orchestrator.py:875-878). But validate_quote and validate_option_quote have zero external callers (grep: only quality.py:134 self-reference) — dead exactly where data ri  
  Action: S — wire validate_option_quote into the chain filter (or delete it and rely on the already-triaged R7-SOON chain-snapshot quality telemetry, one honest path not two half-dead ones); freeze/delete the 8 dead feature columns. No formal SLAs/expectation
- **[PARTIAL] Feature documentation / lineage** — Mechanical lineage is decent: feature_names + per-symbol feature_importances are persisted inside the model pkl (ensemble.py:353-371), FeatureEngine.get_feature_names() is the single source of the contract (features.py:417), and EXPERIMENTS_INSIGHTS.  
  Action: S — one FEATURES.md table: name, source, live/dead/proxy, since-when. ~30 minutes; every audit round so far has re-derived this from scratch.
- **[HAVE] Retrain trigger policy (time vs drift)** — Hybrid and right-sized: weekly time-based (config.yaml:109 retrain_interval_days: 7) plus drift-triggered (trainer.py:50-81; accuracy <40% over a 50-prediction window, calibration-error trigger too). Caveat: the drift path needs 20 settled samples in  
  Action: S (optional) — persist the drift deque to data/ next to thompson_state.json so restarts don't zero the trigger. Policy itself needs no change.
- **[MISSING] Champion/challenger or shadow deployment for retrains** — Weekly retrain hot-swaps ensemble.pkl in place. The only acceptance gate is _should_rollback (trainer.py:154-174, 232-244): rollback if all-symbol-mean CV accuracy drops >5pp vs previous — but prev and new CV scores are computed on DIFFERENT data win  
  Action: M — full shadow/champion-challenger is theater at one box and one trade a day. Minimal version (~half a day): before promoting, score BOTH old and new artifacts on the SAME most-recent held-out slice; promote only if new >= old − epsilon; apply to al
- **[HAVE] Experiment tracking** — Genuinely good for a solo shop: every walkforward run archived under reports/runs/ with run_metadata.json + config_snapshot.yaml; idempotent MLflow import keyed on run_id tag (scripts/backfill_mlflow.py; GUIDE.md:1721-1727 documents mlflow ui against  
  Action: None. Do not auto-log to MLflow from the bot; the file-based archive is the system of record and it works.
- **[PARTIAL] Model cards / assumption documentation** — The DORMANT models have real methodology docs (docs/GARCH_METHODOLOGY.md, docs/OU_KOU_GARCH_METHODOLOGY.md, docs/ENSEMBLE_OOS_ASSESSMENT.md) — ironically better documented than the live one. The LIVE range model's trust boundary exists only as scatte  
  Action: S — one-page docs/RANGE_MODEL.md card: what it predicts, label definition, threshold/horizon, known label-reality mismatch, distrust conditions (immediately after any backtest run until artifact separation lands; unseen symbol; regime break; <N settl
- **[MISSING] Rollback procedure tested** — ensemble.rollback() exists (ensemble.py:476-489) but zero tests reference rollback (grep tests/: no hits), and the surrounding trainer logic has already shipped broken once (R7: rollback keyed on last-symbol-only cv_scores, patched A8 at trainer.py:1  
  Action: S — one unit test (train v1, train degraded v2, assert rollback restores v1 outputs) + one RUNBOOK row with the exact load-version command. Becomes trivial once range.pkl versioning (row 1) lands; do them together.

**Top 5:**
1. 1. Range-model artifact separation + versioned save/rollback (S) — walkforward must stop writing models/range.pkl (range_predictor.py:300 saves unconditionally; walkforward.py:76-88 trains per window). One flag plus copying ensemble.py:423-489's versioning pattern removes the proven backtest-hijacks-live-gate failure AND creates the rollback artifact rows 8 and 11 need. Already user-triaged as R7-SOON; everything else on this list is second to it.
2. 2. Range-gate live scoreboard settled at expiry (S) — persist every gate decision (P, threshold, model_version, taken/vetoed) and settle in-range/breached at expiry (the R7 NOW item 168, ~20 lines on the existing counterfactual recorder). This is the only path to production accuracy AND calibration monitoring for the model the money actually flows through, and it must accumulate during the paper phase or the 50-close verdict cannot separate strategy edge from gate value.
3. 3. Same-window retrain acceptance gate for all three predictors (M) — score old and new artifacts on the same held-out recent slice before promoting; current _should_rollback (trainer.py:232-244) compares CV scores from different weeks (regime-confounded) and range/vol-mag have no gate at all. This is the right-sized champion/challenger: half a day, no infra.
4. 4. Close the three known train/serve skews (S) — shift(1) FRED macro series before reindex (features.py:897-946, the one-day-smarter leak known since R5), propagate the reindex-safe predict from ensemble.py to range/directional/vol_mag, and refuse prediction instead of scoring symbol A with symbol B's model+scaler (R7:139). Each is a few lines; together they make training and serving see the same world.
5. 5. The documentation-and-ops trio (S, ~half a day total) — docs/RANGE_MODEL.md model card (distrust conditions for the live gate), FEATURES.md live/dead/proxy table (R7: readers assume coverage that does not exist), and one tested rollback path with a RUNBOOK row. Cheap insurance that the solo operator six months from now — or the funded go-live decision — doesn't trust the model outside its envelope.

## SDLC

**Strong test corpus (55 test files) and ops hardening, but zero delivery automation: no CI, no hooks, no lockfile, no tags, and no post-deploy gate — the 2026-07-10 dead-session incident (migration column killed TradeRecord mapping, stops/TPs off all day) was a delivery-practice failure that a one-clean-cycle smoke check would have caught.**

- **[MISSING] CI — tests run automatically on push** — No .github/, no gitlab/azure/jenkins config anywhere in trade_v2 or parent dir. 55 test files in tests/ run only when the AI assistant remembers; PLAN.md R5 notes 'two red tests fixed' — they had sat red unnoticed. Confirmed: there is no CI at all.  
  Action: Single GitHub Actions workflow: ruff check + `pytest` (addopts already excludes -m ibkr live tests, so it runs green offline). Linux runner is fine for unit tests. Effort S (~1 hour). At this scale CI is the only thing that runs tests when the human/
- **[MISSING] Pre-commit hooks (lint/format/compile)** — No .pre-commit-config.yaml; .git/hooks contains only samples. Ruff IS configured in pyproject.toml ([tool.ruff], E/F/I/N/UP selects) but nothing enforces it — config without a trigger.  
  Action: pre-commit with ruff + ruff-format + check-yaml. Effort S. Cheap insurance for a repo where hotfixes ship same-day to a live loop (see commit c9bce20 'HOTFIX').
- **[MISSING] Test coverage measurement + floor** — pytest-cov is in dev extras (pyproject.toml:60) but never invoked: no --cov in addopts, no [tool.coverage] section, no floor, coverage never measured.  
  Action: Add --cov=src/ait --cov-report=term to the CI run as INFORMATION only; skip a hard global floor (enterprise theater at this scale). If anything, ratchet only executor/state/risk modules. Effort S.
- **[MISSING] Branch / release strategy** — Everything ships on feature/event-straddle and deploys from its working tree (start_bot.bat cd's to the checkout and runs run_orchestrator.py). main last commit 2026-04-30 (bf6e85d), 219 commits behind; origin/HEAD points at a main whose last merge w  
  Action: One-time cleanup: merge feature/event-straddle → main, deploy from main, delete/archive the 6 stale branches, use short-lived branches only for risky experiments. Effort S. Matters mainly because the misnamed trunk confuses rollback and any future co
- **[PARTIAL] Deploy checklist as code (post-deploy smoke: one clean cycle)** — Pieces exist: launch-coherence self-test at startup (PLAN.md:134), heartbeat + supervisor hang-detection (PLAN.md:145), scripts/run_integration_test.py, watchdog→Telegram (faa42f7). But nothing gates a deploy: the 07-10 incident (state.py:695 comment  
  Action: Codify the 07-10 lesson: scripts/smoke_deploy.py run after every restart-with-new-code — load state, read all trade rows through _row_to_trade, run one monitor tick against paper Gateway, then Telegram 'DEPLOY OK/FAIL <commit>'. Add 'run smoke after 
- **[PARTIAL] Schema migration discipline** — Migrations are an ad-hoc ALTER TABLE list inline in src/ait/bot/state.py (~line 193), no version numbers. Post-incident mitigations exist: _row_to_trade filters to known dataclass fields with logged drift (state.py:695), and tests/test_audit_fixes.py  
  Action: Add SQLite `PRAGMA user_version` + an ordered migration list; keep the round-trip test running the full chain against a copy of the prod schema. Skip migration frameworks (alembic = overkill for one SQLite file). Effort S.
- **[MISSING] Rollback capability (last-known-good tagged?)** — `git tag` returns nothing — zero tags in the repo. Deploy is 'whatever the working tree holds'; rolling back means reading git log and guessing which of the same-day hotfix commits (c9bce20) was good. DB backups exist (RUNBOOK Friday check of ait_sta  
  Action: After each session the smoke check passes, `git tag -f known-good`. One RUNBOOK line: 'bad deploy → git checkout known-good, restore latest DB backup, restart'. Effort S. Pairs with the smoke check; cuts bad-deploy recovery from a debugging session t
- **[HAVE] Secrets management** — Telegram token + API keys live in .env (gitignored — confirmed via git check-ignore; .gitignore '# Secrets' section), committed .env.example template, loaded via pydantic-settings (src/ait/config/settings.py:274). IB credentials in C:/IBC/config.ini   
  Action: Nothing now. At go-live with real money, the only right-sized upgrade is Windows account-level file ACL on C:/IBC/config.ini; a vault is enterprise theater for one machine.
- **[PARTIAL] Dependency pinning / lockfile** — No requirements.lock/requirements.txt anywhere (confirmed). pyproject.toml has lower-bound ranges only; the R5 comments in it are the scar tissue ('venv runs numpy 2.4/torch 2.10 — the old <2.0 pin was a dead letter') — pins were reconciled to the ve  
  Action: `pip freeze > requirements.lock`, commit it, refresh only on deliberate upgrades; install on rebuild with `pip install -r requirements.lock`. Effort S (10 minutes). This is the cheapest insurance in the whole matrix.
- **[PARTIAL] Reproducible environment (rebuild on a new machine)** — Honest estimate: 1-2 days with real divergence risk. Exists: .env.example, backfill scripts (scripts/backfill_*), DB backups to Documents/ait_backups (cloud sync was still a USER todo in PLAN.md:145). Missing: lockfile; data/ models/ logs/ all gitign  
  Action: SETUP.md: exact Python/Gateway/IBC versions, `schtasks /query` export of the two tasks, IBC config keys (not values), restore-from-backup steps. Plus the lockfile above, plus confirm ait_backups actually cloud-syncs. Effort M (half a day). This is di
- **[PARTIAL] Documentation currency policy** — R5 fixed the symptom: STALE — DO NOT TRUST banners on 10 files (README.md:2, CONTEXT.md:2, GUIDE.md:2) declaring 'PLAN.md is the only current source of truth'. RUNBOOK.md is short and current. But no policy prevents recurrence: nothing defines which   
  Action: Move banner-ed docs to docs/archive/, add 2 lines to PLAN.md header: 'Maintained docs: PLAN.md + docs/RUNBOOK.md. Everything else is archive — do not update, do not trust.' Effort S. Matters here more than most shops because the primary maintainer is

**Top 5:**
1. 1. Post-deploy smoke as code (S-M): scripts/smoke_deploy.py — load state, map every trade row, one monitor tick, Telegram DEPLOY OK/FAIL. Directly converts the 07-10 full-session outage class (silent dead loop with stops/TPs off) from 'discovered next day' to 'caught in 5 minutes'. Highest expected value for an autonomous bot.
2. 2. Minimal CI (S): one GitHub Actions workflow running ruff + pytest on push. 55 test files currently run only on memory; red tests have already sat unnoticed (PLAN.md R5). This is the only mechanism that tests the code when nobody remembers to.
3. 3. Lockfile (S, 10 minutes): commit `pip freeze > requirements.lock`. Venv drift already bit once (R5 pin reconciliation in pyproject.toml), py3.13+torch resolution is fragile, and it is a prerequisite for any machine rebuild.
4. 4. known-good tag + rollback one-liner (S): tag after each smoke-passing session; RUNBOOK line 'checkout known-good + restore latest DB backup'. Zero tags exist today — rollback is currently git-log archaeology during a live incident.
5. 5. Machine-rebuild path (M): SETUP.md (Gateway/IBC/scheduled-task setup, restore-from-backup) + confirm ait_backups cloud sync. The bot lives on one aging Windows 10 box with proven machine-specific quirks; today its death costs days and possibly the paper-verdict continuity.
