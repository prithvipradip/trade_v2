# deprecated/ — retired files (kept for history, not for use)

Nothing in this tree is loaded by the live system. Files here are retained
because they document how decisions were made, but every one of them is
superseded. **PLAN.md (repo root) is the only current source of truth.**

| Folder | Contents | Why retired |
|---|---|---|
| docs/ | CONTEXT/GUIDE/TODO/EXPERIMENTS_INSIGHTS + old plan/inconsistency docs | Stale-dangerous per Round 5 audit (2026-07-08): pre-audit sizing formulas, invalidated Sharpe/return numbers, dead-feature claims. All carry warning banners. |
| configs/ | config_QQQ_test.yaml, config_SOFI_test.yaml | Omit the post-audit guardrail blocks (10 contracts/trade, FinBERT crash-loop, no PDT). Never loadable by live paths; tests build their own copies. |
| ops/ | autostart_ait.bat, tail_logs.py | autostart superseded by keeper_ait.bat (Startup shortcut) + guarded start_bot.bat (scheduled task). tail_logs superseded by web_logs.py. |
| scripts/ | plot_exp17_coupling.py | Hardcoded to Experiment 17 run dirs; silently compares wrong runs (R5 verdict: obsolete/fragile). |
| reports/ | daily_2026{03..06}*.json | Generated by pre-Round-5 analytics math (sleeve-capital inflation, sqrt(252) annualization) — numbers are invalid; do not cite. |
| src/hedging.py | DeltaHedger (was ait/risk/hedging.py) | R12-C item 1: DANGEROUS dead code — the auto-execute path placed SPY stock MARKET orders bypassing every executor guardrail (rate limit, INST-5, GOV-1, market-hours); one `auto_hedge: true` flip from live. Greeks feed never populated, so it never ran — retired before it could. |
| src/sentiment/ | SentimentEngine, FinBERT, news, fear/greed (was ait/sentiment/) | R12-C item 3: verified zero influence on iron-condor decisions (R7 traced the consult sites; ML features were constant). FinBERT's torch dependency implicated in the c0000005 crash cluster. Salvage if ever wanted: a ~30-LOC VIX backwardation gate in risk/. |
| src/ib_news.py, src/fundamentals_db.py | IBNewsService + news/analyst SQLite store (was ait/data/) | R12-C item 3: fed only the retired sentiment stack; nothing read the tables. (ait/data/equity_stats.py is NOT retired — `--refresh-fundamentals` still uses it.) |
| src/options_flow.py | OptionsFlowAnalyzer (was ait/data/) | R12-C item 5: flow gate/boost removed from the orchestrator; put/call + block-trade signals never influenced a fill decision. |
| src/directional.py | DirectionalModel (was ait/ml/) | R12-C item 5: dead — no live caller; engine's lazy loader had no callers either. |
| src/options_sim.py | OptionsSimulator + position classes (was ait/backtesting/) | R12-C item 5: dead class half with a loaded 5x-premium short-side max-loss trap. The stateless BS functions the engine actually used moved to src/ait/backtesting/pricing.py. |
| src/covered.py | CoveredCall, CashSecuredPut (was ait/strategies/) | R12-C item 5: naked-call generator — signals never checked share ownership. Never in config.yaml's strategy list. |
| src/calendar.py, src/event_straddle.py | CalendarSpread, EventStraddle (was ait/strategies/) | R12-C: never enabled in config.yaml; selector keeps behavior-identical `return []` stubs because the orchestrator calls them unconditionally. |
| src/tests/ | test_event_straddle.py, test_fundamentals.py | Whole-module tests of the retired strategies/stores above. |
| research/ | garch_range_predictor.py, msgarch.py, ou_jump.py (was ait/ml/) | R12-C: GARCH/MS-GARCH/OU-Kou-GARCH ensemble members — flags-off in production, ~2,900 src LOC and ~1h of CI out of the live tree. Research material, not superseded; resurrect INTO research tooling only. |
| research/tests/ | test_garch_range_predictor.py, test_msgarch.py, test_oujump.py, test_rng_isolation.py | The GARCH family's test suites, plus the RNG-isolation test whose only subject (spawn-isolated GARCH training in walkforward) was deleted with the family. |

Policy: retire, don't delete — but nothing gets resurrected from here without
re-review against PLAN.md's current state.
